# Task: Refactor crafter-do Slice 6 — extract Steps 6 / 6b / 6a (Review + Phase Summary + Session Break) into core capability modules

## Metadata
- **Date:** 2026-05-18
- **Work branch:** refactor/crafter-do-slice-6-steps-6-6b-6a
- **Status:** completed
- **Scope:** Medium

## Request
Slice 6 of the crafter-do core-capability decomposition (the highest-density slice — the design note groups Steps 6/6b/6a together because they share flag-state and approval-path logic). Extract the `## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, and `## Step 6a — Session Break (Medium/Large scope only)` sections of `skills/crafter-do/SKILL.md` into three new internal core capability modules under `rules/do/` — `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, `rules/do/step-6a-session-break.md` — per the taxonomy in `docs/core-capabilities.md`, following exactly the move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slices 1–5.

Constraints:
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume).
- Pre-extraction binding gate (same as Slice 2/3/4/5 step 1.1, GO/NO-GO, headline R1): validate that Step 6's mandatory review STOP gate, the 5-iteration fix-loop cap with the (a)/(b)/(c) user choice, the `crafter-reviewer`/`crafter-implementer` delegations, the mandatory output format, and the "re-run Step 5a / re-run Step 6" routing; Step 6b's `--auto` branch, the three non-`--auto` approval paths, the green-commit invariant references, the manual-verification exception, and the multiple orchestrator-side task-file Decision-append / commit bindings (HEADLINE R1, analogous to Slice 4/5); and Step 6a's session-break routing into Steps 5a/6/7–9 and back to Step 4 — all survive the pointer stubs. Confirm no other section quotes Step 6/6b/6a prose verbatim.
- Inbound references to include in the gate + 1.5 sweep: `rules/do/step-4-execute.md` "run … Step 6" routing; `rules/do/step-5a-phase-verification.md`; the Step 6b "After committing, continue to Step 6a or Steps 7–9" routing; the Step 6 fix-loop "Re-run Step 5a / re-run Step 6"; any "Step 6"/"Step 6a"/"Step 6b" mention in `step-0-resume.md` or other extracted modules; and the `--fast`/`--auto` Skill options blocks that say "See Step 6b".
- HEADLINE R1: Step 6b contains multiple orchestrator-side task-file bindings (Decision-append for auto-fixed/tech-debt entries, `crafter-buffer` UAT recording, the commit-per-`post-change.md` instruction, and the manual-verification wait). The Step 6b stub MUST preserve these explicitly and imperatively, mirroring the Slice 4/5 headline-binding treatment.
- Pure move-and-link only: no prose rewriting, no reordering. Retain the `## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, `## Step 6a — Session Break (Medium/Large scope only)` headings as binding-preserving pointer stubs matching the established Step 0/1/2/3/4/5 stub convention ("Apply the … procedure in …. This procedure …" opening, Interpretation A, lowest drift, no secondary path re-citation). No other sections touched. Preserve the source order: Step 6, then Step 6b, then Step 6a (their actual order in SKILL.md) in the stubs and loader list.
- Installer + `tests/test_install.sh` need one mechanical entry per new module (per Slice 2 R2). Three new modules in this slice.
- Out of scope: any other Step extraction, extension-skill semantic changes, agent edits, CLI/Go changes, repo-wide path normalization.

## Plan

**Plan status:** approved

### 1. Complete request

Extract the `## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, and `## Step 6a — Session Break (Medium/Large scope only)` sections of `skills/crafter-do/SKILL.md` into three new internal core capability modules — `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, `rules/do/step-6a-session-break.md` — applying the exact move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slice 1 (`20260517-refactor-crafter-do-core-capabilities.md`), Slice 2 (`20260517-refactor-crafter-do-slice-2-step-0-resume.md`), Slice 3 (`20260517-refactor-crafter-do-slice-3-steps-1-2.md`), Slice 4 (`20260517-refactor-crafter-do-slice-4-steps-3-4.md`), and Slice 5 (`20260518-refactor-crafter-do-slice-5-steps-5-5a.md`). This is Slice 6 of the deferred follow-up sequence — the **highest-density slice**. The design note explicitly groups Steps 6/6b/6a as one slice "since they share flag-state and approval-path logic."

All three section bodies move verbatim into their respective modules. Each `## Step …` heading in `SKILL.md` is reduced to a thin pointer stub matching the established Step 0/1/2/3/4/5 convention ("Apply the … procedure in …. This procedure …" opening) that (a) keeps the exact heading text so every existing "Step 6"/"Step 6b"/"Step 6a" reference elsewhere still resolves and (b) preserves the binding contracts the prose carried. Source order — Step 6, then Step 6b, then Step 6a (their actual order in SKILL.md) — is preserved in the stubs and the loader list.

The single highest-value binding cluster (HEADLINE R1) is in **Step 6b**: it carries multiple orchestrator-side task-file/commit bindings — under `--auto`, the orchestrator appends `Decision (Auto-Fixed)` and `Decision (Tech Debt — auto-recorded)` entries to `## Decisions`, records manual-verification requirements as UAT buffer entries via the `crafter-buffer` skill, and commits automatically per `post-change.md`; in the three non-`--auto` paths it gates the commit on auto-approve / `--fast` silence-as-approval / explicit approval, honors the manual-verification exception, and on approval (any path) runs the commit per `post-change.md`. **Step 6** carries the secondary headline cluster: the mandatory output format (reproduce the Reviewer's tables verbatim, never convert to prose), the **mandatory review STOP gate** (always wait for the user's response when findings exist, never auto-proceed), the zero-findings → Step 6b auto-approve path, and the Critical/Major fix loop with the **5-iteration cap** and the **(a)/(b)/(c) user choice** (manual override / accept-without-commit / replan-and-abort) plus the `--auto` exit-with-state note. Their preservation is non-negotiable, exactly analogous to Slice 4's `draft → approved` and Slice 5's `Decision (...)`-append headline bindings. The loader list gains three entries under the existing `<!-- do/* capability modules -->` group, after `step-5a-phase-verification.md`.

Why this matters: `crafter-do/SKILL.md` is the entry point every Crafter workflow loads. Step 6 owns code-review delegation to `crafter-reviewer`, the supplemental-only extension-skill check, the mandatory table-reproduction output contract, the mandatory STOP gate, the Critical/Major fix loop with the 5-iteration cap and the (a)/(b)/(c) escalation, the `crafter-implementer` fix re-delegation, and the "re-run Step 5a / re-run Step 6" routing. Step 6b owns the entire post-review approval surface — the `--auto` Decision-append + `crafter-buffer` UAT-record + commit chain, the three non-`--auto` approval paths (auto-approve / `--fast` silence-as-approval / explicit), the manual-verification exception, the green-commit invariant references, and the "After committing, continue to Step 6a or Steps 7–9" routing. Step 6a owns the Small-scope skip, the three session-break routing rules, and the Step 0 resume hand-off. Pulling them into named modules continues shrinking the monolith and lets review/approval/session semantics evolve in focused files, exactly as Slices 1–5 did. The three steps are a natural group: they share flag-state (`--auto`/`--fast`) and approval-path logic, so extracting them together keeps that coupling reviewable in one slice — as the design note recommends.

**Acceptance criteria:**

- Three new modules exist:
  - `rules/do/step-6-review.md` containing the full `## Step 6 — REVIEW` prose verbatim (single `# Title` H1, no carried-over `## Step 6` H2 — same convention as the Slice 1–5 modules). Phase 2 normalizes its concrete runtime paths to `{CRAFTER_HOME}/...` (the `~/.claude/crafter/rules/do/extension-skills.md` and `~/.claude/crafter/rules/task-lifecycle.md` references; re-confirm at extraction time).
  - `rules/do/step-6b-phase-summary.md` containing the full `## Step 6b — Phase Summary and Auto-Commit` prose verbatim (single `# Title` H1, no carried-over `## Step 6b` H2). Phase 2 normalizes its multiple concrete `~/.claude/crafter/rules/post-change.md` runtime paths to `{CRAFTER_HOME}/...` (re-confirm count at extraction time); `skills/crafter-buffer/SKILL.md` is NOT a `~/.claude/crafter/...` install path and is left unchanged per the established convention (bare cross-skill mention, like `rules/do-workflow.md` anchors).
  - `rules/do/step-6a-session-break.md` containing the full `## Step 6a — Session Break (Medium/Large scope only)` prose verbatim (single `# Title` H1, no carried-over `## Step 6a` H2). Step 6a's body contains NO concrete `~/.claude/crafter/...` runtime path (bare Step-number mentions are not runtime paths); Phase 2 applies ZERO substitutions to this module — re-confirm at extraction time.
- `skills/crafter-do/SKILL.md` keeps all three headings (`## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, `## Step 6a — Session Break (Medium/Large scope only)`) followed by short pointer stubs matching the established Step 0/1/2/3/4/5 convention (module pointer via `~/.claude/...` + bindings stated in plain prose, NO secondary path re-citation — per the Slice 3 `## Decisions` ruling). The **Step 6b stub** MUST explicitly and imperatively preserve: the `--auto` branch (Decision-append for `Decision (Auto-Fixed)` + `Decision (Tech Debt — auto-recorded)` entries, `crafter-buffer` UAT recording, commit automatically per `post-change.md`, plus the green-commit-invariant reference); the three non-`--auto` approval paths — (1) auto-approve on a fully clean summary, (2) `--fast` silence-as-approval with tech-debt recording, (3) explicit approval where silence is NOT approval; the manual-verification exception (overrides auto-approve, not bypassed by `--fast`); the "On approval (any path), run the commit per `post-change.md`" binding; and the "After committing, continue to Step 6a or Steps 7–9" routing. The **Step 6 stub** MUST explicitly preserve: the supplemental-only extension-skill check; `crafter-reviewer` delegation; the mandatory output format (reproduce the Reviewer's Diff summary / Issues found / Karpathy scorecard / Contract deviations tables verbatim, never convert to prose); the **mandatory review STOP gate** (always wait for the user's response when findings exist, never auto-proceed); the zero-findings → Step 6b auto-approve path; the Critical/Major fix loop with the **5-iteration cap** and the **(a)/(b)/(c) choice** (manual override / accept-without-commit / replan-and-abort) plus the `--auto` exit-with-state note; `crafter-implementer` fix delegation; "Re-run Step 5a (PHASE VERIFICATION)"; "re-run Step 6 (REVIEW) from the top"; and the record-decisions-per-`task-lifecycle.md` instruction. The **Step 6a stub** MUST explicitly preserve: "Skip this step for Small scope"; the three session-break routing rules (last step in phase → Step 5a + Step 6; last step in plan → Steps 7–9; otherwise → suggest `/clear` + re-invoke, or back to Step 4 (EXECUTE)); and the "resume detection in Step 0 will pick up…" hand-off.
- The loader list in `SKILL.md` includes `~/.claude/crafter/rules/do/step-6-review.md`, `~/.claude/crafter/rules/do/step-6b-phase-summary.md`, and `~/.claude/crafter/rules/do/step-6a-session-break.md` under the existing `<!-- do/* capability modules -->` group, after `step-5a-phase-verification.md` (order: step-6-review → step-6b-phase-summary → step-6a-session-break, the SKILL.md source order; loader-list/pointer paths intentionally stay `~/.claude/...`, same carve-out as Slices 1–5).
- `install.sh` deploys all three new modules (one `cp` line each after the `step-5a-phase-verification.md` line at install.sh:330; `mkdir -p "$rules_dest/do"` already exists) and `tests/test_install.sh` expects all three (one entry each after the `step-5a-phase-verification.md` entry at tests/test_install.sh:304); `tests/test_install.sh` passes.
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume). Every inbound reference still resolves to the retained `## Step 6` / `## Step 6b` / `## Step 6a` headings: `rules/do/step-4-execute.md` line 12 ("…run Step 5a (phase verification) and Step 6 (phase review)"); SKILL.md Step 4 stub line 118 ("…and to Step 6 (phase review)"); the `--fast` Skill-options block line 45 ("See Step 6b for the approval path"); the `--auto` Skill-options block (the "See Step 6b" approval-path mention, if present — confirm in the 1.1 gate); Step 6b's internal "After committing, continue to Step 6a or Steps 7–9" routing; Step 6's fix-loop "Re-run Step 5a / re-run Step 6 from the top"; Step 6a's "proceed to Step 5a (Phase Verification) and Step 6 (Review)" and "go back to Step 4 (EXECUTE)"; Steps 7–9's "The per-phase commit for the final phase has already landed via Step 6b" (line 241); Step 9b references that depend on Step 6b having committed; and any "Step 6"/"6a"/"6b" mention in `step-0-resume.md`/`step-1-scope.md`/`step-2-discuss.md`/`step-3-plan.md`/`step-5-drift.md`/`step-5a-phase-verification.md` (the 1.1 / 1.6 sweep confirmed none of the extracted sibling modules currently reference Step 6/6a/6b — re-verify). Every forward reference from the extracted prose (to `crafter-reviewer`, `crafter-implementer`, `crafter-buffer`, Step 5a, Step 6, Step 6a, Steps 7–9, Step 4) still resolves.
- Two independently committable phases, each with a per-phase verification gate and a `crafter-reviewer` gate; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.

**Validation strategy:** Editorial + installer test. Re-reading the post-extraction `SKILL.md` for the four flows produces identical semantics; a literal diff of each moved block against the original shows only the mechanical H2→H1 normalization (Phase 1) and the enumerated `{CRAFTER_HOME}` substitutions (Phase 2); `tests/test_install.sh` passes after the installer + test edits.

### 2. Assumptions / interpretations

- **Module paths are fixed by the design note:** `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, `rules/do/step-6a-session-break.md` (taxonomy table rows for Step 6 / Step 6b / Step 6a). The `rules/do/` subdirectory was confirmed in Slice 1 and reused in Slices 2–5; this slice follows it without re-deciding.
- **Module structure mirrors the Slice 1–5 modules:** single `# Title` H1 (e.g. `# Step 6 — REVIEW`, `# Step 6b — Phase Summary and Auto-Commit`, `# Step 6a — Session Break (Medium/Large scope only)`, or semantically equivalent H1s), no carried-over H2, prose otherwise verbatim. Exact H1 wording is the Implementer's choice within the move-and-link contract; it must not change meaning.
- **`{CRAFTER_HOME}` enumeration (re-confirm at extraction time):** Step 6 body — two concrete runtime paths: `~/.claude/crafter/rules/do/extension-skills.md` (extension-skill check) and `~/.claude/crafter/rules/task-lifecycle.md` (record-decisions). Step 6b body — multiple concrete `~/.claude/crafter/rules/post-change.md` runtime paths (in the `--auto` branch, paths (1)/(2), and the Commit section); `skills/crafter-buffer/SKILL.md` is a bare cross-skill mention, NOT a `~/.claude/crafter/...` install path, and stays unchanged (same treatment as `rules/do-workflow.md → ###` anchors and bare agent names in Slices 2–5). Step 6a body — ZERO concrete runtime paths (bare Step-number mentions only). The Implementer must re-scan each extracted block at extraction time and normalize exactly the enumerated `~/.claude/crafter/...` occurrences to `{CRAFTER_HOME}/...`; if the count differs, flag the deviation. Bare `rules/do-workflow.md → ###` anchors and bare `crafter-*` agent/Step mentions are not runtime paths and stay unchanged. SKILL.md loader-list and pointer-stub lines stay `~/.claude/...` (installer-resolved carve-out) — identical to Slices 1–5.
- **Installer change IS required (×3), per Slice 2 R2 (confirmed, not "likely none").** `install.sh` enumerates each `rules/do/*.md` file explicitly (the `step-5a-phase-verification.md` line is install.sh:330) and `tests/test_install.sh` enumerates each expected file (the `step-5a-phase-verification.md` entry is tests/test_install.sh:304). Three new modules therefore need exactly three mechanical `cp` lines in `install.sh` and three expected-path entries in `tests/test_install.sh`. `mkdir -p "$rules_dest/do"` already exists — no directory-creation change.
- **Lower-drift cross-reference strategy (chosen — Interpretation A):** keep all three `## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, `## Step 6a — Session Break (Medium/Large scope only)` headings in SKILL.md as thin pointer stubs rather than deleting the headings and rewriting every referrer. This preserves all existing "Step 6"/"6b"/"6a" references (Step 4 module routing, Step 4 stub, `--fast`/`--auto` Skill-options blocks, inter-step routing between 6/6b/6a, Steps 7–9, Step 9b) with zero edits to other sections — strictly lower drift, exactly the conservative posture Slices 1–5 used. Justified in §7.

**Competing interpretations:**

- *Interpretation A (chosen):* thin pointer stubs under the retained `## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, `## Step 6a — Session Break (Medium/Large scope only)` headings; referrers untouched. Same posture as Slices 1–5.
- *Interpretation B (rejected, as in Slices 2–5):* delete the Step 6/6b/6a headings, move all content out, and update every "Step 6"/"6b"/"6a" referrer (Step 4 module, Step 4 stub, `--fast` Skill-options "See Step 6b", `--auto` Skill-options, the 6→6b→6a inter-routing, Steps 7–9 "via Step 6b", Step 9b) to point at the modules. Rejected — much higher drift on the densest section in the file, touches sections and a sibling module (`step-4-execute.md`) outside the extraction targets, and risks weakening the HEADLINE R1 Step 6b orchestrator-side commit/Decision-append bindings and the Step 6 STOP gate / 5-iteration cap. If the user prefers B, Phase 1 changes shape and the cross-reference sweep expands.

### 3. Non-goals

- No other Step extraction (Steps 0–5a are already modules; Steps 7–9 / 9b stay inline — that is Slice 7; this is Slice 6 only).
- No prose rewriting, reordering, splitting, or "while-we're-here" edits. Move-and-link only.
- No changes to the review-delegation semantics, the mandatory output format, the STOP gate, the 5-iteration cap, the (a)/(b)/(c) choice, the `--auto` Decision-append / `crafter-buffer` UAT-record / commit chain, the three non-`--auto` approval paths, the manual-verification exception, the green-commit invariant, or the session-break routing — only the reference *paths* / pointer wording are carried into the modules; the binding text itself is not edited (except the mechanical `{CRAFTER_HOME}` substitutions in Phase 2).
- No edits to `rules/task-lifecycle.md`, `rules/do-workflow.md`, `rules/post-change.md`, `rules/do/extension-skills.md`, `skills/crafter-buffer/SKILL.md`, `rules/do/step-0-resume.md`, `rules/do/step-1-scope.md`, `rules/do/step-2-discuss.md`, `rules/do/step-3-plan.md`, `rules/do/step-4-execute.md`, `rules/do/step-5-drift.md`, `rules/do/step-5a-phase-verification.md`, or any agent file (`crafter-reviewer`, `crafter-implementer`, `crafter-buffer`, etc.) — and specifically NO edits to Steps 7–9 / Step 9b in SKILL.md (they only reference Step 6b by name and stay untouched under Interpretation A).
- No changes to `agents/*.md`, CLI/Go code, buffer or task-file schema.
- No edits to any SKILL.md section other than: the Step 6 stub, the Step 6b stub, the Step 6a stub, the loader list, and (cross-reference sweep) any genuinely dangling internal reference — none expected under Interpretation A.
- No repo-wide `~/.claude/...` normalization beyond the enumerated occurrences inside the three new modules (sibling task `20260421-skills-first-runtime-portability.md` owns the broader sweep).
- No edits to installed runtime copies under `~/.claude/...`.
- No reordering or renaming of existing Step numbers or rules files; no switch of the installer to a directory copy.

### 4. Relevant areas

- `skills/crafter-do/SKILL.md` —
  - Extraction target 1: lines 128–163 (`## Step 6 — REVIEW` through "…record any notable decisions in the task file per `~/.claude/crafter/rules/task-lifecycle.md`."). Critical bindings inside: the supplemental-only extension-skill check (lines 130, referencing `~/.claude/crafter/rules/do/extension-skills.md`); `crafter-reviewer` delegation (lines 132–136); the **mandatory output format** (lines 137–142, "reproduce the Reviewer's tables directly … Never convert tables to prose"); the **mandatory review STOP gate** (line 144); the zero-findings → Step 6b auto-approve path (line 146); the Critical/Major fix loop with the **5-iteration cap** and the `--auto` exit-with-state note (line 153) and the **(a)/(b)/(c) user choice** (lines 154–156); `crafter-implementer` fix delegation (line 158); "Re-run Step 5a (PHASE VERIFICATION)" (line 160); "re-run Step 6 (REVIEW) from the top" (line 161); record-decisions-per-`~/.claude/crafter/rules/task-lifecycle.md` (line 163).
  - Extraction target 2: lines 165–225 (`## Step 6b — Phase Summary and Auto-Commit` through "…continue to **Step 6a** … or **Steps 7–9** …"). **HEADLINE R1 cluster.** Bindings inside: the `--auto` branch (lines 171–182 — `Decision (Auto-Fixed)` append line 175, `Decision (Tech Debt — auto-recorded)` append line 176, `crafter-buffer` UAT recording line 177 referencing `skills/crafter-buffer/SKILL.md`, commit per `~/.claude/crafter/rules/post-change.md` line 178, green-commit-invariant reference line 180); the structured Phase Summary (lines 188–195, incl. "Accepted Decisions … from Step 5 drift-check handling" line 193); path (1) auto-approve + the manual-verification exception (lines 199–205, commit per `~/.claude/crafter/rules/post-change.md` line 205); path (2) `--fast` silence-as-approval + tech-debt recording + commit (lines 207–213); path (3) explicit approval, silence-is-not-approval (lines 215–219); the Commit section "On approval (any path), run the commit per `~/.claude/crafter/rules/post-change.md`" (line 223); the "After committing, continue to Step 6a or Steps 7–9" routing (line 225).
  - Extraction target 3: lines 227–237 (`## Step 6a — Session Break (Medium/Large scope only)` through "…clean context window."). Bindings inside: "Skip this step for Small scope" (line 229); the three session-break routing rules (lines 233–235: last step in phase → Step 5a + Step 6; last step in plan → Steps 7–9; otherwise → suggest `/clear` + re-invoke, or back to Step 4 (EXECUTE)); the Step 0 resume hand-off (line 237). No concrete runtime path.
  - Loader list lines 16–26 (`<!-- do/* capability modules -->` group; add three entries after the `step-5a-phase-verification.md` line at line 26).
  - Inbound references to preserve (all resolve by name/number to the retained headings; all OUT OF SCOPE to edit): `rules/do/step-4-execute.md` line 12 ("…run Step 5a (phase verification) and Step 6 (phase review)"); SKILL.md Step 4 stub line 118 ("…and to Step 6 (phase review)"); `--fast` Skill-options block line 45 ("See Step 6b for the approval path"); `--auto` Skill-options block (confirm whether it contains a "See Step 6b" approval-path mention — re-verify in 1.1); Step 6b internal routing line 225 ("continue to Step 6a or Steps 7–9"); Step 6 fix-loop lines 160–161 ("Re-run Step 5a / re-run Step 6"); Step 6a lines 233–235 ("proceed to Step 5a (Phase Verification) and Step 6 (Review)", "go back to Step 4 (EXECUTE)", "Steps 7–9"); Steps 7–9 line 241 ("…has already landed via Step 6b"); Step 9b (depends on Step 6b having committed); confirmed: no "Step 6/6a/6b" reference currently exists in `step-0-resume.md`/`step-1-scope.md`/`step-2-discuss.md`/`step-3-plan.md`/`step-5-drift.md`/`step-5a-phase-verification.md` (re-verify in the 1.1 gate and 1.6 sweep).
- `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, `rules/do/step-6a-session-break.md` — new files (do not exist yet).
- `rules/do/flag-validation.md`, `rules/do/project-resolution.md`, `rules/do/extension-skills.md`, `rules/do/step-0-resume.md`, `rules/do/step-1-scope.md`, `rules/do/step-2-discuss.md`, `rules/do/step-3-plan.md`, `rules/do/step-4-execute.md`, `rules/do/step-5-drift.md`, `rules/do/step-5a-phase-verification.md` — precedent for module structure (single H1, tone, `{CRAFTER_HOME}` usage) and the Slice 3 stub convention (pointer + bindings in prose, no secondary path re-citation).
- `docs/core-capabilities.md` — governing design note: taxonomy rows for Step 6 / Step 6b / Step 6a, naming scheme, loading convention, runtime-path policy, deferred-slice ordering note (Steps 6/6b/6a = this slice, "highest density; extract together since they share flag-state and approval-path logic"), and the Slice 1/2/3/4/5 ledger entries to extend (after the Slice 5 entry at line 124).
- `install.sh` line 330 (`step-5a-phase-verification.md` cp line) — `rules/do/` deployment block; three `cp` lines to add after it.
- `tests/test_install.sh` line 304 (`step-5a-phase-verification.md` entry) — expected `rules/do/` files; three entries to add after it.
- `.crafter/tasks/20260518-refactor-crafter-do-slice-5-steps-5-5a.md` — the immediate precedent: two-phase structure, pre-extraction binding gate, R1–R5 risk framing, Interpretation A vs B, installer/test "one mechanical entry per module" handling, Karpathy-contract style, the `## Decisions` stub-convention ruling, headline-binding stub treatment.

### 5. Vertical phases and steps

Two phases, mirroring Slice 5's exact shape, adjusted for THREE modules. Phase 1 = the extraction (pre-extraction binding gate + three modules + three pointer stubs + loader/installer/test wiring ×3 + cross-reference sweep) — independently committable and behavior-identical. Phase 2 = runtime-path hygiene scoped to the new modules (enumerated `{CRAFTER_HOME}` substitutions) + design-note ledger bookkeeping. Each phase ends in a reviewable state with a `crafter-reviewer` gate.

#### Phase 1 — Extract Steps 6 / 6b / 6a into capability modules (move-and-link)

After Phase 1, `## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, and `## Step 6a — Session Break (Medium/Large scope only)` in `SKILL.md` are thin pointer stubs under their original headings; the full prose lives verbatim in the three new modules; the loader list and installer/test deploy all three; all cross-references resolve. Behavior identical across all four flows.

- [x] **1.1 — Validate Step 6/6b/6a bindings survive pointer stubs (pre-extraction binding gate, analysis-only, GO/NO-GO).** Before moving anything, record in writing (in the step's working notes / drift evidence) a GO/NO-GO verdict that each binding cluster is pointer-preservable AND that no other SKILL.md section quotes Step 6/6b/6a prose verbatim (it must reference them by name/number, not duplicate the text). Clusters: (i) Step 6 supplemental-only extension-skill check + `crafter-reviewer` delegation + the **mandatory output format** (reproduce Diff summary / Issues found / Karpathy scorecard / Contract deviations tables verbatim, never convert to prose); (ii) Step 6's **mandatory review STOP gate** (always wait when findings exist, never auto-proceed) + the zero-findings → Step 6b auto-approve path; (iii) Step 6's Critical/Major fix loop — the **5-iteration cap**, the **(a)/(b)/(c) user choice** (manual override / accept-without-commit / replan-and-abort), the `--auto` exit-with-state note, `crafter-implementer` fix delegation, "Re-run Step 5a", "re-run Step 6 from the top", record-decisions-per-`task-lifecycle.md`; (iv) **HEADLINE R1 — Step 6b's `--auto` branch**: `Decision (Auto-Fixed)` append, `Decision (Tech Debt — auto-recorded)` append, `crafter-buffer` UAT recording, commit automatically per `post-change.md`, green-commit-invariant reference (verify a stub can carry all of these without weakening them); (v) **HEADLINE R1 — Step 6b's three non-`--auto` approval paths**: (1) auto-approve on a fully clean summary, (2) `--fast` silence-as-approval + tech-debt recording, (3) explicit approval where silence is NOT approval — plus the manual-verification exception (overrides auto-approve; not bypassed by `--fast`), the "On approval (any path), commit per `post-change.md`" binding, and the "continue to Step 6a or Steps 7–9" routing; (vi) Step 6a's "Skip for Small scope" + the three session-break routing rules + the Step 0 resume hand-off; (vii) inbound references that depend on the retained headings — `rules/do/step-4-execute.md` "…and Step 6" routing, SKILL.md Step 4 stub "…to Step 6", `--fast` Skill-options "See Step 6b", `--auto` Skill-options (confirm whether it cites Step 6b), Step 6b "continue to Step 6a or Steps 7–9", Step 6 "Re-run Step 5a / re-run Step 6", Step 6a "proceed to Step 5a … Step 6 … go back to Step 4", Steps 7–9 "via Step 6b", Step 9b, and confirm zero Step-6/6a/6b refs in the other extracted sibling modules. Also enumerate every concrete `~/.claude/crafter/...` runtime path in each body (Step 6: `extension-skills.md`, `task-lifecycle.md`; Step 6b: the `post-change.md` occurrences — count them; `skills/crafter-buffer/SKILL.md` is NOT a runtime install path; Step 6a: none) for Phase 2. If any binding cannot survive a pointer stub, stop and surface as a risk before extracting (NO-GO). — GO; all 7 clusters PRESERVED, verbatim-quote check clean, runtime paths enumerated (Step 6 ×2, Step 6b ×4 post-change.md, Step 6a ×0), Interpretation A confirmed.
  - **Karpathy contract:** outcome = a written GO/NO-GO on pointer-preservability of all seven binding clusters, each with an explicit preserved/at-risk verdict citing the inbound/forward reference it depends on, with clusters (iv) and (v) — the Step 6b orchestrator-side `--auto` Decision-append/`crafter-buffer`/commit chain and the three approval paths + manual-verification exception — called out as the HEADLINE binding and clusters (ii)/(iii) — the Step 6 STOP gate and 5-iteration-cap (a)/(b)/(c) — as the secondary headline, plus the full enumerated `{CRAFTER_HOME}` substitution list per module; scope = analysis only, zero file edits; non-goals = moving prose, editing referrers, drafting stub text; simplicity = a structured evidence note, not a redesign; drift = beginning any extraction before the verdict is recorded, or expanding into a referrer rewrite; verification = each of the seven clusters has an explicit verdict, clusters (iv)/(v) and (ii)/(iii) explicitly addressed, the per-module runtime-path enumeration recorded, and a confirmation that no section duplicates Step 6/6b/6a prose verbatim; stop = any binding judged not pointer-preservable (especially the Step 6b commit/Decision-append chain or the Step 6 STOP gate / cap), or any verbatim duplication found → halt, record risk, do not extract (Interpretation B territory).

- [x] **1.2 — Create `rules/do/step-6-review.md` with verbatim Step 6 prose.** Move the full `## Step 6 — REVIEW` body (SKILL.md lines 130–163) into the new module. Single `# Title` H1, no carried-over `## Step 6` H2 (same convention as the Slice 1–5 modules). Prose otherwise byte-identical — the two concrete runtime paths (`~/.claude/crafter/rules/do/extension-skills.md`, `~/.claude/crafter/rules/task-lifecycle.md`) are copied as-is in Phase 1 (Phase 2 normalizes them); the STOP gate, the mandatory output format, the 5-iteration cap, the (a)/(b)/(c) choice, and the `--auto` exit-with-state note are copied exactly.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 6 prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing paths yet, touching SKILL.md yet, creating the Step 6b/6a modules yet; simplicity = pure move + H2→H1; drift = any non-mechanical change to the extension-skill check, reviewer delegation, mandatory output format, STOP gate, 5-iteration cap, (a)/(b)/(c) choice, `--auto` exit note, implementer re-delegation, re-run-Step-5a/Step-6 routing, or record-decisions instruction; verification = a literal diff of the module body against the original SKILL.md Step 6 block shows only the H2→H1 line change; the STOP gate, the four-table reproduction rule, the 5-iteration cap, and the (a)/(b)/(c) choice are present verbatim; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.3 — Create `rules/do/step-6b-phase-summary.md` with verbatim Step 6b prose (HEADLINE R1).** Move the full `## Step 6b — Phase Summary and Auto-Commit` body (SKILL.md lines 167–225) into the new module. Single `# Title` H1, no carried-over `## Step 6b` H2. Prose otherwise byte-identical — every `~/.claude/crafter/rules/post-change.md` occurrence and the `skills/crafter-buffer/SKILL.md` mention are copied as-is in Phase 1 (Phase 2 normalizes only the `post-change.md` runtime paths; `crafter-buffer` stays); both `Decision (Auto-Fixed)` and `Decision (Tech Debt — auto-recorded)` append sentences, the `crafter-buffer` UAT-record instruction, all three approval paths, the manual-verification exception, and the green-commit-invariant reference are copied exactly.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 6b prose with every orchestrator-side binding intact; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing paths yet, touching SKILL.md yet, altering the Step 6/6a modules; simplicity = pure move + H2→H1; drift = any non-mechanical change to the `--auto` Decision-append/`crafter-buffer`/commit chain, the three approval paths, the manual-verification exception, the green-commit-invariant reference, the "commit per post-change.md" binding, or the "continue to Step 6a or Steps 7–9" routing; verification = a literal diff of the module body against the original SKILL.md Step 6b block shows only the H2→H1 line change; both Decision-append sentences, the `crafter-buffer` UAT-record sentence, the three approval-path headings, and the manual-verification exception are present verbatim; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.4 — Create `rules/do/step-6a-session-break.md` with verbatim Step 6a prose.** Move the full `## Step 6a — Session Break (Medium/Large scope only)` body (SKILL.md lines 229–237) into the new module. Single `# Title` H1, no carried-over `## Step 6a` H2. Prose otherwise byte-identical — Step 6a has no `~/.claude/...` runtime path, so no normalization applies in either phase.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 6a prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing anything, touching SKILL.md yet, altering the Step 6/6b modules; simplicity = pure move + H2→H1; drift = any non-mechanical change to the "Skip for Small scope" rule, the three session-break routing rules, or the Step 0 resume hand-off; verification = a literal diff of the module body against the original SKILL.md Step 6a block shows only the H2→H1 line change and no other differences; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.5 — Replace all three SKILL.md sections with binding-preserving pointer stubs.** Keep all three headings exactly (`## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, `## Step 6a — Session Break (Medium/Large scope only)`). Replace each body with a short pointer matching the established Step 0/1/2/3/4/5 convention ("Apply the … procedure in `~/.claude/...`. This procedure …" opening; bindings stated in plain prose; NO secondary path re-citation — per the Slice 3 `## Decisions` ruling): the **Step 6 stub** points to `~/.claude/crafter/rules/do/step-6-review.md` and explicitly states it performs the supplemental-only extension-skill check, delegates code review to `crafter-reviewer`, reproduces the Reviewer's Diff summary / Issues found / Karpathy scorecard / Contract deviations tables verbatim (never converting tables to prose), **STOPs and always waits for the user's response when any findings exist (never auto-proceeds), proceeding directly to Step 6b only on zero findings**, runs the Critical/Major fix loop with the **5-iteration cap** and the **(a)/(b)/(c) choice** (manual override / accept-without-commit / replan-and-abort) — exiting with state under `--auto` instead — re-delegates fixes to `crafter-implementer`, re-runs Step 5a then Step 6 from the top, and records notable decisions per `task-lifecycle.md`; the **Step 6b stub** points to `~/.claude/crafter/rules/do/step-6b-phase-summary.md` and explicitly states that under `--auto` the orchestrator appends `Decision (Auto-Fixed)` and `Decision (Tech Debt — auto-recorded)` entries to the task file's `## Decisions`, records manual-verification requirements as UAT buffer entries via the `crafter-buffer` skill, and commits automatically per `post-change.md` under the green-commit invariant; and that for non-`--auto` runs it chooses the first of three approval paths — (1) auto-approve on a fully clean summary, (2) `--fast` silence-as-approval with tech-debt recording, (3) explicit approval where silence is NOT approval — honoring the manual-verification exception (which overrides auto-approve and is not bypassed by `--fast`), then on approval (any path) commits per `post-change.md` and continues to Step 6a or Steps 7–9; the **Step 6a stub** points to `~/.claude/crafter/rules/do/step-6a-session-break.md` and explicitly states it is skipped for Small scope and otherwise applies the three session-break routing rules (last step in phase → Step 5a + Step 6; last step in plan → Steps 7–9; otherwise → suggest `/clear` + re-invoke or go back to Step 4 (EXECUTE)), with Step 0 resume detection picking up from the next unchecked step. Pointer paths stay `~/.claude/...` (installer-resolved carve-out). Source order preserved: Step 6, then Step 6b, then Step 6a. No other section touched.
  - **Karpathy contract:** outcome = all three sections are thin stubs under their original headings, each preserving all bindings from 1.1, with the Step 6b stub explicitly carrying the `--auto` Decision-append/`crafter-buffer`/commit chain and the three approval paths + manual-verification exception, and the Step 6 stub explicitly carrying the STOP gate and the 5-iteration-cap (a)/(b)/(c) choice; scope = only the Step 6, Step 6b, and Step 6a sections of SKILL.md; non-goals = touching any other section (especially Steps 7–9 / Step 9b / Skill-options), changing any heading text, rewording referrers, merging the three stubs, re-citing secondary paths; simplicity = three short pointers mirroring the Step 0/1/2/3/4/5 stubs; drift = dropping any HEADLINE R1 Step 6b binding (either Decision-append, the `crafter-buffer` UAT-record, the commit-per-`post-change.md`, any approval path, the manual-verification exception) or any Step 6 binding (STOP gate, 5-iteration cap, (a)/(b)/(c) choice, mandatory output format) or any Step 6a routing rule, altering any heading, re-citing secondary paths (Slice 3 convention violation), editing adjacent sections; verification = all three headings unchanged; the Step 6b stub explicitly names both Decision-append bindings + the `crafter-buffer` UAT-record + the commit-per-`post-change.md` + all three approval paths + the manual-verification exception; the Step 6 stub explicitly names the STOP gate + the 5-iteration cap + the (a)/(b)/(c) choice + the mandatory output format; the Step 6a stub names the Small-scope skip + the three routing rules; no other SKILL.md section diffs; stop = if a pointer cannot carry a binding (especially any Step 6b commit/Decision-append binding or the Step 6 STOP gate) without rewording a referrer, stop and surface (Interpretation B territory).

- [x] **1.6 — Wire the loader list, installer, and install test (×3), and run the cross-reference sweep.** Add `~/.claude/crafter/rules/do/step-6-review.md`, `~/.claude/crafter/rules/do/step-6b-phase-summary.md`, and `~/.claude/crafter/rules/do/step-6a-session-break.md` to the SKILL.md loader list under the existing `<!-- do/* capability modules -->` group, after the `step-5a-phase-verification.md` line (order: step-6-review → step-6b-phase-summary → step-6a-session-break, the SKILL.md source order). Add three `cp` lines in `install.sh`'s `rules/do/` block after install.sh:330 (the `step-5a-phase-verification.md` line); add three expected-path entries (`crafter/rules/do/step-6-review.md`, `crafter/rules/do/step-6b-phase-summary.md`, `crafter/rules/do/step-6a-session-break.md`) to `tests/test_install.sh` after line 304 (the `step-5a-phase-verification.md` entry). Run `tests/test_install.sh`. Then read the post-extraction `SKILL.md` end-to-end and explicitly confirm every inbound reference still resolves to the retained `## Step 6` / `## Step 6b` / `## Step 6a` headings: `rules/do/step-4-execute.md` "…and Step 6" routing, the SKILL.md Step 4 stub "…to Step 6", the `--fast` Skill-options "See Step 6b", the `--auto` Skill-options (if it cites Step 6b), Step 6b "continue to Step 6a or Steps 7–9", Step 6 "Re-run Step 5a / re-run Step 6 from the top", Step 6a "proceed to Step 5a … Step 6 … go back to Step 4 (EXECUTE)", Steps 7–9 "via Step 6b", Step 9b's dependence on Step 6b having committed, and confirm zero Step-6/6a/6b refs remain dangling in the other extracted sibling modules; and every forward reference from the now-stubbed sections / new modules (to `crafter-reviewer`, `crafter-implementer`, `crafter-buffer`, Step 5a, Step 6, Step 6a, Steps 7–9, Step 4) still resolves. No content edits beyond 1.5 — under Interpretation A no referrer should need changing.
  - **Karpathy contract:** outcome = all three new modules are loaded by SKILL.md, deployed+tested by the installer, and confirmed to leave zero dangling internal references; scope = loader list (three lines), `install.sh` (three cp lines), `tests/test_install.sh` (three entries), plus read-only verification; non-goals = switching the installer to a directory copy, reordering existing entries, renaming files, "improving" referrers that already resolve, expanding the stubs; simplicity = exactly nine one-line additions (3 + 3 + 3), each mirroring the adjacent `step-5a-phase-verification.md` entry; drift = restructuring the loader list or installer enumeration, normalizing other entries, patching cosmetic referrers; verification = all three loader entries sit in the `do/*` group after `step-5a-phase-verification.md` in source order; `tests/test_install.sh` passes; a fresh end-to-end read produces no broken Step 6/6b/6a inbound or forward references (every enumerated inbound ref explicitly re-checked); stop = if the test framework cannot absorb three added files without broader changes (R4), or a real semantic gap (not cosmetic) surfaces, stop and re-plan rather than silently patch.

- [x] **Phase 1 verification.** `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, and `rules/do/step-6a-session-break.md` each contain the full corresponding Step prose verbatim (only H2→H1 differs); SKILL.md's three headings are retained with binding-preserving pointer stubs matching the Step 0/1/2/3/4/5 convention, the Step 6b stub explicitly carrying the `--auto` Decision-append/`crafter-buffer`/commit chain + the three approval paths + the manual-verification exception, the Step 6 stub explicitly carrying the STOP gate + 5-iteration cap + (a)/(b)/(c) choice; loader list includes all three new modules in the `do/*` group after `step-5a-phase-verification.md` in source order; `install.sh` + `tests/test_install.sh` updated (three entries each) and the test passes; all enumerated inbound and forward Step 6/6b/6a references resolve; no SKILL.md section other than the three stubs and the loader list changed; semantic behavior identical across default / `--fast` / `--auto` / resume. Delegate to `crafter-verifier`. — crafter-verifier 10/10 PASS; R1 Step 6b bindings imperative; test 45/0; all inbound refs resolve.
- [x] **Phase 1 review.** — crafter-reviewer: 1 Suggestion (consistency, fixed per user request); scorecard all PASS; R1 bindings imperative.

#### Phase 2 — Runtime-path hygiene + bookkeeping

After Phase 2 the new modules comply with the `{CRAFTER_HOME}` runtime-path policy (Step 6: two substitutions; Step 6b: the enumerated `post-change.md` substitutions; Step 6a: zero) and the design-note ledger reflects what shipped. Narrow, proof-of-policy scope — no repo-wide sweep.

- [x] **2.1 — Apply the `{CRAFTER_HOME}` policy to the new modules.** In `rules/do/step-6-review.md`, normalize `~/.claude/crafter/rules/do/extension-skills.md` → `{CRAFTER_HOME}/rules/do/extension-skills.md` and `~/.claude/crafter/rules/task-lifecycle.md` → `{CRAFTER_HOME}/rules/task-lifecycle.md`. In `rules/do/step-6b-phase-summary.md`, normalize every `~/.claude/crafter/rules/post-change.md` occurrence → `{CRAFTER_HOME}/rules/post-change.md` (re-confirm the exact count at edit time; expected multiple — the `--auto` branch, path (1), path (2), and the Commit section); leave `skills/crafter-buffer/SKILL.md` unchanged (bare cross-skill mention, NOT a `~/.claude/crafter/...` install path — same treatment as bare `rules/do-workflow.md → ###` anchors and bare agent names in Slices 2–5). In `rules/do/step-6a-session-break.md`, confirm there is NO `~/.claude/...` occurrence (ZERO substitutions expected). If any module's actual occurrence set differs from this enumeration, normalize what is genuinely a hard-coded `~/.claude/crafter/...` runtime path and flag the deviation. Bare `crafter-reviewer`/`crafter-implementer`/`crafter-buffer`/Step-number mentions and `rules/do-workflow.md → ###` anchors are not runtime install paths and stay unchanged. No other files touched; SKILL.md pointer/loader lines keep `~/.claude/...` (installer-resolved carve-out).
  - **Karpathy contract:** outcome = Step 6 and Step 6b runtime paths normalized to `{CRAFTER_HOME}/...` exactly per the enumeration, Step 6a confirmed zero-substitution; scope = only the three new module files; non-goals = repo-wide normalization, editing SKILL.md, editing `rules/post-change.md` / `rules/task-lifecycle.md` / `rules/do-workflow.md` / `skills/crafter-buffer/SKILL.md` / agents, adding installer path logic, normalizing `skills/crafter-buffer/SKILL.md` or bare agent/Step/anchor mentions; simplicity = the exact enumerated substitutions only; drift = normalizing other files, normalizing the `crafter-buffer` cross-skill mention or bare agent/Step mentions, introducing new placeholders, rewording surrounding prose; verification = Step 6 has both enumerated substitutions and no remaining `~/.claude/...`; Step 6b has all `post-change.md` substitutions and no remaining `~/.claude/crafter/...` (with `skills/crafter-buffer/SKILL.md` deliberately unchanged); Step 6a has zero `~/.claude/...`; bare agent/Step/anchor mentions unchanged; stop = if applying the policy would require installer changes or touching files outside the three modules, stop (scope escalation).

- [x] **2.2 — Update the design-note ledger and confirm sibling delineation.** In `docs/core-capabilities.md`, append a Slice 6 entry to the runtime-path "Applied in this task" ledger (after the existing Slice 5 entry at line 124) recording that Slice 6 created `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, and `rules/do/step-6a-session-break.md` and the `{CRAFTER_HOME}` substitutions applied to each (step-6-review.md: two — `extension-skills.md` + `task-lifecycle.md`; step-6b-phase-summary.md: the enumerated `post-change.md` occurrences with `skills/crafter-buffer/SKILL.md` left unchanged as a bare cross-skill mention; step-6a-session-break.md: zero), restating that the bare `crafter-reviewer`/`crafter-implementer`/`crafter-buffer` mentions and `rules/do-workflow.md → ###` anchors are not runtime paths and that repo-wide normalization remains owned by `20260421-skills-first-runtime-portability.md`. Keep it to a few lines, consistent in tone and structure with the Slice 1/2/3/4/5 entries; do not restructure the doc or re-tag taxonomy rows.
  - **Karpathy contract:** outcome = the design note accurately reflects Slice 6's shipped scope (three new modules, enumerated substitutions), consistent with the Slice 1/2/3/4/5 ledger entries; scope = a few lines in `docs/core-capabilities.md` (the runtime-path "Applied in this task" area); non-goals = restructuring the taxonomy, restating the sibling task's plan, claiming repo-wide normalization, re-tagging other taxonomy rows, editing the Slice 1–5 entries beyond appending; simplicity = ≤ ~6 added lines; drift = expanding into a meta runtime-portability narrative or contradicting prior ledger entries; verification = the ledger names Slice 6, all three new modules, the per-module substitution counts, the deliberate `crafter-buffer` non-normalization, and the sibling-task owner, and reads consistently with the Slice 5 entry; stop = if the edit would contradict the existing Slice 1–5 ledger entries, stop and reconcile explicitly.

- [x] **Phase 2 verification.** — crafter-verifier 6/6 PASS. `rules/do/step-6-review.md` and `rules/do/step-6b-phase-summary.md` have the enumerated `{CRAFTER_HOME}` substitutions and zero remaining hard-coded `~/.claude/crafter/...` references; `rules/do/step-6a-session-break.md` has zero `~/.claude/...`; `skills/crafter-buffer/SKILL.md` and bare agent/Step/anchor mentions unchanged; `docs/core-capabilities.md` ledger reflects Slice 6 consistently with Slice 1–5; no files outside this phase's set touched; behavior still byte-identical. Delegate to `crafter-verifier`.
- [x] **Phase 2 review.** — crafter-reviewer no findings; scorecard all PASS. `crafter-reviewer` pass; Karpathy scorecard PASS on Surgical Changes (no scope creep into untouched files) and Goal-Driven (policy applied exactly as enumerated; `crafter-buffer` correctly not normalized).

#### Post-task housekeeping

- [x] **STATE.md and skillbook update** per `rules/post-change.md` — consolidated end-of-task commit.
- [x] **Task file completion** per `rules/task-lifecycle.md` (Status → completed, `## Outcome` filled, steps checked).
- [x] **Follow-up note** — recorded in `## Outcome`.

### 6. Karpathy Contract — overall

- **Outcome:** Steps 6, 6b, and 6a extracted into `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, and `rules/do/step-6a-session-break.md` with byte-identical behavior, binding-preserving pointer stubs under the retained headings (the Step 6b stub explicitly carrying the HEADLINE R1 `--auto` Decision-append/`crafter-buffer`/commit chain + the three approval paths + manual-verification exception; the Step 6 stub explicitly carrying the STOP gate + 5-iteration cap + (a)/(b)/(c) choice), loader/installer/test wiring (×3), and the `{CRAFTER_HOME}` policy applied (Step 6: 2 subs; Step 6b: the enumerated `post-change.md` subs; Step 6a: 0).
- **Scope boundary:** source files only — `rules/do/step-6-review.md` (new), `rules/do/step-6b-phase-summary.md` (new), `rules/do/step-6a-session-break.md` (new), `skills/crafter-do/SKILL.md` (three stubs + three loader lines), `install.sh` (three cp lines), `tests/test_install.sh` (three entries), `docs/core-capabilities.md` (ledger lines). No agent edits, no CLI/Go, no installed-runtime edits, no schema changes, no edits to `rules/task-lifecycle.md` / `rules/do-workflow.md` / `rules/post-change.md` / `rules/do/extension-skills.md` / `skills/crafter-buffer/SKILL.md` / any other `rules/do/*` module / Steps 7–9 / Step 9b.
- **Non-goals:** other Step extractions, prose rewriting, reordering, referrer rewrites, repo-wide path normalization, review/approval/session-break semantic changes, installer style change to directory copy, normalizing the `crafter-buffer` cross-skill mention.
- **Simplicity constraint:** three new modules, three stub replacements, three loader lines, three cp lines, three test entries, the enumerated `{CRAFTER_HOME}` substitutions only, ≤ ~6 ledger lines. Move-and-link discipline only.
- **Drift criteria:** any reword/reorder/split of Step 6/6b/6a prose; dropping any HEADLINE R1 Step 6b binding (either Decision-append, the `crafter-buffer` UAT-record, the commit-per-`post-change.md`, any of the three approval paths, the manual-verification exception, the green-commit-invariant reference) or any Step 6 binding (STOP gate, 5-iteration cap, (a)/(b)/(c) choice, mandatory output format) or any Step 6a routing rule from a stub; re-citing secondary paths in a stub (Slice 3 convention violation); altering any heading; editing any other SKILL.md section or any referrer that already resolves; normalizing the `crafter-buffer` mention or bare agent/Step/anchor mentions; substituting outside the enumerated `~/.claude/crafter/...` paths; switching the installer to a directory copy; editing `task-lifecycle.md` / `do-workflow.md` / `post-change.md` / `crafter-buffer` / other `rules/do/*` modules / Steps 7–9 / 9b / agents / CLI / schema.
- **Verification evidence:** literal diff of each moved block vs. original shows only H2→H1 (Phase 1) and the enumerated substitutions (Phase 2); all four flows read identically; all enumerated inbound and forward Step 6/6b/6a references resolve; the Step 6b stub provably carries the `--auto` Decision-append/`crafter-buffer`/commit chain and all three approval paths + the manual-verification exception; the Step 6 stub provably carries the STOP gate + 5-iteration cap + (a)/(b)/(c) choice; `tests/test_install.sh` passes; reviewer Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.
- **Stop conditions:** stop and re-plan if (a) any Step 6/6b/6a binding — especially the Step 6b `--auto` commit/Decision-append/`crafter-buffer` chain, the three approval paths, the manual-verification exception, or the Step 6 STOP gate / 5-iteration cap — cannot be pointer-preserved or any verbatim duplication is found (1.1 NO-GO), (b) extraction forces editing a section outside the three stubs non-mechanically, (c) the install test cannot absorb three added files without broader change (R4), (d) the `{CRAFTER_HOME}` policy unexpectedly needs installer changes or surfaces an occurrence set that diverges from the enumeration, or (e) the user prefers Interpretation B.

### 7. Alternatives considered

- **Delete the Step 6/6b/6a headings and retarget all referrers (Interpretation B)** — rejected. Much higher drift on the densest section in the file: touches the `step-4-execute.md` module, the SKILL.md Step 4 stub, the `--fast`/`--auto` Skill-options blocks, the 6→6b→6a inter-routing, Steps 7–9's "via Step 6b", and Step 9b (all outside the extraction targets), and risks weakening the HEADLINE R1 Step 6b orchestrator-side commit/Decision-append chain and the Step 6 STOP gate / 5-iteration cap. Keeping all three headings as thin stubs preserves every existing "Step 6"/"6b"/"6a" reference with zero referrer edits — strictly lower drift, exactly the conservative posture Slices 1–5 used.
- **Extract Steps 6, 6b, 6a in separate slices** — rejected. The design note explicitly groups them as one "highest density … extract together since they share flag-state and approval-path logic" slice; the STOP gate (Step 6) feeds the auto-approve path (Step 6b) which routes into Step 6a, and all three branch on `--auto`/`--fast`. A combined slice keeps that coupling reviewable in one diff with no added drift.
- **Skip the `{CRAFTER_HOME}` substitution on Step 6/6b (treat as zero like Slice 5)** — rejected. Unlike Slice 5's modules, Step 6 and Step 6b genuinely carry concrete `~/.claude/crafter/...` runtime paths (`extension-skills.md`, `task-lifecycle.md`, multiple `post-change.md`); the policy requires normalizing them. The zero-substitution posture applies only to Step 6a, which is genuinely runtime-path-free.
- **Normalize `skills/crafter-buffer/SKILL.md` to `{CRAFTER_HOME}/...`** — rejected. It is a bare cross-skill mention, not a `~/.claude/crafter/...` install path; the established convention (Slices 2–5) leaves bare `rules/do-workflow.md → ###` anchors and bare agent names unchanged. Inventing a placeholder there would be non-mechanical drift and contradict the policy's "do not over-edit" posture.
- **Switch `install.sh` to a `cp -R rules/do/` directory copy** — rejected for this slice. The installer's explicit per-file enumeration is the established pattern (Slices 1–5 kept it); three added `cp` lines are lower-drift than changing installer style, and `tests/test_install.sh` enumerates files explicitly too, so a directory copy would not reduce churn.
- **Skip the pre-extraction binding gate (1.1)** — rejected. This is the highest-density slice with the strongest orchestrator-side binding cluster in the file (Step 6b's commit/Decision-append/`crafter-buffer` chain plus Step 6's STOP gate and 5-iteration cap); validating they survive a pointer before extracting is the precise risk-control Slices 1–5 used and is most warranted here.

### 8. Risks / unknowns / flags

- **R1. Step 6b orchestrator-side binding cluster + Step 6 STOP/cap (HEADLINE risk).** Step 6b carries the densest orchestrator-side binding set in the file: the `--auto` `Decision (Auto-Fixed)` / `Decision (Tech Debt — auto-recorded)` task-file appends, the `crafter-buffer` UAT-record instruction, the commit-automatically-per-`post-change.md` instruction, the green-commit-invariant reference, and the three non-`--auto` approval paths plus the manual-verification exception. Step 6 carries the mandatory STOP gate (never auto-proceed when findings exist), the mandatory table-reproduction output format, the 5-iteration cap, and the (a)/(b)/(c) escalation. If a pointer stub weakens or loses any of these, unattended-commit safety, accepted-decision accounting, the green-commit trail, and the human review gate all break. Strongest mitigation: 1.1 explicitly gates on clusters (ii)/(iii)/(iv)/(v); 1.3 verification requires every Step 6b binding sentence present verbatim in the module and 1.2 the same for Step 6; 1.5 requires the stubs to explicitly restate them imperatively; Phase 1 review must explicitly confirm each. Flag for user: if a reviewer judges a pointer cannot safely carry these bindings, this procedure may need to stay partly inline (escalate to Interpretation B or a hybrid).
- **R2. Installer change is required ×3 (confirmed, not "likely none").** `install.sh` and `tests/test_install.sh` both enumerate `rules/do/*` files explicitly (the `step-5a-phase-verification.md` lines are install.sh:330 / tests/test_install.sh:304), so three cp lines + three test entries are mandatory. Low risk, fully mechanical, but real edits — not zero.
- **R3. Inbound Step 6/6b/6a references span a sibling module, the Skill-options blocks, and the Step-7–9/9b family.** `rules/do/step-4-execute.md` (a sibling module), the SKILL.md Step 4 stub, the `--fast` Skill-options "See Step 6b" (line 45) and the `--auto` Skill-options, the 6→6b→6a inter-routing, Steps 7–9's "via Step 6b", and Step 9b's dependence on Step 6b having committed all rely on the retained headings/Step numbers. Mitigation: Interpretation A retains all three headings; the 1.6 sweep explicitly re-checks every one. If the user picks B, every one of these becomes a required edit (notably across the `step-4-execute.md` module boundary and the user-facing Skill-options blocks).
- **R4. Install-test fragility.** `tests/test_install.sh` uses an explicit expected-file list; adding three entries is straightforward, but if the test has stricter ordering/exact-count assertions the entries may need careful placement. Mitigation: run the test in 1.6 and adjust placement only (no logic change); escalate if logic changes are needed.
- **R5. Move-and-link reflex drift on the densest section in the file.** "Pure move" is easy to violate with a one-word tidy in the reviewer-delegation, the STOP-gate wording, the approval-path conditions, or the Decision-append formats; by accidentally normalizing the `crafter-buffer` cross-skill mention or a bare agent/Step mention; by substituting outside the enumerated `~/.claude/crafter/...` paths; or by re-citing a secondary path in a stub (violating the Slice 3 convention). This is the largest and most binding-dense slice, so the reflex risk is highest here. Mitigation: reviewer must literally diff each moved block vs. original and confirm only H2→H1 changed (Phase 1) and only the enumerated substitutions (Phase 2), and that stubs cite no secondary paths and the `crafter-buffer` mention is unchanged.

---

This contract protects the byte-identical behavior of `crafter-do`'s review, phase-summary/auto-commit, and session-break steps — most critically Step 6b's HEADLINE orchestrator-side `--auto` Decision-append / `crafter-buffer` UAT-record / commit-per-`post-change.md` chain and three non-`--auto` approval paths, plus Step 6's mandatory STOP gate, table-reproduction output format, 5-iteration cap, and (a)/(b)/(c) escalation — while extracting the highest-density trio into named modules exactly as Slices 1–5 extracted the preamble, resume detection, scope/discuss, plan/execute, and drift/verification. The approach is right because it reuses the proven, lowest-drift move-and-link pattern, gates the extraction on an explicit binding-preservation check that singles out the strongest orchestrator-side binding cluster in the file, keeps all three headings as stubs so no referrer (including the sibling Step 4 module, the user-facing Skill-options blocks, and the Step-7–9/9b family) changes, extracts the natural flag-state/approval-path-coupled trio together as the design note recommends, and handles the confirmed ×3 installer/test wiring mechanically while applying the `{CRAFTER_HOME}` policy exactly per the per-module enumeration.

## Decisions
- **Decision (User Accepted):** Phase 1 review Suggestion #1 — user requested the consistency fix. Removed the secondary runtime-path re-citations (`~/.claude/crafter/rules/task-lifecycle.md` from the Step 6 stub; `~/.claude/crafter/rules/post-change.md` ×4 from the Step 6b stub) so the Slice 6 stubs match the established Slice 3/4/5 no-secondary-path-re-citation convention. All bindings retained in prose (records notable decisions / commits / Decision-appends / approval paths / manual-verification exception); the full paths still live in the `rules/do/step-6-review.md` and `rules/do/step-6b-phase-summary.md` modules. Bare `rules/do-workflow.md` anchor and `crafter-buffer` mention preserved. Local, behavior-preserving consistency fix.

## Outcome

**Status:** Complete. Both phases delivered; behavior byte-identical. Highest-density slice of the decomposition.

- **Phase 1** (commit `e7904ff`): `## Step 6 — REVIEW`, `## Step 6b — Phase Summary and Auto-Commit`, `## Step 6a — Session Break …` extracted verbatim into new `rules/do/step-6-review.md`, `rules/do/step-6b-phase-summary.md`, `rules/do/step-6a-session-break.md` (H2→H1 only); all three SKILL.md sections reduced to binding-preserving pointer stubs under retained headings, source order 6→6b→6a. Step 6 stub preserves the mandatory STOP gate + 5-iteration cap + (a)/(b)/(c) choice + mandatory output format; Step 6b stub preserves the full orchestrator-side cluster (both Decision-append types, crafter-buffer UAT-record, commit, green-commit invariant, three approval paths, manual-verification exception) — the headline R1. Review Suggestion #1 (stub path-style consistency) fixed per user request: stubs aligned to the Slice 3/4/5 no-secondary-path-re-citation convention. +3 loader entries; three mechanical install.sh cp lines + three tests/test_install.sh entries. Pre-extraction binding gate GO (7/7 clusters); verifier 10/10 PASS; test 45/0.
- **Phase 2** (this commit): `{CRAFTER_HOME}` policy applied — step-6-review.md ×2 (`extension-skills.md` + `task-lifecycle.md`), step-6b-phase-summary.md ×4 (`post-change.md`), step-6a-session-break.md ×0. `crafter-buffer` mention + `do-workflow.md` anchors correctly preserved. `docs/core-capabilities.md` ledger extended with a Slice 6 entry consistent with Slice 1–5. Verifier 6/6 PASS; reviewer no findings.

**Deviations from plan:** None affecting scope. Review Suggestion #1 (stub path-style consistency) fixed at user request; recorded in `## Decisions`.

**Follow-up:** Slice 7 — extract Steps 7–9 / 9b (Post-Change + PR Composition) per the deferred-slice order in `docs/core-capabilities.md` — is the FINAL slice and COMPLETES the maintenance decomposition. After that, the real end goal follows: progressive on-demand workflow loading (orchestrator master-plan + lazy per-step loading) — recorded in STATE.md `## Planned`.
