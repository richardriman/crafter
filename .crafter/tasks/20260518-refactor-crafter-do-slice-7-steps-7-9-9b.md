# Task: Refactor crafter-do Slice 7 (FINAL) — extract Steps 7–9 / 9b (Post-Change + PR Composition) into core capability modules

## Metadata
- **Date:** 2026-05-18
- **Work branch:** refactor/crafter-do-slice-7-steps-7-9-9b
- **Status:** completed
- **Scope:** Medium

## Request
Slice 7 — the FINAL slice — of the crafter-do core-capability decomposition. Extract the `## Steps 7–9 — Post-Change` and `` ## Step 9b — PR Composition (`--auto` only) `` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules under `rules/do/` — `rules/do/step-7-9-post-change.md` and `rules/do/step-9b-pr-composition.md` — per the taxonomy in `docs/core-capabilities.md`, following exactly the move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slices 1–6. After this slice, every workflow step is a `rules/do/*` module and `skills/crafter-do/SKILL.md` is a thin manifest of pointer stubs — the maintenance decomposition is complete.

Constraints:
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume).
- Pre-extraction binding gate (same as Slice 2–6 step 1.1, GO/NO-GO, headline R1): validate that Steps 7–9's MANDATORY post-change 5-item checklist (check docs / consolidated end-of-task commit / update STATE.md / complete the task file / suggest session wrap-up), the **"Do not end the conversation until all 5 items above are addressed" hard gate**, the orchestrator-side task-file/STATE.md/commit bindings, and the `post-change.md` reference; and Step 9b's `--auto`-only PR-composition procedure (the `crafter pr-body` subcommand invocation, `gh pr create`, the title-derivation rule, failure handling via the Ad-hoc escape hatch, success handling + cleanup hook, the `--run-dir`/`--task-file` inputs) — all survive the pointer stubs. Confirm no other section quotes Steps 7–9/9b prose verbatim.
- HEADLINE R1: the Steps 7–9 mandatory 5-item checklist + "do not end until all 5 addressed" hard gate, and Step 9b's orchestrator-side commit/PR bindings (the only place a `git push` is sanctioned, via `gh pr create`). The Steps 7–9 stub MUST preserve the mandatory-checklist + hard-gate explicitly and imperatively; the Step 9b stub MUST preserve the `--auto`-only trigger, the subcommand+`gh pr create` chain, failure/success handling, and the no-standalone-`git push` rule.
- Inbound references to include in the gate + the cross-reference sweep: `rules/do/step-6b-phase-summary.md` "continue to … Steps 7–9"; `rules/do/step-6a-session-break.md` "proceed … to Steps 7–9"; the `--auto` Skill-options / `rules/do-workflow.md` pointers to Step 9b; Steps 7–9 "via Step 6b"; Step 9b "after Steps 7–9 complete"; any "Steps 7–9"/"7–9"/"Step 9b" mention in `step-0-resume.md` or other extracted modules.
- Pure move-and-link only: no prose rewriting, no reordering. Retain the EXACT headings `## Steps 7–9 — Post-Change` (en-dash `–`) and `` ## Step 9b — PR Composition (`--auto` only) `` as binding-preserving pointer stubs matching the established Step 0/1/2/3/4/5/6 stub convention ("Apply the … procedure in …. This procedure …" opening; Interpretation A; lowest drift; no secondary path re-citation per the unified Slice 6 convention). No other sections touched.
- Installer + `tests/test_install.sh` need one mechanical entry per new module (per Slice 2 R2). Two new modules in this slice.
- Out of scope: any other Step extraction (none remain), extension-skill semantic changes, agent edits, CLI/Go changes, repo-wide path normalization.

## Plan

**Plan status:** approved

### 1. Complete request

Extract the `## Steps 7–9 — Post-Change` and `` ## Step 9b — PR Composition (`--auto` only) `` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules — `rules/do/step-7-9-post-change.md` and `rules/do/step-9b-pr-composition.md` — applying the exact two-phase move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slices 1–6 (most recently the two-module Slice 5 and the three-module Slice 6). This is Slice 7, the FINAL slice of the deferred follow-up sequence. After it lands, every workflow step (Steps 0 through 9b) is a `rules/do/*` module and `skills/crafter-do/SKILL.md` is a thin manifest — frontmatter + Skill options + orchestrator identity + the loader list + pointer stubs only, with no remaining inlined Step prose. This completes the maintenance decomposition.

Both section bodies move verbatim into their respective modules. Each `## …` heading in `SKILL.md` is reduced to a thin binding-preserving pointer stub that (a) keeps the EXACT heading text — `## Steps 7–9 — Post-Change` (note the en-dash `–` in "7–9", not a hyphen) and `` ## Step 9b — PR Composition (`--auto` only) `` (note the backticked `--auto` inside the heading) — so every existing "Steps 7–9"/"7–9"/"Step 9b" reference elsewhere still resolves, and (b) preserves the binding contracts the prose carried.

The headline binding cluster (R1) has two halves:
- **Steps 7–9:** the **MANDATORY CHECKLIST** of 5 items (1 check docs incl. delegate ARCHITECTURE.md check to Implementer; 2 consolidated end-of-task commit per `post-change.md`; 3 update STATE.md; 4 complete the task file in `{PROJECT_PATH}/{CRAFTER_DIR}/tasks/`; 5 suggest session wrap-up with `/crafter-do`/`/crafter-debug`), PLUS the **"Do not end the conversation until all 5 items above are addressed" hard gate**, PLUS the "Follow the post-change steps in `post-change.md`" delegation and the lead-in ("The per-phase commit for the final phase has already landed via Step 6b").
- **Step 9b:** the `--auto`-only trigger ("ONLY after Steps 7–9 complete"), the `rules/do-workflow.md → ### --auto` anchor, the Inputs (`--run-dir` `.crafter/run/<task-id>/`, `--task-file`, baseline body, the "If `## Outcome` is empty … exit via the Ad-hoc escape hatch" rule), the 5 Action steps (compose baseline body; `crafter pr-body --run-dir … --task-file …`; concatenate; derive title from `git log -1 --format='%s'`; open PR via `printf … | gh pr create --title "$TITLE" --body-file -`), the title-derivation rule, the **"the only push in the `--auto` flow is the one embedded in `gh pr create`; `rules/post-change.md` forbids a standalone `git push`" binding**, the failure-handling branch (append `Decision (Auto-Recorded)`, do NOT run cleanup hook, exit via the Ad-hoc escape hatch), the success-handling branch (print PR URL, run cleanup hook `rm -rf .crafter/run/<task-id>/`, proceed to session wrap-up), and the Reviewer-note about GFM task-list checkboxes.

Why this matters: `crafter-do/SKILL.md` is the entry point every Crafter workflow loads. Steps 7–9 own the mandatory end-of-task housekeeping gate that guarantees docs/STATE.md/task-file completion before a run ends; Step 9b owns the entire unattended PR-composition procedure including the sole sanctioned `git push`. Pulling them into named modules completes the decomposition and lets these semantics evolve in focused files, exactly as Slices 1–6 did. The two are a natural pair (Step 9b runs only "after Steps 7–9 complete" and reuses the task-file `## Outcome` they fill), so extracting them together keeps that coupling reviewable in one slice — and the design note explicitly groups them as the final deferred pair ("already heavily delegated to `rules/post-change.md`; extraction is thin pointer work").

**Acceptance criteria:**

- Two new modules exist:
  - `rules/do/step-7-9-post-change.md` containing the full `## Steps 7–9 — Post-Change` prose verbatim, single `# Title` H1 with the exact heading text `# Steps 7–9 — Post-Change` (en-dash preserved, `#` not `##`), no carried-over H2.
  - `rules/do/step-9b-pr-composition.md` containing the full `` ## Step 9b — PR Composition (`--auto` only) `` prose verbatim, single `# Title` H1 with the exact heading text `` # Step 9b — PR Composition (`--auto` only) `` (backticked `--auto` preserved, `#` not `##`), no carried-over H2.
- `skills/crafter-do/SKILL.md` keeps BOTH headings exactly (including the en-dash and backticked `--auto`) followed by short pointer stubs matching the established Step 0–6 stub convention (the "Apply the … procedure in …. This procedure …" opening; module pointer via `~/.claude/...`; bindings stated in plain prose; NO secondary path re-citation per the unified Slice 6 convention). The Steps 7–9 stub MUST explicitly and imperatively preserve the lead-in, the `post-change.md` delegation, the full 5-item mandatory checklist, AND the "do not end the conversation until all 5 items above are addressed" hard gate. The Step 9b stub MUST explicitly and imperatively preserve the `--auto`-only trigger, the `crafter pr-body` + `gh pr create` chain, the title-derivation rule, the failure-handling (Ad-hoc escape hatch) and success-handling (cleanup hook) branches, and the "only push is the one embedded in `gh pr create`; no standalone `git push`" binding.
- The loader list in `SKILL.md` includes `~/.claude/crafter/rules/do/step-7-9-post-change.md` then `~/.claude/crafter/rules/do/step-9b-pr-composition.md` under the existing `<!-- do/* capability modules -->` group, after the `step-6a-session-break.md` line (SKILL.md source order; loader/pointer paths intentionally stay `~/.claude/...`, same carve-out as Slices 1–6).
- `install.sh` deploys both new modules (one `cp` line each after the `step-6a-session-break.md` line at install.sh:333) and `tests/test_install.sh` expects both (one entry each after the `step-6a-session-break.md` entry at tests/test_install.sh:307); `tests/test_install.sh` passes.
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume). Every inbound reference still resolves to the retained headings: `rules/do/step-6b-phase-summary.md` "continues to Step 6a … or Steps 7–9"; `rules/do/step-6a-session-break.md` "proceed directly to Steps 7–9" (×2, items 1–2–3) and "Step 6 (Review)"; the `--auto` Skill-options / `rules/do-workflow.md` end-to-end promise; Step 9b's own "after Steps 7–9 complete" and "Step 7–9 item 5" cross-ref; the `rules/do-workflow.md → ### --auto` and `#### Ad-hoc escape hatch` anchors; and any "Steps 7–9"/"7–9"/"Step 9b"/"Steps 7-9" mention in `step-0-resume.md` / `step-1-scope.md` / `step-2-discuss.md` / `step-3-plan.md` / `step-4-execute.md` / `step-5-drift.md` / `step-5a-phase-verification.md` / `step-6-review.md` / `step-6b-phase-summary.md` / `step-6a-session-break.md`.
- The full post-extraction `SKILL.md` is a thin manifest: frontmatter + Skill options + orchestrator identity + the loader list + the Step stubs only — no remaining inlined Step prose anywhere.
- Two independently committable phases, each with a per-phase verification gate and a `crafter-reviewer` gate; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.

**Validation strategy:** Editorial + installer test. Re-reading the post-extraction `SKILL.md` for the four flows produces identical semantics; a literal diff of each moved block against the original shows only the mechanical H2→H1 normalization (Phase 1) and exactly the enumerated `{CRAFTER_HOME}` substitutions (Phase 2 — two in step-7-9-post-change.md, zero in step-9b-pr-composition.md); `tests/test_install.sh` passes after the installer + test edits; a final end-to-end read confirms SKILL.md is a thin manifest with zero remaining inlined Step prose.

### 2. Assumptions / interpretations

- **Module paths are fixed by the design note:** `rules/do/step-7-9-post-change.md` and `rules/do/step-9b-pr-composition.md` (taxonomy table rows lines 46–47). The `rules/do/` subdirectory was confirmed in Slice 1 and reused in Slices 2–6; this slice follows it without re-deciding.
- **Module structure mirrors the Slice 1–6 modules:** single `# Title` H1, exact heading text with `#` instead of `##`, no carried-over H2, prose otherwise verbatim. The exact-text preservation is mandatory here (en-dash `–` in "7–9", backticked `--auto`), not Implementer's choice.
- **`{CRAFTER_HOME}` enumeration (re-confirm at extraction time):** From SKILL.md lines 143–157, the Steps 7–9 body contains exactly TWO concrete `~/.claude/crafter/...` runtime paths, both `~/.claude/crafter/rules/post-change.md` — one in the "Follow the post-change steps in …" lead-in (line 147) and one in checklist item 2 (line 152). The Step 9b body (lines 159–234) contains ZERO `~/.claude/crafter/...` install paths: it references `.crafter/run/<task-id>/` (project-relative runtime dir, NOT a `{CRAFTER_HOME}` install path), `{PROJECT_PATH}/{CRAFTER_DIR}/tasks/<task-id>.md` (project-relative placeholder), `crafter pr-body` / `gh pr create` / `git log` (commands, not paths), and `rules/do-workflow.md → ###`/`####` anchors and bare `rules/post-change.md` mentions (NOT `~/.claude/crafter/...` install paths). So Phase 2 applies exactly TWO substitutions to step-7-9-post-change.md and ZERO to step-9b-pr-composition.md. The Implementer must re-scan each extracted block at extraction time to confirm these exact counts; if any differs, normalize accordingly and flag the deviation. SKILL.md loader-list and pointer-stub lines stay `~/.claude/...` (installer-resolved carve-out) — identical to Slices 1–6.
- **Installer change IS required (×2), per Slice 2 R2 (confirmed, not "likely none").** `install.sh` enumerates each `rules/do/*.md` explicitly (the `step-6a-session-break.md` line is install.sh:333) and `tests/test_install.sh` enumerates each expected file (the `step-6a-session-break.md` entry is tests/test_install.sh:307). Two new modules therefore need exactly two mechanical `cp` lines and two expected-path entries. `mkdir -p "$rules_dest/do"` already exists — no directory-creation change.
- **Lower-drift cross-reference strategy (chosen — Interpretation A):** keep both headings as thin pointer stubs rather than deleting headings and rewriting referrers. Preserves all existing "Steps 7–9"/"Step 9b" references with zero edits to other sections or sibling modules — strictly lower drift, exactly the conservative posture Slices 1–6 used. Justified in §7.

**Competing interpretations:**

- *Interpretation A (chosen):* thin pointer stubs under the retained `## Steps 7–9 — Post-Change` and `` ## Step 9b — PR Composition (`--auto` only) `` headings; referrers untouched. Same posture as Slices 1–6.
- *Interpretation B (rejected, as in Slices 2–6):* delete the headings, move all content out, and update every referrer (`step-6b-phase-summary.md`, `step-6a-session-break.md`, the Skill-options/`do-workflow.md` pointers, Step 9b's own back-references). Rejected — higher drift, touches sibling modules and user-facing blocks outside the extraction targets, and risks weakening the mandatory-checklist hard gate and the no-standalone-`git push` binding. If the user prefers B, Phase 1 changes shape and the cross-reference sweep expands.

### 3. Non-goals

- No other Step extraction (none remain — this is the final slice; Steps 0–6a are already modules).
- No prose rewriting, reordering, splitting, or "while-we're-here" edits. Move-and-link only.
- No changes to the mandatory 5-item checklist, the hard-gate wording, the `post-change.md` delegation, the `--auto`-only trigger, the `crafter pr-body`/`gh pr create` chain, the title-derivation rule, the failure/success handling branches, or the no-standalone-`git push` binding — only the reference *paths* / pointer wording are carried into the modules; the binding text itself is not edited.
- No edits to `rules/post-change.md`, `rules/do-workflow.md`, `rules/task-lifecycle.md`, `rules/do/extension-skills.md`, or any other `rules/do/*` module (specifically NO edits to `step-6b-phase-summary.md` / `step-6a-session-break.md`, which only reference Steps 7–9 by name and stay untouched under Interpretation A), or any agent file.
- No changes to `agents/*.md`, CLI/Go code (including `crafter pr-body`), buffer or task-file schema, the `--auto`/`--fast` Skill-options blocks.
- No edits to any SKILL.md section other than: the Steps 7–9 stub, the Step 9b stub, and the loader list (plus the cross-reference sweep, read-only — no dangling reference expected under Interpretation A).
- No repo-wide `~/.claude/...` normalization (sibling task `20260421-skills-first-runtime-portability.md` owns the broader sweep). No edits to installed runtime copies under `~/.claude/...`.
- No reordering or renaming of existing Step numbers or rules files; no switch of the installer to a directory copy.
- This task does NOT begin the progressive on-demand workflow loading effort (recorded in STATE.md `## Planned`) — it only completes the extraction prerequisite.

### 4. Relevant areas

- `skills/crafter-do/SKILL.md` —
  - Extraction target 1: lines 143–157 (`## Steps 7–9 — Post-Change` through "**Do not end the conversation until all 5 items above are addressed.**"). Critical bindings: lead-in (line 145), `post-change.md` delegation (line 147, runtime path #1), the 5-item mandatory checklist (lines 149–155, `post-change.md` runtime path #2 in item 2 line 152), the hard gate (line 157, headline R1).
  - Extraction target 2: lines 159–234 (`` ## Step 9b — PR Composition (`--auto` only) `` through the Reviewer-note). Bindings: `--auto`-only trigger (line 161), `do-workflow.md → ### --auto` anchor (line 163), Inputs incl. Ad-hoc-escape-hatch on empty `## Outcome` (lines 165–169), the 5 Action steps incl. fenced `crafter pr-body` / `printf … | gh pr create` blocks (lines 171–212), the no-standalone-`git push` binding (line 212, headline R1), title-derivation rule (line 214), failure handling (lines 216–223), success handling incl. cleanup hook (lines 225–232), Reviewer note (line 234). No `~/.claude/crafter/...` install path anywhere in this block.
  - Loader list lines 16–29 (`<!-- do/* capability modules -->` group; add two entries after the `step-6a-session-break.md` line at line 29).
  - Inbound references to preserve (all resolve by name/number to the retained headings; all OUT OF SCOPE to edit): `rules/do/step-6b-phase-summary.md` "continues to Step 6a … or Steps 7–9"; `rules/do/step-6a-session-break.md` "proceed directly to Steps 7–9" (×2) and "Step 6 (Review)"; SKILL.md lines 137/141 (the Step 6b / Step 6a stubs' "Steps 7–9" mentions); the `--auto`/`--fast` Skill-options blocks and the `rules/do-workflow.md` end-to-end promise; Step 9b's own "after Steps 7–9 complete" (line 161) and "Step 7–9 item 5" (line 232); the `rules/do-workflow.md → ### --auto` (line 163) and `#### Ad-hoc escape hatch` (line 223) anchors; any "Steps 7–9"/"7–9"/"Step 9b" in `step-0-resume.md` and the other extracted `rules/do/*` modules. Confirm all in the 1.1 gate and the wiring/sweep step.
- `rules/do/step-7-9-post-change.md`, `rules/do/step-9b-pr-composition.md` — new files (do not exist yet).
- `rules/do/{flag-validation,project-resolution,extension-skills,step-0-resume,step-1-scope,step-2-discuss,step-3-plan,step-4-execute,step-5-drift,step-5a-phase-verification,step-6-review,step-6b-phase-summary,step-6a-session-break}.md` — precedent for module structure (single H1, tone, `{CRAFTER_HOME}` usage) and the unified Slice 6 no-secondary-path-re-citation stub convention.
- `docs/core-capabilities.md` — governing design note: taxonomy rows 46–47 (Steps 7–9, Step 9b), naming scheme, loading convention, runtime-path policy, deferred-slice ordering note (line 82, "thin pointer work"), and the Slice 1–6 ledger entries to extend (after the Slice 6 entry at line 126).
- `install.sh` line 333 (`step-6a-session-break.md` cp line) — `rules/do/` deployment block; two `cp` lines to add after it.
- `tests/test_install.sh` line 307 (`step-6a-session-break.md` entry) — expected `rules/do/` files; two entries to add after it.
- `.crafter/tasks/20260518-refactor-crafter-do-slice-5-steps-5-5a.md` (two-module precedent) and `.crafter/tasks/20260518-refactor-crafter-do-slice-6-steps-6-6b-6a.md` (most recent; `## Decisions` Suggestion #1 established the no-secondary-path-re-citation stub convention).

### 5. Vertical phases and steps

Two phases, mirroring Slice 5's exact two-module shape. Phase 1 = the extraction (pre-extraction binding gate + two module-creation steps + a stub-replacement step + loader/installer/test wiring ×2 + cross-reference & thin-manifest sweep) — independently committable and behavior-identical. Phase 2 = runtime-path hygiene scoped to the new modules + design-note ledger bookkeeping. Each phase ends in a reviewable state with a `crafter-reviewer` gate.

#### Phase 1 — Extract Steps 7–9 / 9b into capability modules (move-and-link)

After Phase 1, `## Steps 7–9 — Post-Change` and `` ## Step 9b — PR Composition (`--auto` only) `` in `SKILL.md` are thin pointer stubs under their original headings; the full prose lives verbatim in the two new modules; the loader list and installer/test deploy both; all cross-references resolve; SKILL.md is a thin manifest. Behavior identical across all four flows.

- [x] **1.1 — Pre-extraction binding gate (analysis-only, GO/NO-GO).** Before moving anything, record a written GO/NO-GO verdict (in the step's drift evidence) that each binding cluster is pointer-preservable AND that no other SKILL.md section quotes Steps 7–9/9b prose verbatim (referrers must point by name/number, not duplicate text): (i) Steps 7–9 lead-in + `post-change.md` delegation; (ii) **Steps 7–9 mandatory 5-item checklist + the "do not end the conversation until all 5 items above are addressed" hard gate** (headline R1; verify a stub can carry both imperatively without weakening); (iii) Step 9b `--auto`-only trigger + `do-workflow.md → ### --auto` anchor + Inputs incl. the empty-`## Outcome` Ad-hoc-escape-hatch rule; (iv) Step 9b's 5 Action steps (compose body / `crafter pr-body` / concatenate / derive title / `gh pr create`) + title-derivation rule; (v) **Step 9b no-standalone-`git push` binding (the only push is the one embedded in `gh pr create`)** (headline R1); (vi) Step 9b failure handling (`Decision (Auto-Recorded)` append, do NOT run cleanup hook, exit via Ad-hoc escape hatch) + success handling (print PR URL, cleanup hook `rm -rf .crafter/run/<task-id>/`, session wrap-up) + Reviewer note; (vii) inbound refs that depend on the retained headings — `step-6b-phase-summary.md`, `step-6a-session-break.md` (×2 + "Step 6 (Review)"), the SKILL.md Step 6b/6a stubs, the Skill-options/`do-workflow.md` promise, Step 9b's own back-references, the `### --auto`/`#### Ad-hoc escape hatch` anchors, and any mention in `step-0-resume.md`/other modules. Also confirm the exact `{CRAFTER_HOME}` count: Steps 7–9 has exactly two `~/.claude/crafter/rules/post-change.md` (lead-in + checklist item 2); Step 9b has zero `~/.claude/crafter/...` install paths. If any binding cannot survive a pointer stub, stop and surface as a risk before extracting (NO-GO).. — GO; all 7 clusters PRESERVED, verbatim-quote check clean, runtime paths 2/0 (Steps 7-9 ×2 post-change.md, Step 9b ×0), Interpretation A confirmed.
  - **Karpathy contract:** outcome = a written GO/NO-GO with an explicit preserved/at-risk verdict for clusters (i)–(vii), clusters (ii) and (v) called out as the headline bindings, plus the confirmed runtime-path counts (2 / 0); scope = analysis only, zero file edits; non-goals = moving prose, editing referrers, drafting stub text; simplicity = a short evidence note, not a redesign; drift = beginning any extraction before the verdict is recorded, or expanding into a referrer rewrite; verification = each cluster has an explicit verdict, (ii)/(v) explicitly addressed, the 2/0 path counts recorded, and confirmation that no section duplicates Steps 7–9/9b prose verbatim; stop = any binding judged not pointer-preservable (especially (ii) or (v)), or any verbatim duplication found → halt, record risk, do not extract (Interpretation B territory).

- [x] **1.2 — Create `rules/do/step-7-9-post-change.md` with verbatim Steps 7–9 prose.** Move the full `## Steps 7–9 — Post-Change` body (SKILL.md lines 145–157) into the new module. Single `# Title` H1 with EXACT text `# Steps 7–9 — Post-Change` (en-dash `–` preserved, `#` not `##`), no carried-over `## Steps 7–9` H2. Prose otherwise byte-identical — the two `~/.claude/crafter/rules/post-change.md` occurrences are copied as-is here (normalization is Phase 2).
  - **Karpathy contract:** outcome = one new module containing the full original Steps 7–9 prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing the `post-change.md` paths (Phase 2 owns that), touching SKILL.md yet, creating the Step 9b module yet; simplicity = pure move + H2→H1; drift = any non-mechanical change to the lead-in, the `post-change.md` delegation, the 5 checklist items, or the hard gate; altering the en-dash; verification = a literal diff of the module body vs. the original block shows only the H2→H1 line change (heading text otherwise identical incl. en-dash); both `post-change.md` occurrences and all 5 checklist items + the hard gate present verbatim; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.3 — Create `rules/do/step-9b-pr-composition.md` with verbatim Step 9b prose.** Move the full `` ## Step 9b — PR Composition (`--auto` only) `` body (SKILL.md lines 161–234) into the new module. Single `# Title` H1 with EXACT text `` # Step 9b — PR Composition (`--auto` only) `` (backticked `--auto` preserved, `#` not `##`), no carried-over `## Step 9b` H2. Prose otherwise byte-identical — including the fenced `crafter pr-body` and `printf … | gh pr create` blocks; Step 9b has no `~/.claude/crafter/...` install path so no normalization applies.
  - **Karpathy contract:** outcome = one new module containing the full original Step 9b prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing anything, touching SKILL.md yet, altering the Steps 7–9 module; simplicity = pure move + H2→H1; drift = any non-mechanical change to the `--auto`-only trigger, the Inputs, the 5 Action steps, the fenced command blocks, the no-standalone-`git push` binding, the title-derivation rule, the failure/success branches, or the Reviewer note; altering the backticked `--auto` in the heading; verification = a literal diff of the module body vs. the original block shows only the H2→H1 line change (heading text otherwise identical incl. backticked `--auto`); the no-standalone-`git push` sentence and both fenced command blocks present verbatim; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.4 — Replace both SKILL.md sections with binding-preserving pointer stubs.** Keep both headings EXACTLY (`## Steps 7–9 — Post-Change` with the en-dash; `` ## Step 9b — PR Composition (`--auto` only) `` with the backticked `--auto`). Replace each body with a short pointer matching the established Step 0–6 stub convention (the "Apply the … procedure in …. This procedure …" opening; module pointer via `~/.claude/...`; bindings stated in plain prose; NO secondary path re-citation per the unified Slice 6 convention). The Steps 7–9 stub points to `~/.claude/crafter/rules/do/step-7-9-post-change.md` and explicitly states: the final-phase commit already landed via Step 6b; it follows the post-change steps (delegating to `post-change.md` by name in prose, no secondary path re-cited); it enforces the mandatory 5-item checklist (check docs incl. delegate ARCHITECTURE.md check to Implementer / consolidated end-of-task commit / update STATE.md / complete the task file / suggest session wrap-up); **and that the conversation does not end until all 5 items are addressed (hard gate)**. The Step 9b stub points to `~/.claude/crafter/rules/do/step-9b-pr-composition.md` and explicitly states: it runs ONLY under `--auto` and ONLY after Steps 7–9 complete; it composes a baseline body, invokes `crafter pr-body`, concatenates, derives the title from `git log -1 --format='%s'` (fallback to the task-file H1), and opens the PR via `gh pr create`; **the only push in the `--auto` flow is the one embedded in `gh pr create` — no standalone `git push`**; on failure it appends a `Decision (Auto-Recorded)` entry, does NOT run the cleanup hook, and exits via the Ad-hoc escape hatch; on success it prints the PR URL, runs the cleanup hook (`rm -rf .crafter/run/<task-id>/`), and proceeds to session wrap-up. Pointer paths stay `~/.claude/...` (installer-resolved carve-out). No other section touched.
  - **Karpathy contract:** outcome = both sections are thin stubs under their original (exact) headings, each preserving all bindings from 1.1, with the Steps 7–9 stub explicitly carrying the 5-item checklist + hard gate and the Step 9b stub explicitly carrying the `--auto`-only trigger / `crafter pr-body`+`gh pr create` chain / title rule / no-standalone-`git push` / failure & success branches; scope = only the Steps 7–9 and Step 9b sections of SKILL.md; non-goals = touching any other section, changing either heading (incl. en-dash/backtick), rewording referrers, merging the two stubs, re-citing secondary paths; simplicity = two short pointers mirroring the Step 0–6 stubs; drift = dropping any binding language, altering either heading's exact text, re-citing secondary paths (Slice 6 convention violation), editing adjacent sections; verification = both headings byte-identical (en-dash + backticked `--auto` intact); the Steps 7–9 stub explicitly names all 5 checklist items + the hard gate; the Step 9b stub explicitly names the trigger, the subcommand+`gh pr create` chain, the title rule, the no-standalone-`git push` binding, and both failure/success branches; no other SKILL.md section diffs; stop = if a pointer cannot carry a headline binding (cluster (ii) or (v)) without rewording a referrer, stop and surface (Interpretation B territory).

- [x] **1.5 — Wire the loader list, installer, install test (×2), and run the cross-reference + thin-manifest sweep.** Add `~/.claude/crafter/rules/do/step-7-9-post-change.md` then `~/.claude/crafter/rules/do/step-9b-pr-composition.md` to the SKILL.md loader list under the `<!-- do/* capability modules -->` group, after the `step-6a-session-break.md` line (SKILL.md source order). Add two `cp` lines in `install.sh`'s `rules/do/` block after install.sh:333; add two expected-path entries (`crafter/rules/do/step-7-9-post-change.md`, `crafter/rules/do/step-9b-pr-composition.md`) to `tests/test_install.sh` after line 307. Run `tests/test_install.sh`. Then read the post-extraction `SKILL.md` end-to-end and explicitly confirm: (a) every enumerated inbound reference still resolves to the retained headings (`step-6b-phase-summary.md`, `step-6a-session-break.md` ×2 + "Step 6 (Review)", the SKILL.md Step 6b/6a stub mentions, the Skill-options/`do-workflow.md` promise, Step 9b's own back-references, the `### --auto`/`#### Ad-hoc escape hatch` anchors, `step-0-resume.md` and other modules); (b) every forward reference from the stubbed sections / new modules still resolves; (c) **SKILL.md is now a thin manifest** — frontmatter + Skill options + orchestrator identity + the loader list + the Step stubs only, with NO remaining inlined Step prose anywhere. No content edits beyond 1.4 — under Interpretation A no referrer should need changing.
  - **Karpathy contract:** outcome = both new modules are loaded, deployed+tested, leave zero dangling internal references, and SKILL.md is confirmed a thin manifest; scope = loader list (two lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), plus read-only verification; non-goals = switching the installer to a directory copy, reordering existing entries, renaming files, "improving" referrers that already resolve, expanding the stubs; simplicity = exactly six one-line additions (2+2+2), each mirroring the adjacent `step-6a-session-break.md` entry; drift = restructuring the loader list or installer enumeration, normalizing other entries, patching cosmetic referrers; verification = both loader entries sit in the `do/*` group after `step-6a-session-break.md` in source order; `tests/test_install.sh` passes; a fresh end-to-end read shows no broken inbound/forward reference and confirms zero remaining inlined Step prose; stop = if the test framework cannot absorb two added files without broader changes (R4), or a real semantic gap (not cosmetic) surfaces, stop and re-plan rather than silently patch.

- [x] **Phase 1 verification.** `rules/do/step-7-9-post-change.md` and `rules/do/step-9b-pr-composition.md` each contain the full corresponding Step prose verbatim (only H2→H1 differs; exact heading text incl. en-dash and backticked `--auto` preserved); SKILL.md's two headings are retained with binding-preserving pointer stubs matching the Step 0–6 convention, the Steps 7–9 stub explicitly carrying the 5-item checklist + hard gate and the Step 9b stub explicitly carrying the `--auto`-only trigger / `crafter pr-body`+`gh pr create` chain / title rule / no-standalone-`git push` / failure & success branches; loader list includes both new modules in the `do/*` group after `step-6a-session-break.md`; `install.sh` + `tests/test_install.sh` updated (two entries each) and the test passes; all enumerated inbound and forward references resolve; SKILL.md is a thin manifest with zero remaining inlined Step prose; no SKILL.md section other than the two stubs and the loader list changed; semantic behavior identical across default / `--fast` / `--auto` / resume. Delegate to `crafter-verifier`. — crafter-verifier 9/9 PASS; thin-manifest confirmed; test 45/0.
- [x] **Phase 1 review.** `crafter-reviewer` pass; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven. Reviewer must literally diff each moved block against its original and confirm move-and-link discipline (no rewording, only H2→H1, exact heading text incl. en-dash/backtick), and explicitly confirm the Steps 7–9 stub preserves the 5-item checklist + hard gate and the Step 9b stub preserves the no-standalone-`git push` binding. — crafter-reviewer no findings; scorecard all PASS; stub condensation judged convention-compliant.

#### Phase 2 — Runtime-path hygiene + bookkeeping

After Phase 2 the new modules comply with the `{CRAFTER_HOME}` runtime-path policy (two substitutions in step-7-9-post-change.md, zero in step-9b-pr-composition.md) and the design-note ledger reflects what shipped. Narrow, proof-of-policy scope — no repo-wide sweep.

- [x] **2.1 — Apply the `{CRAFTER_HOME}` policy to the new modules.** — step-7-9 ×2 post-change.md; step-9b ×0 (confirmed).
  - **Karpathy contract:** outcome = step-7-9-post-change.md has exactly two `~/.claude/crafter/rules/post-change.md → {CRAFTER_HOME}/rules/post-change.md` substitutions and step-9b-pr-composition.md has zero; scope = only these two module files; non-goals = repo-wide normalization, editing SKILL.md, editing `post-change.md`/`do-workflow.md`/other modules/agents, adding installer path logic, normalizing bare commands/anchors/Step mentions or project-relative placeholders; simplicity = two targeted substitutions + one zero-substitution confirmation; drift = normalizing other files, introducing new placeholders, rewording surrounding prose, normalizing `.crafter/run/…` or `{PROJECT_PATH}/…`; verification = step-7-9-post-change.md contains no `~/.claude/...` and exactly two `{CRAFTER_HOME}/rules/post-change.md`; step-9b-pr-composition.md contains zero `~/.claude/...` and zero `{CRAFTER_HOME}`; bare command/anchor/Step mentions unchanged; stop = if applying the policy would require installer changes or touching out-of-scope files, stop (scope escalation).

- [x] **2.2 — Update the design-note ledger and confirm sibling delineation.** — Slice 7 entry + decomposition-complete sentence.
  - **Karpathy contract:** outcome = the design note accurately reflects Slice 7's shipped scope (two new modules, 2/0 substitutions, decomposition complete), consistent with the Slice 1–6 ledger entries; scope = a few lines in `docs/core-capabilities.md` (the runtime-path "Applied in this task" area); non-goals = restructuring the taxonomy, re-tagging taxonomy rows, restating the sibling task's plan, claiming repo-wide normalization, editing the Slice 1–6 entries beyond appending; simplicity = ≤ ~6 added lines; drift = expanding into a meta runtime-portability narrative or contradicting prior ledger entries; verification = the ledger names Slice 7, both new modules, the 2/0 counts, the sibling-task owner, and the decomposition-complete fact, and reads consistently with the Slice 6 entry; stop = if the edit would contradict the existing Slice 1–6 ledger entries, stop and reconcile explicitly.

- [x] **Phase 2 verification.** — crafter-verifier 7/7 PASS; thin-manifest independently confirmed.
- [x] **Phase 2 review.** — crafter-reviewer no findings; scorecard all PASS.

#### Post-task housekeeping

- [x] **STATE.md and skillbook update** per `rules/post-change.md` — consolidated end-of-task commit.
- [x] **Task file completion** per `rules/task-lifecycle.md`.
- [x] **Follow-up note** — recorded in `## Outcome`.

### 6. Karpathy Contract — overall

- **Outcome:** Steps 7–9 and Step 9b extracted into `rules/do/step-7-9-post-change.md` and `rules/do/step-9b-pr-composition.md` with byte-identical behavior, binding-preserving pointer stubs under the retained exact headings (Steps 7–9 stub explicitly carrying the 5-item mandatory checklist + hard gate; Step 9b stub explicitly carrying the `--auto`-only trigger, `crafter pr-body`+`gh pr create` chain, title rule, no-standalone-`git push` binding, failure & success branches), loader/installer/test wiring (×2), the `{CRAFTER_HOME}` policy applied (2 substitutions in step-7-9-post-change.md, 0 in step-9b-pr-composition.md), and SKILL.md confirmed a thin manifest — completing the maintenance decomposition.
- **Scope boundary:** source files only — `rules/do/step-7-9-post-change.md` (new), `rules/do/step-9b-pr-composition.md` (new), `skills/crafter-do/SKILL.md` (two stubs + two loader lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), `docs/core-capabilities.md` (ledger lines). No agent edits, no CLI/Go, no installed-runtime edits, no schema changes, no edits to `post-change.md` / `do-workflow.md` / `task-lifecycle.md` / any other `rules/do/*` module / Skill-options blocks.
- **Non-goals:** other Step extractions (none remain), prose rewriting, reordering, referrer rewrites, repo-wide path normalization, post-change/PR-composition semantic changes, installer style change, starting the progressive-loading effort.
- **Simplicity constraint:** two new modules, two stub replacements, two loader lines, two cp lines, two test entries, two `{CRAFTER_HOME}` substitutions (step-7-9-post-change.md only), zero substitutions in step-9b-pr-composition.md, ≤ ~6 ledger lines. Move-and-link discipline only.
- **Drift criteria:** any reword/reorder/split of Steps 7–9 or Step 9b prose; dropping any checklist item, the hard gate, the no-standalone-`git push` binding, or any trigger/chain/branch language from a stub; re-citing secondary paths in a stub (Slice 6 convention violation); altering either heading's exact text (en-dash, backticked `--auto`); editing any other SKILL.md section or any referrer that already resolves; normalizing anything outside the two enumerated `post-change.md` paths or normalizing bare commands/anchors/Step mentions/project-relative placeholders; switching the installer to a directory copy; editing out-of-scope files.
- **Verification evidence:** literal diff of each moved block vs. original shows only H2→H1 (Phase 1) and exactly the 2/0 substitutions (Phase 2); all four flows read identically; all enumerated inbound and forward references resolve; the Steps 7–9 stub provably carries the 5-item checklist + hard gate and the Step 9b stub provably carries the no-standalone-`git push` binding; `tests/test_install.sh` passes; SKILL.md is a thin manifest with zero remaining inlined Step prose; reviewer Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.
- **Stop conditions:** stop and re-plan if (a) any Steps 7–9/9b binding — especially the mandatory-checklist hard gate or the no-standalone-`git push` rule — cannot be pointer-preserved or any verbatim duplication is found (1.1 NO-GO), (b) extraction forces editing a section outside the two stubs non-mechanically, (c) the install test cannot absorb two added files without broader change (R4), (d) the `{CRAFTER_HOME}` policy unexpectedly needs installer changes or the runtime-path counts differ from 2/0, or (e) the user prefers Interpretation B.

### 7. Alternatives considered

- **Delete the headings and retarget all referrers (Interpretation B)** — rejected. Higher drift: touches `step-6b-phase-summary.md`, `step-6a-session-break.md`, the user-facing Skill-options blocks, and the `do-workflow.md` end-to-end promise (all outside the extraction targets), and risks weakening the mandatory-checklist hard gate and the no-standalone-`git push` binding. Keeping both headings as thin stubs preserves every existing reference with zero referrer edits — strictly lower drift, exactly the conservative posture Slices 1–6 used.
- **Extract Steps 7–9 and Step 9b in separate slices** — rejected. The design note explicitly groups them as one final "thin pointer work" slice; Step 9b runs only "after Steps 7–9 complete" and reuses the task-file `## Outcome` Steps 7–9 fill, so a combined slice keeps that coupling reviewable in one diff with no added drift, and cleanly closes the decomposition.
- **Treat step-7-9-post-change.md as zero-substitution like Slice 5** — rejected. Unlike Slice 5's modules, Steps 7–9 genuinely carries two concrete `~/.claude/crafter/rules/post-change.md` runtime paths; the policy requires normalizing them. The zero-substitution posture applies only to step-9b-pr-composition.md, which is genuinely install-path-free.
- **Switch `install.sh` to a `cp -R rules/do/` directory copy** — rejected for this slice. The explicit per-file enumeration is the established pattern (Slices 1–6 kept it); two added `cp` lines are lower-drift than changing installer style, and `tests/test_install.sh` enumerates files explicitly too, so a directory copy would not reduce churn.
- **Skip the pre-extraction binding gate (1.1)** — rejected. Even though this is "thin pointer work," Steps 7–9 carries the mandatory end-of-task hard gate and Step 9b carries the sole sanctioned `git push`; validating they survive a pointer before extracting is the precise risk-control Slices 1–6 used.

### 8. Risks / unknowns / flags

- **R1. Steps 7–9 mandatory-checklist + hard-gate AND Step 9b commit/PR/no-standalone-push bindings (HEADLINE risk).** Steps 7–9 carries the 5-item mandatory checklist plus the "do not end the conversation until all 5 items above are addressed" hard gate — the guarantee that docs/STATE.md/task-file completion happen before any run ends. Step 9b carries the `--auto`-only trigger, the `crafter pr-body`+`gh pr create` chain, the title-derivation rule, the failure (Ad-hoc escape hatch) and success (cleanup hook) branches, and the binding that the ONLY push is the one embedded in `gh pr create` (`rules/post-change.md` forbids a standalone `git push`). If a pointer stub weakens or loses any of these, end-of-task completeness, unattended PR safety, and the green-commit/no-rogue-push invariant break. Strongest mitigation: 1.1 explicitly gates on clusters (ii) and (v); 1.2/1.3 verification requires every binding sentence present verbatim in the modules; 1.4 requires the stubs to explicitly restate them imperatively; Phase 1 review must explicitly confirm each. Flag for user: if a reviewer judges a pointer cannot safely carry these bindings, this procedure may need to stay partly inline (escalate to Interpretation B or a hybrid).
- **R2. Installer change is required ×2 (confirmed, not "likely none").** `install.sh` and `tests/test_install.sh` both enumerate `rules/do/*` files explicitly (the `step-6a-session-break.md` lines are install.sh:333 / tests/test_install.sh:307), so two cp lines + two test entries are mandatory. Low risk, fully mechanical, but real edits — not zero.
- **R3. Inbound references span sibling modules and user-facing blocks.** `rules/do/step-6b-phase-summary.md`, `rules/do/step-6a-session-break.md` (×2 + "Step 6 (Review)"), the SKILL.md Step 6b/6a stub mentions, the `--auto`/`--fast` Skill-options blocks and the `do-workflow.md` end-to-end promise, Step 9b's own back-references, and the `### --auto`/`#### Ad-hoc escape hatch` anchors all depend on the retained headings/Step numbers. Mitigation: Interpretation A retains both headings; the 1.5 sweep explicitly re-checks every one. If the user picks B, every one becomes a required edit (notably across the `step-6b`/`step-6a` module boundaries and the user-facing Skill-options blocks).
- **R4. Install-test fragility.** `tests/test_install.sh` uses an explicit expected-file list; adding two entries is straightforward, but if the test has stricter ordering/exact-count assertions the entries may need careful placement. Mitigation: run the test in 1.5 and adjust placement only (no logic change); escalate if logic changes are needed.
- **R5. Move-and-link reflex drift on the exact-text-sensitive headings.** "Pure move" is easy to violate by silently converting the en-dash `–` in "Steps 7–9" to a hyphen `-`, by dropping or reflowing the backticked `` `--auto` `` inside the Step 9b heading, by tidying a checklist item or the fenced `crafter pr-body`/`gh pr create` blocks, by normalizing a bare command/anchor/project-relative placeholder, or by re-citing a secondary path in a stub (violating the Slice 6 convention). Mitigation: reviewer must literally diff each moved block vs. original and confirm only H2→H1 changed (heading text otherwise byte-identical incl. en-dash and backticked `--auto`), only the two enumerated substitutions applied (Phase 2), and the stubs cite no secondary paths.

---

This contract protects the byte-identical behavior of `crafter-do`'s post-change and PR-composition steps — most critically the Steps 7–9 mandatory 5-item checklist + "do not end until all 5 addressed" hard gate and Step 9b's `--auto`-only `crafter pr-body`+`gh pr create` chain, title rule, failure/success branches, and the sole-sanctioned-push (no standalone `git push`) binding — while extracting the final deferred pair into named modules exactly as Slices 1–6 extracted the rest. The approach is right because it reuses the proven, lowest-drift two-phase move-and-link pattern, gates the extraction on an explicit binding-preservation check that singles out the mandatory-gate and no-rogue-push bindings, keeps both exact headings (en-dash + backticked `--auto`) as stubs so no referrer changes, extracts the natural final pair together as the design note recommends, handles the confirmed ×2 installer/test wiring mechanically, applies the `{CRAFTER_HOME}` policy exactly per the 2/0 enumeration, and confirms SKILL.md is a thin manifest — completing the maintenance decomposition and clearing the path for the real end goal (progressive on-demand workflow loading) recorded in STATE.md `## Planned`.

## Decisions

## Outcome

**Status:** Complete. Both phases delivered; behavior byte-identical. **This slice COMPLETES the crafter-do core-capability maintenance decomposition.**

- **Phase 1** (commit `31fa89f`): `## Steps 7–9 — Post-Change` and `` ## Step 9b — PR Composition (`--auto` only) `` extracted verbatim into new `rules/do/step-7-9-post-change.md` and `rules/do/step-9b-pr-composition.md` (H2→H1 only; exact heading text incl. en-dash and backticked `--auto`); both SKILL.md sections reduced to binding-preserving pointer stubs. The Steps 7–9 stub preserves the full 5-item mandatory checklist + the verbatim "do not end the conversation until all 5 items above are addressed" hard gate; the Step 9b stub preserves the `--auto`-only trigger, the `crafter pr-body`/`gh pr create` chain, the title-derivation rule, the failure/success branches, and the verbatim no-standalone-`git push` binding. +2 loader entries. Pre-extraction binding gate GO (7/7 clusters); verifier 9/9 PASS; reviewer no findings (stub condensation judged convention-compliant); test 45/0.
- **Phase 2** (this commit): `{CRAFTER_HOME}` policy applied — step-7-9-post-change.md ×2 (`post-change.md`), step-9b-pr-composition.md ×0. `docs/core-capabilities.md` ledger extended with a Slice 7 entry that records the decomposition is complete. Verifier 7/7 PASS (independently confirmed SKILL.md is a thin manifest of 15 stubs); reviewer no findings.

**Decomposition complete:** every workflow step (Flag Validation, Project Resolution, Extension Skills, Step 0, 1, 2, 3, 4, 5, 5a, 6, 6b, 6a, Steps 7–9, Step 9b) is now a `rules/do/*` module; `skills/crafter-do/SKILL.md` is a thin manifest (frontmatter + Skill options + orchestrator identity + loader list + pointer stubs only). Across Slices 1–7: 15 modules, byte-identical behavior throughout, every phase verified + reviewed.

**Deviations from plan:** None.

**Follow-up — NOT another extraction slice.** The maintenance decomposition is done. The real end goal follows, already recorded in STATE.md `## Planned`: **progressive on-demand workflow loading** — convert `crafter-do/SKILL.md` from a loader that reads ALL `rules/do/*` modules upfront into a thin orchestrator master-plan that loads each step module lazily, only when it reaches that step. The decomposition was the prerequisite; this is the deliverable that achieves the originally intended progressive loading.
