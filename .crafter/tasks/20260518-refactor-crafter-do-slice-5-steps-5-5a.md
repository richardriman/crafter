# Task: Refactor crafter-do Slice 5 — extract Steps 5 / 5a (Drift + Phase Verification) into core capability modules

## Metadata
- **Date:** 2026-05-18
- **Work branch:** refactor/crafter-do-slice-5-steps-5-5a
- **Status:** active
- **Scope:** Medium

## Request
Slice 5 of the crafter-do core-capability decomposition. Extract the `## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules under `rules/do/` — `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md` — per the taxonomy in `docs/core-capabilities.md`, following exactly the move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slices 1–4.

Constraints:
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume).
- Pre-extraction binding gate (same as Slice 2/3/4 step 1.1): validate that Step 5's `crafter-verifier` delegation, the five recommended-action handlers (continue / record decision and continue / fix current step / ask user / replan), the orchestrator-side "append `Decision (Orchestrator Accepted)` / `Decision (User Accepted)`" task-file Edit bindings, and the "replan: return to Step 3" routing; and Step 5a's `crafter-verifier` phase-verification delegation + "if phase verification fails … discuss / re-delegate / re-run a specific step drift check" routing — all survive the pointer stubs. Confirm no other section quotes Step 5/Step 5a prose verbatim.
- Inbound references to include in the gate + 1.5 sweep: Step 4's "After each step, run Step 5 (drift check). After all steps in a phase pass drift checks, run Step 5a" routing (now in `rules/do/step-4-execute.md`); Step 6f's "Re-run Step 5a (PHASE VERIFICATION)" in the review fix-loop; Step 6a's Step 5a reference; any "Step 5"/"Step 5a" mention in `step-0-resume.md` or other extracted modules.
- HEADLINE R1: Step 5 contains orchestrator-side task-file Edit bindings (append `Decision (Orchestrator Accepted)` / `Decision (User Accepted)` entries) — analogous to Slice 4's `draft → approved` headline binding. The Step 5 stub MUST preserve these explicitly and imperatively.
- Pure move-and-link only: no prose rewriting, no reordering. Retain the `## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION` headings as binding-preserving pointer stubs matching the Step 0/1/2/3/4 stub convention (Interpretation A, lowest drift, no secondary path re-citation). No other sections touched.
- Installer + `tests/test_install.sh` need one mechanical entry per new module (per Slice 2 R2). Two new modules in this slice.
- Out of scope: any other Step extraction, extension-skill semantic changes, agent edits, CLI/Go changes, repo-wide path normalization.

## Plan

**Plan status:** approved

### 1. Complete request

Extract the `## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules, `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md`, applying the exact move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slice 1 (`20260517-refactor-crafter-do-core-capabilities.md`), Slice 2 (`20260517-refactor-crafter-do-slice-2-step-0-resume.md`), Slice 3 (`20260517-refactor-crafter-do-slice-3-steps-1-2.md`), and Slice 4 (`20260517-refactor-crafter-do-slice-4-steps-3-4.md`). This is Slice 5 of the deferred follow-up sequence; the design note flags Steps 5/5a as the recommended next slice — "straightforward procedure; few bindings."

Both section bodies move verbatim into their respective modules. Each `## Step …` heading in `SKILL.md` is reduced to a thin pointer stub that (a) keeps the exact heading text so every existing "Step 5"/"Step 5a" reference elsewhere in the file and in already-extracted sibling modules still resolves, and (b) preserves the binding contracts the prose carried. The single highest-value binding cluster is in Step 5: the two orchestrator-side task-file Edit instructions — on the verifier's "record decision and continue" action the orchestrator appends a `Decision (Orchestrator Accepted)` entry to the task file, and on the "ask user → accepted" path it appends a `Decision (User Accepted)` entry. These are the mechanisms by which drift decisions become durable handoff artifacts (Step 6b's "Accepted Decisions" summary and the resume model both depend on them); their preservation is non-negotiable (R1), exactly analogous to Slice 4's `draft → approved` headline binding. The loader list gains two entries under the existing `<!-- do/* capability modules -->` group, after `step-4-execute.md`.

Why this matters: `crafter-do/SKILL.md` is the entry point every Crafter workflow loads. Step 5 owns step-level drift verification delegation to `crafter-verifier`, the plain-text-report reminder, the five recommended-action handlers (continue / record decision and continue / fix current step / ask user / replan), the two orchestrator-side `Decision (...)`-append Edit instructions, the `crafter-implementer` re-delegation on "fix current step", and the `replan: return to Step 3` routing. Step 5a owns phase-level verification delegation to `crafter-verifier`, the same plain-text-report reminder, and the failure routing (discuss / re-delegate to Implementer / adjust plan / re-run a specific step drift check). Pulling them into named modules continues shrinking the monolith and lets drift/verification semantics evolve in focused files, exactly as Slices 1–4 did. Steps 5 and 5a are a natural pair: Step 5a consumes the per-step drift outcomes Step 5 produces, so extracting them together keeps that coupling reviewable in one slice — and the design note explicitly groups them.

**Acceptance criteria:**

- Two new modules exist:
  - `rules/do/step-5-drift.md` containing the full `## Step 5 — STEP DRIFT CHECK` prose verbatim (single `# Title` H1, no carried-over `## Step 5` H2 — same convention as the Slice 1–4 modules). Step 5's body contains NO concrete `~/.claude/crafter/...` runtime path (the `Decision (...)` handlers reference the task file conceptually, not via a runtime install path; the `~/.claude/crafter/rules/task-lifecycle.md` Decision-recording reference lives in **Step 6**, line 182, NOT in Step 5); the Implementer must re-confirm this at extraction time. Phase 2 therefore applies ZERO substitutions to this module.
  - `rules/do/step-5a-phase-verification.md` containing the full `## Step 5a — PHASE VERIFICATION` prose verbatim (single `# Title` H1, no carried-over `## Step 5a` H2). Step 5a's body contains NO concrete `~/.claude/crafter/...` runtime path; bare `crafter-verifier`/`crafter-implementer`/Step-number mentions are not runtime paths and stay unchanged. Phase 2 applies ZERO substitutions to this module.
- `skills/crafter-do/SKILL.md` keeps both headings (`## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION`) followed by short pointer stubs that match the established Step 0/1/2/3/4 stub convention (module pointer via `~/.claude/...` + bindings stated in plain prose, NO secondary path re-citation — per the Slice 3 `## Decisions` ruling). The Step 5 stub MUST explicitly preserve: `crafter-verifier` delegation in mode `step drift check`; the plain-text-report reminder ("Write your verification report as plain text … Do not create any files"); the five recommended-action handlers (continue / record decision and continue / fix current step / ask user / replan); **the two orchestrator-side task-file Edit instructions — on "record decision and continue" the orchestrator appends a `Decision (Orchestrator Accepted)` entry to the task file, and on "ask user → accepted" it appends a `Decision (User Accepted)` entry**; the `crafter-implementer` re-delegation on "fix current step"; and the `replan: return to Step 3` routing. The Step 5a stub MUST explicitly preserve: `crafter-verifier` delegation in mode `phase verification`; the same plain-text-report reminder; and the failure routing (discuss with user, then re-delegate to the Implementer / adjust the plan / re-run a specific step drift check).
- The loader list in `SKILL.md` includes `~/.claude/crafter/rules/do/step-5-drift.md` and `~/.claude/crafter/rules/do/step-5a-phase-verification.md` under the existing `<!-- do/* capability modules -->` group, after `step-4-execute.md` (order: step-5-drift then step-5a-phase-verification; loader-list/pointer paths intentionally stay `~/.claude/...`, same carve-out as Slices 1–4).
- `install.sh` deploys both new modules (one `cp` line each after the `step-4-execute.md` line at install.sh:328; `mkdir -p "$rules_dest/do"` already exists) and `tests/test_install.sh` expects both (one entry each after the `step-4-execute.md` entry at tests/test_install.sh:302); `tests/test_install.sh` passes.
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume). Every inbound reference still resolves to the retained `## Step 5` / `## Step 5a` headings: Step 4 module's routing (`rules/do/step-4-execute.md` line 12 "After each step, run Step 5 (drift check). After all steps in a phase pass drift checks, run Step 5a (phase verification) and Step 6"); Step 6 fix-loop sub-step (f.4) ("Re-run **Step 5a (PHASE VERIFICATION)**", SKILL.md line 179); Step 6a item 1 ("proceed to Step 5a (Phase Verification) and Step 6 (Review)", SKILL.md line 252); Step 6b's "Accepted Decisions" note ("from Step 5 drift-check handling", SKILL.md line 212); the SKILL.md Step 4 stub's own "routes to Step 5 … and to Step 5a" mention (line 116); Step 3's "Phase boundaries determine when phase verification and full review run" concept reference (now in `rules/do/step-3-plan.md` — confirm it references the concept by name, not by quoting Step 5a prose); and any "Step 5"/"Step 5a" mention in `step-0-resume.md`/`step-1-scope.md`/`step-2-discuss.md`. Every forward reference from the extracted prose (to `crafter-verifier`, `crafter-implementer`, Step 3 for replan) still resolves.
- Two independently committable phases, each with a per-phase verification gate and a `crafter-reviewer` gate; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.

**Validation strategy:** Editorial + installer test. Re-reading the post-extraction `SKILL.md` for the four flows produces identical semantics; a literal diff of each moved block against the original shows only the mechanical H2→H1 normalization (Phase 1) and zero `{CRAFTER_HOME}` substitutions (Phase 2 — both modules confirmed runtime-path-free); `tests/test_install.sh` passes after the installer + test edits.

### 2. Assumptions / interpretations

- **Module paths are fixed by the design note:** `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md` (taxonomy table rows for Step 5 and Step 5a). The `rules/do/` subdirectory was confirmed in Slice 1 and reused in Slices 2–4; this slice follows it without re-deciding.
- **Module structure mirrors the Slice 1–4 modules:** single `# Title` H1 (e.g. `# Step 5 — STEP DRIFT CHECK` / `# Step 5a — PHASE VERIFICATION`, or a semantically equivalent H1), no carried-over H2, prose otherwise verbatim. Exact H1 wording is the Implementer's choice within the move-and-link contract; it must not change meaning.
- **`{CRAFTER_HOME}` enumeration (re-confirm at extraction time) — both modules are zero-substitution:** Reading SKILL.md lines 118–145, neither the Step 5 body nor the Step 5a body contains a concrete `~/.claude/crafter/...` runtime path. The `Decision (Orchestrator Accepted)` / `Decision (User Accepted)` append handlers reference "the task file" conceptually, with no runtime install path. The `~/.claude/crafter/rules/task-lifecycle.md` Decision-recording reference is in **Step 6** (line 182), which is OUT OF SCOPE for this slice. The `~/.claude/crafter/rules/do/extension-skills.md` reference is likewise in Step 6 (line 149), not Step 5/5a. Therefore Phase 2 applies ZERO substitutions to both new modules (same pattern as Slice 4's step-3-plan.md, but here for both). The Implementer must re-scan each extracted block at extraction time to confirm no `~/.claude/...` occurrence exists; if any is unexpectedly present, normalize it and flag the deviation. SKILL.md loader-list and pointer-stub lines stay `~/.claude/...` (installer-resolved carve-out) — identical to Slices 1–4.
- **Installer change IS required (×2), per Slice 2 R2 (confirmed, not "likely none").** `install.sh` enumerates each `rules/do/*.md` file explicitly (the `step-4-execute.md` line is install.sh:328) and `tests/test_install.sh` enumerates each expected file (the `step-4-execute.md` entry is tests/test_install.sh:302). Two new modules therefore need exactly two mechanical `cp` lines in `install.sh` and two expected-path entries in `tests/test_install.sh`. `mkdir -p "$rules_dest/do"` already exists — no directory-creation change.
- **Lower-drift cross-reference strategy (chosen — Interpretation A):** keep both `## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION` headings in SKILL.md as thin pointer stubs rather than deleting the headings and rewriting every referrer. This preserves all existing "Step 5"/"Step 5a" references (Step 4 module routing, Step 6f, Step 6a, Step 6b "Accepted Decisions", Step 4 stub's mention, Step 5's own replan) with zero edits to other sections — strictly lower drift than renaming/retargeting referrers, exactly the conservative posture Slices 1–4 used. Justified in §7.

**Competing interpretations:**

- *Interpretation A (chosen):* thin pointer stubs under the retained `## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION` headings; referrers untouched. Same posture as Slices 1–4.
- *Interpretation B (rejected, as in Slices 2–4):* delete the Step 5/Step 5a headings, move all content out, and update every "Step 5"/"Step 5a" referrer (Step 4 module, Step 6f, Step 6a, Step 6b, Step 4 stub, Step 5 replan) to point at the modules. Rejected — higher drift, touches sections and a sibling module (`step-4-execute.md`) outside the extraction targets, and risks weakening the `Decision (...)`-append Edit bindings and the drift-check → phase-verification ordering. If the user prefers B, Phase 1 changes shape and the cross-reference sweep expands.

### 3. Non-goals

- No other Step extraction (Steps 0–4 are already modules; Steps 6–9b stay inline; this is Slice 5 only).
- No prose rewriting, reordering, splitting, or "while-we're-here" edits. Move-and-link only.
- No changes to the drift-check delegation semantics, the five recommended-action handlers, the `Decision (Orchestrator Accepted)` / `Decision (User Accepted)` append mechanics, the plain-text-report reminder, the phase-verification delegation, or the failure routing — only the reference *paths* / pointer wording are carried into the modules; the binding text itself is not edited.
- No edits to `rules/task-lifecycle.md`, `rules/do-workflow.md`, `rules/do/extension-skills.md`, `rules/do/step-0-resume.md`, `rules/do/step-1-scope.md`, `rules/do/step-2-discuss.md`, `rules/do/step-3-plan.md`, `rules/do/step-4-execute.md`, or any agent file (`crafter-verifier`, `crafter-implementer`, etc.) — and specifically NO edits to Step 6 / Step 6a / Step 6b in SKILL.md (they only reference Step 5/5a by name and stay untouched under Interpretation A).
- No changes to `agents/*.md`, CLI/Go code, buffer or task-file schema.
- No edits to any SKILL.md section other than: the Step 5 stub, the Step 5a stub, the loader list, and (cross-reference sweep) any genuinely dangling internal reference — none expected under Interpretation A.
- No repo-wide `~/.claude/...` normalization (both new modules are confirmed runtime-path-free; sibling task `20260421-skills-first-runtime-portability.md` owns the broader sweep).
- No edits to installed runtime copies under `~/.claude/...`.
- No reordering or renaming of existing Step numbers or rules files; no switch of the installer to a directory copy.

### 4. Relevant areas

- `skills/crafter-do/SKILL.md` —
  - Extraction target 1: lines 118–134 (`## Step 5 — STEP DRIFT CHECK` through "**replan:** return to Step 3 with the new discovery."). Critical bindings inside: the `crafter-verifier` delegation in mode `step drift check` (lines 120–123), the plain-text-report reminder (line 124), the present-to-user step (line 126), the FIVE recommended-action handlers (lines 130–134), the **two orchestrator-side `Decision (...)`-append Edit instructions** (line 131 `Decision (Orchestrator Accepted)`; line 133 `Decision (User Accepted)` — THE highest-value binding cluster, R1), the `crafter-implementer` re-delegation on "fix current step" (line 132), and the `replan: return to Step 3` routing (line 134).
  - Extraction target 2: lines 136–145 (`## Step 5a — PHASE VERIFICATION` through "…re-run a specific step drift check."). Bindings inside: the `crafter-verifier` delegation in mode `phase verification` (lines 138–141), the plain-text-report reminder (line 142), the receive-and-present step (line 143), and the failure routing (line 145: discuss → re-delegate to Implementer / adjust plan / re-run a specific step drift check).
  - Loader list lines 16–24 (`<!-- do/* capability modules -->` group; add two entries after the `step-4-execute.md` line at line 24).
  - Inbound references to preserve (all resolve by name/number to the retained `## Step 5`/`## Step 5a` headings; all OUT OF SCOPE to edit): `rules/do/step-4-execute.md` line 12 ("After each step, run Step 5 … run Step 5a (phase verification) and Step 6"); SKILL.md Step 4 stub line 116 ("routes to Step 5 (drift check) … and to Step 5a (phase verification)"); Step 6 fix-loop sub-step (f.4) line 179 ("Re-run **Step 5a (PHASE VERIFICATION)**"); Step 6a item 1 line 252 ("proceed to Step 5a (Phase Verification) and Step 6 (Review)"); Step 6b "Accepted Decisions" line 212 ("from Step 5 drift-check handling or the review loop"); Step 5's own replan line 134 ("return to Step 3"); Step 3's "Phase boundaries determine when phase verification and full review run" concept reference (now in `rules/do/step-3-plan.md` — confirm it names the concept, does not quote Step 5a prose). Confirm all in the 1.1 gate and the 1.5 sweep.
- `rules/do/step-5-drift.md`, `rules/do/step-5a-phase-verification.md` — new files (do not exist yet).
- `rules/do/flag-validation.md`, `rules/do/project-resolution.md`, `rules/do/extension-skills.md`, `rules/do/step-0-resume.md`, `rules/do/step-1-scope.md`, `rules/do/step-2-discuss.md`, `rules/do/step-3-plan.md`, `rules/do/step-4-execute.md` — precedent for module structure (single H1, tone, `{CRAFTER_HOME}` usage where applicable) and the Slice 3 stub convention (pointer + bindings in prose, no secondary path re-citation).
- `docs/core-capabilities.md` — governing design note: taxonomy rows for Step 5 (`rules/do/step-5-drift.md`) and Step 5a (`rules/do/step-5a-phase-verification.md`), naming scheme, loading convention, runtime-path policy, deferred-slice ordering note (Steps 5/5a = recommended next slice, "straightforward procedure; few bindings"), and the Slice 1/2/3/4 ledger entries to extend (after the Slice 4 entry at line 122).
- `install.sh` line 328 (`step-4-execute.md` cp line) — `rules/do/` deployment block; two `cp` lines to add after it.
- `tests/test_install.sh` line 302 (`step-4-execute.md` entry) — expected `rules/do/` files; two entries to add after it.
- `.crafter/tasks/20260517-refactor-crafter-do-slice-4-steps-3-4.md` — the immediate precedent: two-phase structure, pre-extraction binding gate, R1–R5 risk framing, Interpretation A vs B, installer/test "one mechanical entry per module" handling, Karpathy-contract style, the `## Decisions` stub-convention ruling, zero-substitution module handling (step-3-plan.md precedent).

### 5. Vertical phases and steps

Two phases, mirroring Slice 4's exact shape. Phase 1 = the extraction (pre-extraction binding gate + two modules + two pointer stubs + loader/installer/test wiring ×2 + cross-reference sweep) — independently committable and behavior-identical. Phase 2 = runtime-path hygiene scoped to the new modules (confirm zero-substitution) + design-note ledger bookkeeping. Each phase ends in a reviewable state with a `crafter-reviewer` gate.

#### Phase 1 — Extract Steps 5 / 5a into capability modules (move-and-link)

After Phase 1, `## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION` in `SKILL.md` are thin pointer stubs under their original headings; the full prose lives verbatim in `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md`; the loader list and installer/test deploy both new modules; all cross-references resolve. Behavior identical across all four flows.

- [x] **1.1 — Validate Step 5/Step 5a bindings survive pointer stubs (pre-extraction binding gate, analysis-only, GO/NO-GO).** — GO; all 6 clusters PRESERVED (cluster iii: both Decision-append instructions preservable with explicit orchestrator-actor wording), both bodies zero-runtime-path, verbatim-quote check clean, Interpretation A confirmed. Before moving anything, confirm in writing (in the step's working notes / drift evidence) a GO/NO-GO verdict that each binding cluster is pointer-preservable AND that no other SKILL.md section quotes Step 5/Step 5a prose verbatim (it must reference them by name/number, not duplicate the text): (i) Step 5's `crafter-verifier` delegation (mode `step drift check`) + the plain-text-report reminder + present-to-user step; (ii) Step 5's FIVE recommended-action handlers (continue / record decision and continue / fix current step / ask user / replan); (iii) **Step 5's two orchestrator-side task-file Edit instructions** — on "record decision and continue" the orchestrator appends a `Decision (Orchestrator Accepted)` entry to the task file, and on "ask user → accepted" it appends a `Decision (User Accepted)` entry (the headline binding cluster, R1; verify a stub can carry both without weakening them); (iv) Step 5's `crafter-implementer` re-delegation on "fix current step" and the `replan: return to Step 3` routing; (v) Step 5a's `crafter-verifier` delegation (mode `phase verification`) + plain-text-report reminder + the failure routing (discuss / re-delegate to Implementer / adjust plan / re-run a specific step drift check); (vi) inbound references that depend on the retained headings — `rules/do/step-4-execute.md` "run Step 5 … run Step 5a" routing, SKILL.md Step 4 stub's "routes to Step 5 … Step 5a", Step 6f "Re-run Step 5a", Step 6a "proceed to Step 5a", Step 6b "from Step 5 drift-check handling", Step 5's own replan "return to Step 3", and Step 3's "phase verification … run" concept reference in `rules/do/step-3-plan.md` (confirm it names the concept, does not quote Step 5a prose). Also confirm both Step 5 and Step 5a bodies contain ZERO `~/.claude/...` runtime paths (the only nearby runtime paths are in Step 6, out of scope). If any binding cannot survive a pointer stub, stop and surface as a risk before extracting (NO-GO).
  - **Karpathy contract:** outcome = a written GO/NO-GO on pointer-preservability of all six binding clusters, each with an explicit preserved/at-risk verdict citing the inbound/forward reference it depends on, with cluster (iii) — the two `Decision (...)`-append Edit instructions — called out as the headline binding, plus a confirmation that both bodies are runtime-path-free; scope = analysis only, zero file edits; non-goals = moving prose, editing referrers, drafting stub text; simplicity = a short evidence note, not a redesign; drift = beginning any extraction before the verdict is recorded, or expanding into a referrer rewrite; verification = each of the six clusters has an explicit verdict, cluster (iii) explicitly addressed, the zero-runtime-path finding recorded, and a confirmation that no section duplicates Step 5/Step 5a prose verbatim; stop = any binding judged not pointer-preservable (especially (iii)), or any verbatim duplication found → halt, record risk, do not extract (Interpretation B territory).

- [x] **1.2 — Create `rules/do/step-5-drift.md` with verbatim Step 5 prose.** Move the full `## Step 5 — STEP DRIFT CHECK` body (SKILL.md lines 120–134, from "Delegate verification to the **`crafter-verifier`** agent:" through "**replan:** return to Step 3 with the new discovery.") into the new module. Single `# Title` H1, no carried-over `## Step 5` H2 (same convention as the Slice 1–4 modules). Prose otherwise byte-identical — Step 5 has no `~/.claude/...` runtime path, so no normalization applies; both `Decision (Orchestrator Accepted)` and `Decision (User Accepted)` append sentences are copied exactly as-is.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 5 prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing anything, touching SKILL.md yet, creating the Step 5a module yet; simplicity = pure move + H2→H1; drift = any non-mechanical change to the verifier-delegation, plain-text-report reminder, the five handlers, the two `Decision (...)`-append instructions, the `crafter-implementer` re-delegation, or the replan routing; verification = a literal diff of the module body against the original SKILL.md Step 5 block shows only the H2→H1 line change and no other differences; both `Decision (Orchestrator Accepted)` and `Decision (User Accepted)` append sentences present verbatim; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.3 — Create `rules/do/step-5a-phase-verification.md` with verbatim Step 5a prose.** Move the full `## Step 5a — PHASE VERIFICATION` body (SKILL.md lines 138–145, from "When all steps in the current phase have passed drift checks, delegate phase verification…" through "…re-run a specific step drift check.") into the new module. Single `# Title` H1, no carried-over `## Step 5a` H2. Prose otherwise byte-identical — Step 5a has no `~/.claude/...` runtime path, so no normalization applies.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 5a prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing anything, touching SKILL.md yet, altering the Step 5 module; simplicity = pure move + H2→H1; drift = any non-mechanical change to the `crafter-verifier` phase-verification delegation, the plain-text-report reminder, or the failure routing; verification = a literal diff of the module body against the original SKILL.md Step 5a block shows only the H2→H1 line change and no other differences; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.4 — Replace both SKILL.md sections with binding-preserving pointer stubs.** Keep both headings exactly (`## Step 5 — STEP DRIFT CHECK`, `## Step 5a — PHASE VERIFICATION`). Replace each body with a short pointer matching the established Step 0/1/2/3/4 stub convention (module pointer via `~/.claude/...` + bindings stated in plain prose, NO secondary path re-citation — per the Slice 3 `## Decisions` ruling): the Step 5 stub points to `~/.claude/crafter/rules/do/step-5-drift.md` and explicitly states it delegates step drift verification to `crafter-verifier` (mode `step drift check`) with the plain-text-report reminder, handles the five recommended actions (continue / record decision and continue / fix current step / ask user / replan), **and that on "record decision and continue" the orchestrator itself appends a `Decision (Orchestrator Accepted)` entry to the task file and on "ask user → accepted" it appends a `Decision (User Accepted)` entry**, re-delegates the current step to `crafter-implementer` on "fix current step", and returns to Step 3 on "replan"; the Step 5a stub points to `~/.claude/crafter/rules/do/step-5a-phase-verification.md` and explicitly states it delegates phase verification to `crafter-verifier` (mode `phase verification`) with the plain-text-report reminder, and on failure discusses with the user and re-delegates to the Implementer / adjusts the plan / re-runs a specific step drift check. Pointer paths stay `~/.claude/...` (installer-resolved carve-out). No other section touched.
  - **Karpathy contract:** outcome = both Step 5 and Step 5a sections are thin stubs under their original headings, each preserving all bindings from 1.1, with the Step 5 stub explicitly carrying the two orchestrator-side `Decision (...)`-append Edit instructions; scope = only the Step 5 and Step 5a sections of SKILL.md; non-goals = touching any other section (especially Step 6/6a/6b), changing the heading text, rewording referrers, merging the two stubs, re-citing secondary paths; simplicity = two short pointers mirroring the Step 0/1/2/3/4 stubs; drift = dropping either `Decision (...)`-append instruction or any handler/routing/delegation binding language, altering either heading, re-citing secondary paths (Slice 3 convention violation), editing adjacent sections; verification = both headings unchanged; the Step 5 stub explicitly names both the `Decision (Orchestrator Accepted)` and `Decision (User Accepted)` append bindings plus the verifier/reminder/five-handler/replan/implementer-re-delegation language; the Step 5a stub explicitly names the verifier phase-verification delegation + reminder + failure routing; no other SKILL.md section diffs; stop = if a pointer cannot carry a binding (especially either `Decision (...)`-append instruction) without rewording a referrer, stop and surface (Interpretation B territory).

- [x] **1.5 — Wire the loader list, installer, and install test (×2), and run the cross-reference sweep.** Add `~/.claude/crafter/rules/do/step-5-drift.md` and `~/.claude/crafter/rules/do/step-5a-phase-verification.md` to the SKILL.md loader list under the existing `<!-- do/* capability modules -->` group, after the `step-4-execute.md` line (order: step-5-drift then step-5a-phase-verification, consistent with Step ordering and prior slices). Add two `cp` lines in `install.sh`'s `rules/do/` block after install.sh:328 (the `step-4-execute.md` line); add two expected-path entries (`crafter/rules/do/step-5-drift.md`, `crafter/rules/do/step-5a-phase-verification.md`) to `tests/test_install.sh` after line 302 (the `step-4-execute.md` entry). Run `tests/test_install.sh`. Then read the post-extraction `SKILL.md` end-to-end and explicitly confirm every inbound reference still resolves to the retained `## Step 5`/`## Step 5a` headings: `rules/do/step-4-execute.md` "run Step 5 … run Step 5a" routing, the SKILL.md Step 4 stub "routes to Step 5 … Step 5a", Step 6 fix-loop sub-step (f.4) "Re-run **Step 5a (PHASE VERIFICATION)**", Step 6a item 1 "proceed to Step 5a (Phase Verification)", Step 6b "Accepted Decisions … from Step 5 drift-check handling", Step 5's own replan "return to Step 3", and Step 3's "phase verification … run" concept reference in `rules/do/step-3-plan.md` (confirm it names the concept, does not quote Step 5a prose); and every forward reference from the now-stubbed sections / new modules (to `crafter-verifier`, `crafter-implementer`, Step 3) still resolves. No content edits beyond 1.4 — under Interpretation A no referrer should need changing.
  - **Karpathy contract:** outcome = both new modules are loaded by SKILL.md, deployed+tested by the installer, and confirmed to leave zero dangling internal references (including the `step-4-execute.md` routing, Step 6f, Step 6a, Step 6b "Accepted Decisions", Step 4 stub, Step 5 replan, and Step 3 concept ref); scope = loader list (two lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), plus read-only verification; non-goals = switching the installer to a directory copy, reordering existing entries, renaming files, "improving" referrers that already resolve, expanding the stubs; simplicity = exactly six one-line additions (2 + 2 + 2), each mirroring the adjacent `step-4-execute.md` entry; drift = restructuring the loader list or installer enumeration, normalizing other entries, patching cosmetic referrers; verification = both loader entries sit in the `do/*` group after `step-4-execute.md`; `tests/test_install.sh` passes; a fresh end-to-end read produces no broken Step 5/Step 5a inbound or forward references (the seven enumerated inbound refs explicitly re-checked); stop = if the test framework cannot absorb two added files without broader changes (R4), or a real semantic gap (not cosmetic) surfaces, stop and re-plan rather than silently patch.

- [x] **Phase 1 verification.** — crafter-verifier 9/9 PASS; both Decision-append bindings imperative; test 45/0; 7 inbound refs resolve. `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md` each contain the full corresponding Step prose verbatim (only H2→H1 differs); SKILL.md's `## Step 5 — STEP DRIFT CHECK` and `## Step 5a — PHASE VERIFICATION` headings are retained with binding-preserving pointer stubs matching the Step 0/1/2/3/4 convention, the Step 5 stub explicitly carrying both the `Decision (Orchestrator Accepted)` and `Decision (User Accepted)` orchestrator-side append instructions; loader list includes both new modules in the `do/*` group after `step-4-execute.md`; `install.sh` + `tests/test_install.sh` updated (two entries each) and the test passes; all inbound (`step-4-execute.md` routing, Step 4 stub, Step 6f, Step 6a, Step 6b, Step 5 replan, Step 3 concept ref) and forward Step 5/Step 5a references resolve; no SKILL.md section other than the two stubs and the loader list changed; semantic behavior identical across default / `--fast` / `--auto` / resume. Delegate to `crafter-verifier`.
- [x] **Phase 1 review.** — crafter-reviewer no findings; scorecard all PASS; both Decision-append bindings imperative. `crafter-reviewer` pass; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven. Reviewer must literally diff each moved block against its original and confirm move-and-link discipline (no rewording, only H2→H1), and explicitly confirm the Step 5 stub preserves both the `Decision (Orchestrator Accepted)` and `Decision (User Accepted)` orchestrator-side append bindings.

#### Phase 2 — Runtime-path hygiene + bookkeeping

After Phase 2 the new modules are confirmed compliant with the `{CRAFTER_HOME}` runtime-path policy (both zero-substitution) and the design-note ledger reflects what shipped. Narrow, proof-of-policy scope — no repo-wide sweep.

- [ ] **2.1 — Confirm the `{CRAFTER_HOME}` policy on the new modules (both zero-substitution).** In `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md`, confirm there is NO `~/.claude/...` occurrence (expected — Step 5/5a reference the task file conceptually and delegate to bare-named agents; the only nearby runtime paths, `~/.claude/crafter/rules/task-lifecycle.md` and `~/.claude/crafter/rules/do/extension-skills.md`, are in Step 6, which is out of scope and not extracted here). If any `~/.claude/...` is unexpectedly present in either module, normalize it to the `{CRAFTER_HOME}/...` form and flag the deviation. Bare `crafter-verifier`/`crafter-implementer`/Step-number mentions are not runtime install paths and stay unchanged. No other files touched; SKILL.md pointer/loader lines keep `~/.claude/...` (installer-resolved carve-out).
  - **Karpathy contract:** outcome = both new modules confirmed to have zero hard-coded `~/.claude/...` references (no substitution needed); scope = read-only confirmation of `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md` (substitution only if an unexpected occurrence is found); non-goals = repo-wide normalization, editing SKILL.md, editing `rules/task-lifecycle.md` / `rules/do-workflow.md` / Step 6 / agents, adding installer path logic, adding a footnote; simplicity = a confirmation, with at most a substitution if a surprise occurrence appears; drift = normalizing other files, introducing new placeholders, rewording surrounding prose, normalizing bare agent/Step mentions; verification = both modules contain zero `~/.claude/...` references; bare agent/Step mentions unchanged; stop = if applying the policy would require installer changes or touching Step 6, stop (scope escalation).

- [ ] **2.2 — Update the design-note ledger and confirm sibling delineation.** In `docs/core-capabilities.md`, append a Slice 5 entry to the runtime-path "Applied in this task" ledger (after the existing Slice 4 entry at line 122) recording that Slice 5 created `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md` and that the `{CRAFTER_HOME}` policy required ZERO substitutions in either (both contain no runtime install paths; the orchestrator-side `Decision (...)`-append handlers reference the task file conceptually and the `task-lifecycle.md`/`extension-skills.md` runtime paths live in Step 6, out of scope), restating that the bare `crafter-verifier`/`crafter-implementer` mentions are not runtime paths and that repo-wide normalization remains owned by `20260421-skills-first-runtime-portability.md`. Keep it to a few lines, consistent in tone and structure with the Slice 1/2/3/4 entries; do not restructure the doc or re-tag taxonomy rows.
  - **Karpathy contract:** outcome = the design note accurately reflects Slice 5's shipped scope (two new modules, zero substitutions), consistent with the Slice 1/2/3/4 ledger entries; scope = a few lines in `docs/core-capabilities.md` (the runtime-path "Applied in this task" area); non-goals = restructuring the taxonomy, restating the sibling task's plan, claiming repo-wide normalization, re-tagging other taxonomy rows, editing the Slice 1/2/3/4 entries beyond appending; simplicity = ≤ ~5 added lines; drift = expanding into a meta runtime-portability narrative or contradicting prior ledger entries; verification = the ledger names Slice 5, both new modules, the zero-substitution count, and the sibling-task owner, and reads consistently with the Slice 4 entry; stop = if the edit would contradict the existing Slice 1/2/3/4 ledger entries, stop and reconcile explicitly.

- [ ] **Phase 2 verification.** `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md` each have zero hard-coded `~/.claude/...` references; bare agent/Step mentions unchanged; `docs/core-capabilities.md` ledger reflects Slice 5 consistently with Slice 1/2/3/4; no files outside this phase's set touched; behavior still byte-identical. Delegate to `crafter-verifier`.
- [ ] **Phase 2 review.** `crafter-reviewer` pass; Karpathy scorecard PASS on Surgical Changes (no scope creep into untouched files) and Goal-Driven (policy confirmed; zero substitutions applied exactly as enumerated).

#### Post-task housekeeping

- [ ] **STATE.md and skillbook update** per `rules/post-change.md` — consolidated end-of-task commit.
- [ ] **Task file completion** per `rules/task-lifecycle.md` (Status → completed, `## Outcome` filled, steps checked).
- [ ] **Follow-up note** — record in `## Outcome` that Slice 6 — extract Steps 6 / 6b / 6a (Review + Phase Summary + Session Break) per the deferred-slice order in `docs/core-capabilities.md` (highest density; extract together since they share flag-state and approval-path logic) — is the next candidate. Remaining deferred after that: Steps 7–9 / 9b (Post-Change + PR Composition).

### 6. Karpathy Contract — overall

- **Outcome:** Steps 5 and 5a extracted into `rules/do/step-5-drift.md` and `rules/do/step-5a-phase-verification.md` with byte-identical behavior, binding-preserving pointer stubs under the retained headings (the Step 5 stub explicitly carrying both orchestrator-side `Decision (Orchestrator Accepted)` / `Decision (User Accepted)` append instructions), loader/installer/test wiring (×2), and the `{CRAFTER_HOME}` policy confirmed (zero substitutions — both modules runtime-path-free).
- **Scope boundary:** source files only — `rules/do/step-5-drift.md` (new), `rules/do/step-5a-phase-verification.md` (new), `skills/crafter-do/SKILL.md` (two stubs + two loader lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), `docs/core-capabilities.md` (ledger lines). No agent edits, no CLI/Go, no installed-runtime edits, no schema changes, no edits to `rules/task-lifecycle.md` / `rules/do-workflow.md` / `rules/do/extension-skills.md` / any other `rules/do/*` module / Step 6 / Step 6a / Step 6b.
- **Non-goals:** other Step extractions, prose rewriting, reordering, referrer rewrites, repo-wide path normalization, drift-handler/Decision-append/phase-verification semantic changes, installer style change to directory copy.
- **Simplicity constraint:** two new modules, two stub replacements, two loader lines, two cp lines, two test entries, zero `{CRAFTER_HOME}` substitutions, ≤ ~5 ledger lines. Move-and-link discipline only.
- **Drift criteria:** any reword/reorder/split of Step 5 or Step 5a prose; dropping either `Decision (...)`-append instruction or any handler/routing/delegation binding from a stub; re-citing secondary paths in a stub (Slice 3 convention violation); altering either heading; editing any other SKILL.md section or any referrer that already resolves; normalizing `~/.claude/...` anywhere (both modules are runtime-path-free) or normalizing bare agent/Step mentions; switching the installer to a directory copy; editing `task-lifecycle.md` / `do-workflow.md` / other `rules/do/*` modules / Step 6 / agents / CLI / schema.
- **Verification evidence:** literal diff of each moved block vs. original shows only H2→H1 (Phase 1) and zero substitutions (Phase 2); all four flows read identically; all inbound (`step-4-execute.md` routing, Step 4 stub, Step 6f, Step 6a, Step 6b, Step 5 replan, Step 3 concept ref) and forward Step 5/Step 5a references resolve; the Step 5 stub provably carries both `Decision (...)`-append instructions; `tests/test_install.sh` passes; both new modules have zero `~/.claude/...` refs; reviewer Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.
- **Stop conditions:** stop and re-plan if (a) any Step 5/Step 5a binding — especially either orchestrator-side `Decision (...)`-append Edit instruction — cannot be pointer-preserved or any verbatim duplication is found (1.1 NO-GO), (b) extraction forces editing a section outside the two stubs non-mechanically, (c) the install test cannot absorb two added files without broader change (R4), (d) the `{CRAFTER_HOME}` policy unexpectedly needs installer changes or surfaces a runtime path inside the extracted blocks, or (e) the user prefers Interpretation B.

### 7. Alternatives considered

- **Delete the Step 5/Step 5a headings and retarget all referrers (Interpretation B)** — rejected. Higher drift: touches the `step-4-execute.md` module, the SKILL.md Step 4 stub, the Step 6 fix loop, Step 6a, Step 6b's "Accepted Decisions" note, and Step 5's own replan routing (all outside the extraction targets), and risks weakening the two `Decision (...)`-append Edit bindings and the drift-check → phase-verification ordering. Keeping both headings as thin stubs preserves every existing "Step 5"/"Step 5a" reference with zero referrer edits — strictly lower drift, exactly the conservative posture Slices 1–4 used.
- **Extract Steps 5 and 5a in separate slices** — rejected. The design note explicitly recommends Steps 5/5a as a single "straightforward procedure; few bindings" slice; Step 5a consumes the per-step drift outcomes Step 5 produces, so a combined slice keeps that coupling reviewable in one diff with no added drift.
- **Apply a defensive `{CRAFTER_HOME}` substitution anyway / add a footnote** — rejected. Both bodies are confirmed runtime-path-free; inventing a placeholder where none is needed would be non-mechanical drift and contradict the policy's "do not make the situation worse, do not over-edit" posture. Slice 4's step-3-plan.md established the zero-substitution pattern; Slice 5 follows it for both modules.
- **Switch `install.sh` to a `cp -R rules/do/` directory copy** — rejected for this slice. The installer's explicit per-file enumeration is the established pattern (Slices 1–4 kept it); two added `cp` lines are lower-drift than changing installer style, and `tests/test_install.sh` enumerates files explicitly too, so a directory copy would not reduce churn.
- **Skip the pre-extraction binding gate (1.1)** — rejected. Although the design note calls Steps 5/5a "few bindings," Step 5 carries two orchestrator-side task-file Edit instructions (the durable-decision mechanism that Step 6b's summary and the resume model depend on); validating they survive a pointer before extracting is the precise risk-control Slices 1–4 used.

### 8. Risks / unknowns / flags

- **R1. Decision-append Edit-binding preservation (headline risk).** Step 5 carries two orchestrator-side task-file Edit instructions: on "record decision and continue" the orchestrator appends a `Decision (Orchestrator Accepted)` entry, and on "ask user → accepted" it appends a `Decision (User Accepted)` entry. These are how drift decisions become durable handoff artifacts — Step 6b's "Accepted Decisions" summary (SKILL.md line 212) and the resume model both depend on them. If a pointer stub weakens or loses either binding, accepted-drift accounting and the green-commit decision trail break. Strongest mitigation: 1.1 explicitly gates on cluster (iii); 1.2 verification requires both `Decision (...)` sentences present verbatim in the module; 1.4 requires the stub to explicitly restate both orchestrator-side append instructions; Phase 1 review must explicitly confirm both. Flag for user: if a reviewer judges a pointer cannot safely carry these bindings, this procedure may need to stay partly inline (escalate to Interpretation B or a hybrid).
- **R2. Installer change is required ×2 (confirmed, not "likely none").** `install.sh` and `tests/test_install.sh` both enumerate `rules/do/*` files explicitly (the `step-4-execute.md` lines are install.sh:328 / tests/test_install.sh:302), so two cp lines + two test entries are mandatory. Low risk, fully mechanical, but real edits — not zero.
- **R3. Inbound Step 5/Step 5a references span sibling modules and multiple Step-6 family sites.** `rules/do/step-4-execute.md` (a sibling module, not just SKILL.md), the SKILL.md Step 4 stub, the Step 6 fix loop sub-step (f.4), Step 6a item 1, Step 6b's "Accepted Decisions" note, Step 5's own replan, and Step 3's phase-verification concept reference in `rules/do/step-3-plan.md` all depend on the retained headings/Step numbers. Mitigation: Interpretation A retains both headings; the 1.5 sweep explicitly re-checks all seven. If the user picks B, every one of these becomes a required edit (notably across the `step-4-execute.md` and `step-3-plan.md` module boundaries).
- **R4. Install-test fragility.** `tests/test_install.sh` uses an explicit expected-file list; adding two entries is straightforward, but if the test has stricter ordering/exact-count assertions the entries may need careful placement. Mitigation: run the test in 1.5 and adjust placement only (no logic change); escalate if logic changes are needed.
- **R5. Move-and-link reflex drift.** "Pure move" is easy to violate with a one-word tidy in the verifier-delegation or handler prose, by accidentally normalizing a bare agent/Step mention, by inventing a `{CRAFTER_HOME}` placeholder where none is needed (both modules are runtime-path-free), or by re-citing a secondary path in a stub (violating the Slice 3 convention). Mitigation: reviewer must literally diff each moved block vs. original and confirm only H2→H1 changed (Phase 1) and zero substitutions (Phase 2), and that stubs cite no secondary paths.

---

This contract protects the byte-identical behavior of `crafter-do`'s step-drift-check and phase-verification steps — most critically the two orchestrator-side `Decision (Orchestrator Accepted)` / `Decision (User Accepted)` task-file Edit instructions, the five recommended-action handlers, and the drift-check → phase-verification → review routing — while extracting Steps 5 and 5a into named modules exactly as Slices 1–4 extracted the preamble, resume detection, scope/discuss, and plan/execute. The approach is right because it reuses the proven, lowest-drift move-and-link pattern, gates the extraction on an explicit binding-preservation check that singles out the highest-value Decision-append bindings, keeps both headings as stubs so no referrer (including the sibling Step 4 and Step 3 modules) changes, extracts the natural pair together as the design note recommends, and handles the confirmed ×2 installer/test wiring mechanically while correctly recognizing both modules as zero-substitution for the `{CRAFTER_HOME}` policy.

## Decisions

## Outcome
<!-- Filled on completion: what was actually done, commit SHA(s), any deviations from plan -->
