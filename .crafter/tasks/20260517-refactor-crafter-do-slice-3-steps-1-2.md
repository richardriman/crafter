# Task: Refactor crafter-do Slice 3 — extract Steps 1–2 (Scope + Discuss) into core capability modules

## Metadata
- **Date:** 2026-05-17
- **Work branch:** refactor/crafter-do-slice-3-steps-1-2
- **Status:** active
- **Scope:** Medium

## Request
Slice 3 of the crafter-do core-capability decomposition. Extract the `## Step 1 — Completeness and scope` and `## Step 2 — DISCUSS / RESEARCH (when incomplete or uncertain)` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules under `rules/do/` — `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md` — per the taxonomy in `docs/core-capabilities.md`, following exactly the move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slice 1 (`.crafter/tasks/20260517-refactor-crafter-do-core-capabilities.md`) and Slice 2 (`.crafter/tasks/20260517-refactor-crafter-do-slice-2-step-0-resume.md`).

Constraints:
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume).
- Pre-extraction binding gate (same as Slice 2 step 1.1): validate that the extension-skill supplemental-only cross-references inside Step 1, the Step 1 → Step 2/Step 3 routing, and the Step 2 → Step 3 routing survive the pointer stubs before extracting.
- Pure move-and-link only: no prose rewriting, no reordering. Retain the `## Step 1 — Completeness and scope` and `## Step 2 — DISCUSS / RESEARCH …` headings as binding-preserving pointer stubs (Interpretation A, lowest drift). No other sections touched.
- Installer + `tests/test_install.sh` need one mechanical entry per new module (per Slice 2 R2). Two new modules in this slice.
- Out of scope: any other Step extraction, extension-skill semantic changes, agent edits, CLI/Go changes, repo-wide path normalization.

## Plan

**Plan status:** approved

### 1. Complete request

Extract the `## Step 1 — Completeness and scope` and `## Step 2 — DISCUSS / RESEARCH (when incomplete or uncertain)` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules, `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md`, applying the exact move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slice 1 (`.crafter/tasks/20260517-refactor-crafter-do-core-capabilities.md`) and Slice 2 (`.crafter/tasks/20260517-refactor-crafter-do-slice-2-step-0-resume.md`). This is Slice 3 of the deferred follow-up sequence; the design note flags Steps 1–2 as the recommended next slice — a "natural pair, low cross-reference density."

Both section bodies move verbatim into their respective modules. Each `## Step …` heading in `SKILL.md` is reduced to a thin pointer stub that (a) keeps the exact heading text so every existing "Step 1" / "Step 2" reference elsewhere in the file still resolves, and (b) preserves the binding contracts the prose carried — most notably Step 1's extension-skill supplemental-only cross-references, the Step 1 → Step 2 / Step 3 routing, and the Step 2 → Step 3 routing. The loader list gains two entries under the existing `<!-- do/* capability modules -->` group, after `step-0-resume.md`.

Why this matters: `crafter-do/SKILL.md` is the entry point every Crafter workflow loads. Steps 1 and 2 own completeness assessment, scope classification (Small/Medium/Large), and the clarify-or-delegate-to-Analyzer decision — the gate that determines whether the workflow can proceed to planning. Pulling them into named modules continues shrinking the monolith and lets scope/discuss semantics evolve in focused files, exactly as Slices 1 and 2 did for the preamble and resume detection. Steps 1 and 2 are a natural pair: Step 1's completeness check feeds directly into Step 2's discuss/research path, and both converge on the same "create task file, continue to Step 3" exit, so extracting them together keeps that coupling reviewable in one slice.

**Acceptance criteria:**

- Two new modules exist:
  - `rules/do/step-1-scope.md` containing the full `## Step 1 — Completeness and scope` prose verbatim (single `# Title` H1, no carried-over `## Step 1` H2 — same convention as the Slice 1/2 modules), with its one `~/.claude/crafter/...` runtime path(s) normalized to `{CRAFTER_HOME}/...` per the runtime-path policy (substitution applied in Phase 2; see §2).
  - `rules/do/step-2-discuss.md` containing the full `## Step 2 — DISCUSS / RESEARCH …` prose verbatim (single `# Title` H1, no carried-over `## Step 2` H2), with its one `~/.claude/crafter/...` runtime path(s) normalized to `{CRAFTER_HOME}/...`.
- `skills/crafter-do/SKILL.md` keeps both headings (`## Step 1 — Completeness and scope` and `## Step 2 — DISCUSS / RESEARCH (when incomplete or uncertain)`) followed by short pointer stubs that preserve the binding contracts (Step 1's extension-skill supplemental-only invariant and Analyzer/plan-context hand-off; Step 1 → Step 2/Step 3 routing; Step 2 → Step 3 routing; task-file creation per `task-lifecycle.md`).
- The loader list in `SKILL.md` includes `~/.claude/crafter/rules/do/step-1-scope.md` and `~/.claude/crafter/rules/do/step-2-discuss.md` under the existing `<!-- do/* capability modules -->` group, after `step-0-resume.md` (loader-list/pointer paths intentionally stay `~/.claude/...`, same carve-out as Slice 1/2).
- `install.sh` deploys both new modules (one `cp` line each in the `rules/do/` block; `mkdir -p "$rules_dest/do"` already exists) and `tests/test_install.sh` expects both (one entry each); `tests/test_install.sh` passes.
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume). Every inbound reference to "Step 1" / "Step 2" (Step 0's "continue to Step 1" sense via the retained heading, Step 1's own forward routing, Step 2's forward routing) and every forward reference from the extracted prose (to `extension-skills.md`, `do-workflow.md → ### Extension-skill supplemental-only invariant`, `task-lifecycle.md`, `crafter-analyzer`, Steps 2/3) still resolves.
- Two independently committable phases, each with a per-phase verification gate and a `crafter-reviewer` gate; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.

**Validation strategy:** Editorial + installer test. Re-reading the post-extraction `SKILL.md` for the four flows produces identical semantics; a literal diff of each moved block against the original shows only the mechanical `{CRAFTER_HOME}` substitution(s) and the H2→H1 normalization; `tests/test_install.sh` passes after the installer + test edits.

### 2. Assumptions / interpretations

- **Module paths are fixed by the design note:** `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md` (taxonomy table rows for Step 1 and Step 2). The `rules/do/` subdirectory was confirmed in Slice 1 and reused in Slice 2; this slice follows it without re-deciding.
- **Module structure mirrors Slice 1/2 modules:** single `# Title` H1 (e.g. `# Step 1 — Completeness and scope` / `# Step 2 — DISCUSS / RESEARCH (when incomplete or uncertain)`, or a semantically equivalent H1), no carried-over H2, prose otherwise verbatim. Exact H1 wording is the Implementer's choice within the move-and-link contract; it must not change meaning.
- **`{CRAFTER_HOME}` applies to the concrete runtime paths inside each module.** Enumerated from the current SKILL.md text:
  - `rules/do/step-1-scope.md` — Step 1's body contains the concrete runtime path `~/.claude/crafter/rules/do/extension-skills.md` (the parenthetical "see …" in the Extension skill check block) and `~/.claude/crafter/rules/task-lifecycle.md` (the "create the task file per …" sentence). Both become `{CRAFTER_HOME}/rules/do/extension-skills.md` and `{CRAFTER_HOME}/rules/task-lifecycle.md` respectively. The bare `rules/do-workflow.md → ### Extension-skill supplemental-only invariant` reference is NOT a concrete `~/.claude/...` runtime install path and stays unchanged (same treatment as Slice 2's bare `task-lifecycle.md` mention).
  - `rules/do/step-2-discuss.md` — Step 2's body contains the concrete runtime path `~/.claude/crafter/rules/task-lifecycle.md` (the "create the task file per …" sentence). It becomes `{CRAFTER_HOME}/rules/task-lifecycle.md`. The bare `crafter-analyzer` agent name is not a runtime path and stays unchanged.
  - SKILL.md loader-list and pointer-stub lines stay `~/.claude/...` (installer-resolved carve-out) — identical to Slice 1/2.
  The Implementer must re-enumerate the exact `~/.claude/...` occurrences in each extracted block at extraction time and normalize precisely those, leaving bare module/agent mentions untouched.
- **Installer change IS required (×2), per Slice 2 R2 (confirmed, not "likely none").** `install.sh` enumerates each `rules/do/*.md` file explicitly (lines 321–324) and `tests/test_install.sh` enumerates each expected file (lines 295–298). Two new modules therefore need exactly two mechanical `cp` lines in `install.sh` and two expected-path entries in `tests/test_install.sh`. `mkdir -p "$rules_dest/do"` already exists from Slice 1 — no directory-creation change.
- **Lower-drift cross-reference strategy (chosen — Interpretation A):** keep both `## Step 1 …` and `## Step 2 …` headings in SKILL.md as thin pointer stubs rather than deleting the headings and rewriting every referrer. This preserves all existing "Step 1"/"Step 2" references (Step 0's pointer stub says the resume procedure runs "before Step 1 begins"; Step 1's own "continue to Step 3" / "delegating to the Analyzer in Step 2"; Step 2's "continue to Step 3"; the Skill options/Step 6b cross-step references are unaffected) with zero edits to other sections — strictly lower drift than renaming/retargeting referrers. Justified in §7.

**Competing interpretations:**

- *Interpretation A (chosen):* thin pointer stubs under the retained `## Step 1 …` and `## Step 2 …` headings; referrers untouched. Same posture as Slice 1 and Slice 2.
- *Interpretation B (rejected, as in Slice 2):* delete the Step 1/Step 2 headings, move all content out, and update every "Step 1"/"Step 2" referrer to point at the modules. Rejected — higher drift, touches sections outside the extraction targets (Step 0 stub, Step 1's own forward routing, Step 2's forward routing), and risks weakening the implicit "Step 1 → Step 2 → Step 3" ordering and the extension-skill supplemental-only invariant binding.

If the user prefers B, Phase 1 changes shape and the cross-reference sweep expands.

### 3. Non-goals

- No other Step extraction (Step 0 is already a module; Steps 3–9b stay inline; this is Slice 3 only).
- No prose rewriting, reordering, splitting, or "while-we're-here" edits. Move-and-link only.
- No changes to extension-skill semantics or the supplemental-only invariant — only the *reference paths* to `extension-skills.md` / `do-workflow.md` are carried into the module (and the concrete `~/.claude/...` ones normalized in Phase 2); the invariant text itself is not edited.
- No edits to `rules/task-lifecycle.md`, `rules/do-workflow.md`, `rules/do/extension-skills.md`, or any agent file — only reference *paths* into them are normalized inside the new modules.
- No changes to `agents/*.md`, CLI/Go code, buffer or task-file schema.
- No edits to any SKILL.md section other than: the Step 1 stub, the Step 2 stub, the loader list, and (cross-reference sweep) any genuinely dangling internal reference — none expected under Interpretation A.
- No repo-wide `~/.claude/...` normalization; only the enumerated runtime paths inside the two new modules (sibling task `20260421-skills-first-runtime-portability.md` owns the broader sweep).
- No edits to installed runtime copies under `~/.claude/...`.
- No reordering or renaming of existing Step numbers or rules files; no switch of the installer to a directory copy.

### 4. Relevant areas

- `skills/crafter-do/SKILL.md` —
  - Extraction target 1: lines ~98–112 (`## Step 1 — Completeness and scope` through "…continue to Step 3. Respect the main/master guard first…"). Note the Extension skill check block at lines ~110 (references `~/.claude/crafter/rules/do/extension-skills.md`, `rules/do-workflow.md → ### Extension-skill supplemental-only invariant`, and "delegating to the Analyzer in Step 2 / building plan context in Step 3"), and the task-file/`task-lifecycle.md` + "continue to Step 3" routing at line ~112.
  - Extraction target 2: lines ~114–120 (`## Step 2 — DISCUSS / RESEARCH (when incomplete or uncertain)` through "…continue to Step 3."). Note the `crafter-analyzer` delegation (line ~118) and the `~/.claude/crafter/rules/task-lifecycle.md` + "continue to Step 3" routing (line ~120).
  - Loader list lines 16–20 (`<!-- do/* capability modules -->` group; add two entries after `step-0-resume.md` at line 20).
  - Inbound references to preserve: Step 0 stub line ~96 ("…before Step 1 begins."); Step 1's internal forward refs to Step 2/Step 3; Step 2's forward ref to Step 3. No Step 6a/Step 6b reference targets Step 1/Step 2 by number (Step 6a references Step 0); confirm in the 1.1 gate and 1.5 sweep.
- `rules/do/step-1-scope.md`, `rules/do/step-2-discuss.md` — new files (do not exist yet).
- `rules/do/flag-validation.md`, `rules/do/project-resolution.md`, `rules/do/extension-skills.md`, `rules/do/step-0-resume.md` — precedent for module structure (single H1, tone, `{CRAFTER_HOME}` usage, pointer-stub wording style).
- `docs/core-capabilities.md` — governing design note: taxonomy rows for Step 1 (`rules/do/step-1-scope.md`) and Step 2 (`rules/do/step-2-discuss.md`), naming scheme, loading convention, runtime-path policy, deferred-slice ordering note (Steps 1–2 = recommended next slice, low cross-reference density), and the Slice 1 + Slice 2 ledger entries to extend.
- `install.sh` lines 320–324 — `rules/do/` deployment block; two `cp` lines to add after the `step-0-resume.md` line.
- `tests/test_install.sh` lines 295–298 — expected `rules/do/` files; two entries to add after the `step-0-resume.md` entry.
- `.crafter/tasks/20260517-refactor-crafter-do-slice-2-step-0-resume.md` — the immediate precedent: two-phase structure, pre-extraction binding gate, R1/R2/R5 risk framing, Interpretation A, installer/test "one mechanical entry per module" handling, Karpathy-contract style, `## Decisions` conventions.

### 5. Vertical phases and steps

Two phases, mirroring Slice 2's exact shape. Phase 1 = the extraction (pre-extraction binding gate + two modules + two pointer stubs + loader/installer/test wiring ×2 + cross-reference sweep) — independently committable and behavior-identical. Phase 2 = runtime-path hygiene scoped to the two new modules + design-note ledger bookkeeping. Each phase ends in a reviewable state with a `crafter-reviewer` gate.

#### Phase 1 — Extract Steps 1–2 into capability modules (move-and-link)

After Phase 1, `## Step 1 — Completeness and scope` and `## Step 2 — DISCUSS / RESEARCH …` in `SKILL.md` are thin pointer stubs under their original headings; the full prose lives verbatim in `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md`; the loader list and installer/test deploy both new modules; all cross-references resolve. Behavior identical across all four flows.

- [x] **1.1 — Validate Step 1/Step 2 bindings survive pointer stubs (pre-extraction binding gate, analysis-only, GO/NO-GO).** — GO; all 5 clusters PRESERVED, verbatim-quote check clean, Interpretation A confirmed. Before moving anything, confirm in writing (in the step's working notes / drift evidence) a GO/NO-GO verdict that each of these bindings is pointer-preservable AND that no other SKILL.md section quotes Step 1/Step 2 prose verbatim (it must reference them by name/number, not duplicate the text): (i) Step 1's extension-skill supplemental-only cross-references (`extension-skills.md` discovery reference and `do-workflow.md → ### Extension-skill supplemental-only invariant`, plus "pass as supplemental context to the Analyzer in Step 2 / plan context in Step 3"); (ii) Step 1 → Step 2 / Step 3 routing ("If the request is complete enough to plan … continue to Step 3" and the implicit "otherwise go to Step 2"); (iii) Step 2 → Step 3 routing ("Once it is complete … continue to Step 3"); (iv) Step 0's "go to / before Step 1" routing resolving to the retained Step 1 heading (Step 0 stub line ~96); (v) Step 2's `crafter-analyzer` delegation and `task-lifecycle.md` task-file-creation reference. If any binding cannot survive a pointer stub, stop and surface as a risk before extracting (NO-GO).
  - **Karpathy contract:** outcome = a written GO/NO-GO on pointer-preservability of all five binding clusters, each with an explicit preserved/at-risk verdict citing the inbound/forward reference it depends on; scope = analysis only, zero file edits; non-goals = moving prose, editing referrers, drafting stub text; simplicity = a short evidence note, not a redesign; drift = beginning any extraction before the verdict is recorded, or expanding into a referrer rewrite; verification = each of the five clusters has an explicit verdict and a confirmation that no section duplicates Step 1/Step 2 prose verbatim; stop = any binding judged not pointer-preservable, or any verbatim duplication found → halt, record risk, do not extract (Interpretation B territory).

- [x] **1.2 — Create `rules/do/step-1-scope.md` with verbatim Step 1 prose.** Move the full `## Step 1 — Completeness and scope` body (from "**If the effective request contains a clear, actionable request**…" through "…Respect the main/master guard first — fresh task files should normally capture the approved topic branch, not `main/master`.") into the new module. Single `# Title` H1, no carried-over `## Step 1 …` H2 (same convention as the Slice 1/2 modules). Prose otherwise byte-identical — the `{CRAFTER_HOME}` substitution is deferred to Phase 2, so this step copies the `~/.claude/crafter/...` references exactly as-is.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 1 prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing paths (deferred to Phase 2), touching SKILL.md yet, creating the Step 2 module yet; simplicity = pure move + H2→H1; drift = any non-mechanical change to the completeness criteria, scope classification, extension-skill check, or routing wording; verification = a literal diff of the module body against the original SKILL.md Step 1 block shows only the H2→H1 line change and no other differences; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.3 — Create `rules/do/step-2-discuss.md` with verbatim Step 2 prose.** Move the full `## Step 2 — DISCUSS / RESEARCH (when incomplete or uncertain)` body (from "If the completeness check finds gaps…" through "…create the task file per … and continue to Step 3.") into the new module. Single `# Title` H1, no carried-over `## Step 2 …` H2. Prose otherwise byte-identical — `{CRAFTER_HOME}` substitution deferred to Phase 2.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 2 prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing paths (deferred to Phase 2), touching SKILL.md yet, altering the Step 1 module; simplicity = pure move + H2→H1; drift = any non-mechanical change to the clarifying-questions guidance, `crafter-analyzer` delegation, or the "do not proceed until complete / continue to Step 3" routing; verification = a literal diff of the module body against the original SKILL.md Step 2 block shows only the H2→H1 line change and no other differences; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.4 — Replace both SKILL.md sections with binding-preserving pointer stubs.** Keep both headings exactly (`## Step 1 — Completeness and scope`, `## Step 2 — DISCUSS / RESEARCH (when incomplete or uncertain)`). Replace each body with a short pointer (Slice 1/2 R1 pattern): the Step 1 stub points to `~/.claude/crafter/rules/do/step-1-scope.md` and explicitly states the procedure performs the completeness check and scope classification, applies the extension-skill supplemental-only check, and routes to Step 2 (if gaps) or Step 3 (if complete, creating the task file); the Step 2 stub points to `~/.claude/crafter/rules/do/step-2-discuss.md` and explicitly states it handles clarification / Analyzer delegation and continues to Step 3 once complete. Pointer paths stay `~/.claude/...` (installer-resolved carve-out). No other section touched.
  - **Karpathy contract:** outcome = both Step 1 and Step 2 sections are thin stubs under their original headings, each preserving all bindings from 1.1; scope = only the Step 1 and Step 2 sections of SKILL.md; non-goals = touching any other section, changing the heading text, rewording referrers, merging the two stubs; simplicity = two 2–4 line pointers mirroring the Slice 1/2 preamble/Step 0 pointers; drift = dropping the routing/extension-skill binding language, altering either heading, editing adjacent sections; verification = both headings unchanged; each stub explicitly names its preserved bindings (Step 1: completeness + scope + extension-skill supplemental check + route to Step 2/Step 3 + task-file creation; Step 2: clarify/Analyzer + continue to Step 3); no other SKILL.md section diffs; stop = if a pointer cannot carry a binding without rewording a referrer, stop and surface (Interpretation B territory).

- [x] **1.5 — Wire the loader list, installer, and install test (×2), and run the cross-reference sweep.** Add `~/.claude/crafter/rules/do/step-1-scope.md` and `~/.claude/crafter/rules/do/step-2-discuss.md` to the SKILL.md loader list under the existing `<!-- do/* capability modules -->` group, after `step-0-resume.md` (order: step-1-scope then step-2-discuss, consistent with Step number ordering and prior slices). Add two `cp` lines in `install.sh`'s `rules/do/` block after the `step-0-resume.md` line; add two expected-path entries (`crafter/rules/do/step-1-scope.md`, `crafter/rules/do/step-2-discuss.md`) to `tests/test_install.sh` after the `step-0-resume.md` entry. Run `tests/test_install.sh`. Then read the post-extraction `SKILL.md` end-to-end and confirm every inbound "Step 1"/"Step 2" reference still resolves to the retained headings and every forward reference from the now-stubbed sections / new modules (to `extension-skills.md`, `do-workflow.md → invariant`, `task-lifecycle.md`, `crafter-analyzer`, Steps 2/3) still resolves. No content edits beyond 1.4 — under Interpretation A no referrer should need changing.
  - **Karpathy contract:** outcome = both new modules are loaded by SKILL.md, deployed+tested by the installer, and confirmed to leave zero dangling internal references; scope = loader list (two lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), plus read-only verification; non-goals = switching the installer to a directory copy, reordering existing entries, renaming files, "improving" referrers that already resolve, expanding the stubs; simplicity = exactly six one-line additions (2 + 2 + 2), each mirroring the adjacent `step-0-resume.md` entry; drift = restructuring the loader list or installer enumeration, normalizing other entries, patching cosmetic referrers; verification = both loader entries sit in the `do/*` group after `step-0-resume.md`; `tests/test_install.sh` passes; a fresh end-to-end read produces no broken "Step 1"/"Step 2" or forward references; stop = if the test framework cannot absorb two added files without broader changes (R4), or a real semantic gap (not cosmetic) surfaces, stop and re-plan rather than silently patch.

- [x] **Phase 1 verification.** — crafter-verifier 7/7 PASS; test 45/0, new files confirmed exercised. `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md` each contain the full corresponding Step prose verbatim (only H2→H1 differs); SKILL.md's `## Step 1 …` and `## Step 2 …` headings are retained with binding-preserving pointer stubs; loader list includes both new modules in the `do/*` group after `step-0-resume.md`; `install.sh` + `tests/test_install.sh` updated (two entries each) and the test passes; all inbound/forward Step 1/Step 2 references resolve; no SKILL.md section other than the two stubs and the loader list changed; semantic behavior identical across default / `--fast` / `--auto` / resume.
- [x] **Phase 1 review.** — crafter-reviewer: 1 Suggestion (fixed per user request); scorecard all PASS. `crafter-reviewer` pass; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven. Reviewer must literally diff each moved block against its original and confirm move-and-link discipline (no rewording, only H2→H1).

#### Phase 2 — Runtime-path hygiene + bookkeeping

After Phase 2 the two new modules follow the `{CRAFTER_HOME}` runtime-path policy and the design-note ledger reflects what shipped. Narrow, proof-of-policy scope — no repo-wide sweep.

- [ ] **2.1 — Apply the `{CRAFTER_HOME}` policy to both new modules.** In `rules/do/step-1-scope.md`, replace each concrete runtime path `~/.claude/crafter/rules/do/extension-skills.md` → `{CRAFTER_HOME}/rules/do/extension-skills.md` and `~/.claude/crafter/rules/task-lifecycle.md` → `{CRAFTER_HOME}/rules/task-lifecycle.md`. In `rules/do/step-2-discuss.md`, replace `~/.claude/crafter/rules/task-lifecycle.md` → `{CRAFTER_HOME}/rules/task-lifecycle.md`. The bare `rules/do-workflow.md → ### Extension-skill supplemental-only invariant` reference and the bare `crafter-analyzer` agent name are not concrete `~/.claude/...` runtime install paths and stay unchanged. Re-enumerate the exact occurrences at edit time to ensure every `~/.claude/...` in each module is normalized and nothing else. No other files touched; SKILL.md pointer/loader lines keep `~/.claude/...` (installer-resolved carve-out).
  - **Karpathy contract:** outcome = both new modules have zero hard-coded `~/.claude/...` references and the correct `{CRAFTER_HOME}/...` forms; scope = only `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md`; non-goals = repo-wide normalization, editing SKILL.md, editing `rules/task-lifecycle.md` / `rules/do-workflow.md` / `rules/do/extension-skills.md`, adding installer path logic, adding a footnote (Slice 1/2 needed none for analogous substitutions); simplicity = exactly the enumerated string substitutions and no more; drift = normalizing other files, introducing new placeholders, rewording surrounding prose, normalizing bare module/agent mentions; verification = each module contains zero `~/.claude/...` references; `step-1-scope.md` has exactly the two `{CRAFTER_HOME}/...` forms and `step-2-discuss.md` exactly one; bare `do-workflow.md`/`crafter-analyzer` mentions unchanged; stop = if applying the policy would require installer changes, stop (scope escalation).

- [ ] **2.2 — Update the design-note ledger and confirm sibling delineation.** In `docs/core-capabilities.md`, append a Slice 3 entry to the "Applied in this task" / runtime-path ledger (after the existing Slice 2 entry) recording that Slice 3 created `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md` and applied the `{CRAFTER_HOME}` policy to their runtime references (Step 1: `extension-skills.md` + `task-lifecycle.md`; Step 2: `task-lifecycle.md`), restating that the bare `do-workflow.md`/`crafter-analyzer` mentions are not runtime paths and that repo-wide normalization remains owned by `20260421-skills-first-runtime-portability.md`. Keep it to a few lines, consistent in tone and structure with the Slice 1/2 entries; do not restructure the doc or re-tag taxonomy rows.
  - **Karpathy contract:** outcome = the design note accurately reflects Slice 3's shipped scope, consistent with the Slice 1/2 ledger entries; scope = a few lines in `docs/core-capabilities.md` (the runtime-path "Applied in this task" area); non-goals = restructuring the taxonomy, restating the sibling task's plan, claiming repo-wide normalization, re-tagging other taxonomy rows, editing the Slice 1/2 entries beyond appending; simplicity = ≤ ~5 added lines; drift = expanding into a meta runtime-portability narrative or contradicting prior ledger entries; verification = the ledger names Slice 3, both new modules, the exact substitutions, and the sibling-task owner, and reads consistently with the Slice 2 entry; stop = if the edit would contradict the existing Slice 1/2 ledger entries, stop and reconcile explicitly.

- [ ] **Phase 2 verification.** `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md` have zero hard-coded `~/.claude/...` references and exactly the enumerated `{CRAFTER_HOME}/...` forms; bare `do-workflow.md`/`crafter-analyzer` mentions unchanged; `docs/core-capabilities.md` ledger reflects Slice 3 consistently with Slice 1/2; no files outside this phase's set touched; behavior still byte-identical.
- [ ] **Phase 2 review.** `crafter-reviewer` pass; Karpathy scorecard PASS on Surgical Changes (no scope creep into untouched files) and Goal-Driven (policy applied exactly to the enumerated paths).

#### Post-task housekeeping

- [ ] **STATE.md and skillbook update** per `rules/post-change.md` — consolidated end-of-task commit.
- [ ] **Task file completion** per `rules/task-lifecycle.md` (Status → completed, `## Outcome` filled, steps checked).
- [ ] **Follow-up note** — record next deferred slice (Steps 3–4) in `## Outcome`.

### 6. Karpathy Contract — overall

- **Outcome:** Steps 1 and 2 extracted into `rules/do/step-1-scope.md` and `rules/do/step-2-discuss.md` with byte-identical behavior, binding-preserving pointer stubs under the retained headings, loader/installer/test wiring (×2), and the `{CRAFTER_HOME}` policy applied to the modules' enumerated runtime paths.
- **Scope boundary:** source files only — `rules/do/step-1-scope.md` (new), `rules/do/step-2-discuss.md` (new), `skills/crafter-do/SKILL.md` (two stubs + two loader lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), `docs/core-capabilities.md` (ledger lines). No agent edits, no CLI/Go, no installed-runtime edits, no schema changes, no edits to `rules/task-lifecycle.md` / `rules/do-workflow.md` / `rules/do/extension-skills.md`.
- **Non-goals:** other Step extractions, prose rewriting, reordering, referrer rewrites, repo-wide path normalization, extension-skill semantic changes, installer style change to directory copy.
- **Simplicity constraint:** two new modules, two stub replacements, two loader lines, two cp lines, two test entries, the enumerated `{CRAFTER_HOME}` substitutions (3 total: 2 in step-1, 1 in step-2), ≤ ~5 ledger lines. Move-and-link discipline only.
- **Drift criteria:** any reword/reorder/split of Step 1 or Step 2 prose; dropping a routing or extension-skill binding from a stub; altering either heading; editing any other SKILL.md section or any referrer that already resolves; normalizing `~/.claude/...` outside the two new modules or normalizing bare module/agent mentions; switching the installer to a directory copy; editing `task-lifecycle.md` / `do-workflow.md` / `extension-skills.md` / agents / CLI / schema.
- **Verification evidence:** literal diff of each moved block vs. original shows only H2→H1 (Phase 1) plus the enumerated `{CRAFTER_HOME}` substitutions (Phase 2); all four flows read identically; all inbound/forward Step 1/Step 2 references resolve; `tests/test_install.sh` passes; both modules have zero `~/.claude/...` refs after Phase 2; reviewer Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.
- **Stop conditions:** stop and re-plan if (a) any Step 1/Step 2 binding cannot be pointer-preserved or any verbatim duplication is found (1.1 NO-GO), (b) extraction forces editing a section outside the two stubs non-mechanically, (c) the install test cannot absorb two added files without broader change (R4), (d) the `{CRAFTER_HOME}` policy needs installer changes, or (e) the user prefers Interpretation B.

### 7. Alternatives considered

- **Delete the Step 1/Step 2 headings and retarget all referrers (Interpretation B)** — rejected. Higher drift: touches the Step 0 stub and Step 1/Step 2's own forward routing (sections outside the extraction targets), and risks weakening the "Step 1 → Step 2 → Step 3" ordering contract and the extension-skill supplemental-only invariant binding. Keeping both headings as thin stubs preserves every existing "Step 1"/"Step 2" reference with zero referrer edits — strictly lower drift, exactly the conservative posture Slices 1 and 2 used.
- **Extract Steps 1 and 2 in separate slices** — rejected. The design note explicitly recommends Steps 1–2 as a single "natural pair, low cross-reference density" slice; Step 1's completeness check feeds Step 2's discuss/research path and both share the "create task file, continue to Step 3" exit, so a combined slice keeps that coupling reviewable in one diff with no added drift.
- **Defer the `{CRAFTER_HOME}` substitution out of this task** — rejected. Slices 1 and 2 established the policy applies to new module files in the same task that creates them; deferring would leave the modules non-compliant and contradict the design note.
- **Switch `install.sh` to a `cp -R rules/do/` directory copy** — rejected for this slice. The installer's explicit per-file enumeration is the established pattern (Slices 1/2 kept it); two added `cp` lines are lower-drift than changing installer style, and `tests/test_install.sh` enumerates files explicitly too, so a directory copy would not reduce churn.
- **Skip the pre-extraction binding gate (1.1)** — rejected. Even though Steps 1–2 are "low cross-reference density," Step 1 carries the extension-skill supplemental-only cross-references and the Step 1 → Step 2/Step 3 routing; validating these survive a pointer before extracting is the precise risk-control Slices 1 and 2 used and the design note's deferred-slice discipline.

### 8. Risks / unknowns / flags

- **R1. Binding preservation (Slice 1/2 R1 analog).** Step 1 carries the extension-skill supplemental-only cross-references and the "complete → Step 3 / gaps → Step 2" routing; Step 2 carries the `crafter-analyzer` delegation and "continue to Step 3" routing. Prose-by-location currently enforces the ordering. Mitigation: the 1.1 gate gates the extraction, and the 1.4 stubs explicitly restate each binding. Flag for user: if a reviewer judges a pointer weakens the extension-skill invariant or the Step ordering, the affected procedure may need to stay partly inline (escalate to Interpretation B or a hybrid).
- **R2. Installer change is required ×2 (confirmed, not "likely none").** `install.sh` and `tests/test_install.sh` both enumerate `rules/do/*` files explicitly (lines 321–324 / 295–298), so two cp lines + two test entries are mandatory. Low risk, fully mechanical, but real edits — not zero.
- **R3. Inbound "Step 1"/"Step 2" references.** The Step 0 stub ("…before Step 1 begins."), Step 1's own forward routing, and Step 2's forward routing depend on the headings existing. Mitigation: Interpretation A retains both headings; the 1.5 sweep verifies. If the user picks B, these become required edits.
- **R4. Install-test fragility.** `tests/test_install.sh` uses an explicit expected-file list; adding two entries is straightforward, but if the test has stricter ordering/exact-count assertions the entries may need careful placement. Mitigation: run the test in 1.5 and adjust placement only (no logic change); escalate if logic changes are needed.
- **R5. Move-and-link reflex drift.** "Pure move" is easy to violate with a one-word tidy in the completeness/scope or clarifying-questions prose, or by accidentally normalizing the bare `do-workflow.md`/`crafter-analyzer` mentions. Mitigation: reviewer must literally diff each moved block vs. original and confirm only H2→H1 (Phase 1) and exactly the enumerated `{CRAFTER_HOME}` substitutions (Phase 2) changed.

---

This contract protects the byte-identical behavior of `crafter-do`'s completeness/scope and discuss/research steps — including the extension-skill supplemental-only invariant binding and the Step 1 → Step 2 → Step 3 routing — while extracting Steps 1 and 2 into named modules exactly as Slices 1 and 2 extracted the preamble and resume detection. The approach is right because it reuses the proven, lowest-drift move-and-link pattern, gates the extraction on an explicit binding-preservation check, keeps both headings as stubs so no referrer changes, extracts the natural pair together as the design note recommends, and handles the confirmed ×2 installer/test wiring mechanically.

## Decisions
- **Decision (User Accepted):** Phase 1 review Suggestion #1 — user requested the fix. Removed the redundant secondary path citations (` per rules/do-workflow.md → ### Extension-skill supplemental-only invariant` from the Step 1 stub; ` per rules/task-lifecycle.md` from the Step 2 stub) so both new stubs match the established Step 0 stub convention (point to the module via `~/.claude/...`, state bindings in plain prose, no secondary path re-citation). All bindings retained; module pointers and headings unchanged; the full references still live in the modules. Local, beneficial, behavior-preserving consistency fix.

## Outcome
<!-- Filled on completion: what was actually done, commit SHA(s), any deviations from plan -->
