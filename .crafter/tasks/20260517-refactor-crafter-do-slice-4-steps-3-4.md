# Task: Refactor crafter-do Slice 4 — extract Steps 3–4 (Plan + Execute) into core capability modules

## Metadata
- **Date:** 2026-05-17
- **Work branch:** refactor/crafter-do-slice-4-steps-3-4
- **Status:** active
- **Scope:** Medium

## Request
Slice 4 of the crafter-do core-capability decomposition. Extract the `## Step 3 — PLAN` and `## Step 4 — EXECUTE` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules under `rules/do/` — `rules/do/step-3-plan.md` and `rules/do/step-4-execute.md` — per the taxonomy in `docs/core-capabilities.md`, following exactly the move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slices 1–3 (`.crafter/tasks/20260517-refactor-crafter-do-core-capabilities.md`, `.crafter/tasks/20260517-refactor-crafter-do-slice-2-step-0-resume.md`, `.crafter/tasks/20260517-refactor-crafter-do-slice-3-steps-1-2.md`).

Constraints:
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume).
- Pre-extraction binding gate (same as Slice 2/3 step 1.1): validate that Step 3's `crafter-planner` delegation, the plan-approval gate, the `**Plan status:** draft → approved` orchestrator-side Edit instruction, and the "If the approved plan contains phases … execute one step at a time" routing; and Step 4's extension-skill supplemental-only check, `crafter-implementer` delegation, and "After each step, run Step 5 / Step 5a / Step 6" routing — all survive the pointer stubs. Confirm no other section quotes Step 3/Step 4 prose verbatim. Explicitly include the inbound references: Step 0's plan-status routing ("draft → go to Step 3", "approved → go to Step 4"), the Step 6f review fix-loop ("Spawn the `crafter-implementer` agent" / re-run flow), and Step 6a ("go back to Step 4 (EXECUTE)").
- Pure move-and-link only: no prose rewriting, no reordering. Retain the `## Step 3 — PLAN` and `## Step 4 — EXECUTE` headings as binding-preserving pointer stubs matching the Step 0/1/2 stub convention (Interpretation A, lowest drift). No other sections touched.
- Step 3 contains the only orchestrator-side Edit instruction (change `**Plan status:** draft` to `approved` after user approval) — the stub MUST preserve this binding explicitly.
- Installer + `tests/test_install.sh` need one mechanical entry per new module (per Slice 2 R2). Two new modules in this slice.
- Out of scope: any other Step extraction, extension-skill semantic changes, agent edits, CLI/Go changes, repo-wide path normalization.

## Plan

**Plan status:** approved

### 1. Complete request

Extract the `## Step 3 — PLAN` and `## Step 4 — EXECUTE` sections of `skills/crafter-do/SKILL.md` into two new internal core capability modules, `rules/do/step-3-plan.md` and `rules/do/step-4-execute.md`, applying the exact move-and-link pattern, taxonomy, naming scheme, loading convention, and `{CRAFTER_HOME}` runtime-path policy established in `docs/core-capabilities.md` and proven in Slice 1 (`20260517-refactor-crafter-do-core-capabilities.md`), Slice 2 (`20260517-refactor-crafter-do-slice-2-step-0-resume.md`), and Slice 3 (`20260517-refactor-crafter-do-slice-3-steps-1-2.md`). This is Slice 4 of the deferred follow-up sequence; the design note flags Steps 3–4 as the recommended next slice — "medium density; test the pattern on agent-delegation prose."

Both section bodies move verbatim into their respective modules. Each `## Step …` heading in `SKILL.md` is reduced to a thin pointer stub that (a) keeps the exact heading text so every existing "Step 3"/"Step 4" reference elsewhere in the file (and in the already-extracted Step 0 module) still resolves, and (b) preserves the binding contracts the prose carried. The single highest-value binding is in Step 3: the orchestrator-side Edit instruction — "Once the user approves, use the Edit tool directly to change `**Plan status:** draft` to `**Plan status:** approved` in the task file's `## Plan` section." This is the ONLY orchestrator-side Edit instruction in the entire file and the mechanism that gates execution; its preservation is non-negotiable (R1). The loader list gains two entries under the existing `<!-- do/* capability modules -->` group, after `step-2-discuss.md`.

Why this matters: `crafter-do/SKILL.md` is the entry point every Crafter workflow loads. Step 3 owns plan delegation to `crafter-planner`, the structured-summary contract presented to the user, the plan-approval gate, the `draft → approved` status flip, and the phase-routing into Step 4. Step 4 owns the extension-skill supplemental-only check, `crafter-implementer` delegation, and the per-step / per-phase routing into Steps 5/5a/6. Pulling them into named modules continues shrinking the monolith and lets plan/execute semantics evolve in focused files, exactly as Slices 1–3 did for the preamble, resume detection, and scope/discuss. Steps 3 and 4 are a natural pair: Step 3's approval gate flows directly into Step 4's execution loop, so extracting them together keeps that coupling reviewable in one slice — and tests the pattern on agent-delegation prose, as the design note intends.

**Acceptance criteria:**

- Two new modules exist:
  - `rules/do/step-3-plan.md` containing the full `## Step 3 — PLAN` prose verbatim (single `# Title` H1, no carried-over `## Step 3` H2 — same convention as the Slice 1/2/3 modules). Step 3's body contains NO concrete `~/.claude/crafter/...` runtime path (it references ARCHITECTURE.md via `{PROJECT_PATH}/{CRAFTER_DIR}/`, which is a project-relative placeholder, not a runtime install path, and stays unchanged); the Implementer must re-confirm this at extraction time.
  - `rules/do/step-4-execute.md` containing the full `## Step 4 — EXECUTE` prose verbatim (single `# Title` H1, no carried-over `## Step 4` H2), with its one concrete runtime path `~/.claude/crafter/rules/do/extension-skills.md` normalized to `{CRAFTER_HOME}/rules/do/extension-skills.md` (substitution applied in Phase 2; see §5 Phase 2). The bare `rules/do-workflow.md → ### Extension-skill supplemental-only invariant` reference and the bare `crafter-implementer` / `Step 5` / `Step 5a` / `Step 6` mentions are not runtime paths and stay unchanged.
- `skills/crafter-do/SKILL.md` keeps both headings (`## Step 3 — PLAN` and `## Step 4 — EXECUTE`) followed by short pointer stubs that match the established Step 0/1/2 stub convention (module pointer via `~/.claude/...` + bindings stated in plain prose, NO secondary path re-citation — per the Slice 3 `## Decisions` ruling). The Step 3 stub MUST explicitly preserve: `crafter-planner` delegation, the structured-summary presentation, the plan-approval gate ("wait for explicit user approval"), the orchestrator-side Edit instruction (after approval, the orchestrator itself changes `**Plan status:** draft` to `**Plan status:** approved` in the task file), and the "if the approved plan contains phases, execute one step at a time" routing into Step 4. The Step 4 stub MUST explicitly preserve: the extension-skill supplemental-only check before delegation, `crafter-implementer` delegation, and the "after each step run Step 5; after all steps in a phase pass, run Step 5a and Step 6" routing.
- The loader list in `SKILL.md` includes `~/.claude/crafter/rules/do/step-3-plan.md` and `~/.claude/crafter/rules/do/step-4-execute.md` under the existing `<!-- do/* capability modules -->` group, after `step-2-discuss.md` (loader-list/pointer paths intentionally stay `~/.claude/...`, same carve-out as Slices 1–3).
- `install.sh` deploys both new modules (one `cp` line each after the `step-2-discuss.md` line at install.sh:326; `mkdir -p "$rules_dest/do"` already exists) and `tests/test_install.sh` expects both (one entry each after the `step-2-discuss.md` entry at tests/test_install.sh:300); `tests/test_install.sh` passes.
- Behavior byte-identical across all four flows (default, `--fast`, `--auto`, resume). Every inbound reference still resolves to the retained `## Step 3` / `## Step 4` headings: Step 0 module's plan-status routing ("`**Plan status:** draft` — go to Step 3", "`**Plan status:** approved` — go to Step 4"), the Step 6 fix loop sub-step (f) ("Spawn the `crafter-implementer` agent" / re-run flow), Step 6a ("go back to **Step 4 (EXECUTE)** for the next plan step"), Step 1's "continue to Step 3", Step 2's "continue to Step 3", and Step 5's "return to Step 3 with the new discovery" (the `replan` action). Every forward reference from the extracted prose (to `crafter-planner`, `crafter-implementer`, `extension-skills.md`, `do-workflow.md → invariant`, `{PROJECT_PATH}/{CRAFTER_DIR}/ARCHITECTURE.md`, Steps 4/5/5a/6) still resolves.
- Two independently committable phases, each with a per-phase verification gate and a `crafter-reviewer` gate; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.

**Validation strategy:** Editorial + installer test. Re-reading the post-extraction `SKILL.md` for the four flows produces identical semantics; a literal diff of each moved block against the original shows only the mechanical H2→H1 normalization (Phase 1) and the one `{CRAFTER_HOME}` substitution in step-4 (Phase 2); `tests/test_install.sh` passes after the installer + test edits.

### 2. Assumptions / interpretations

- **Module paths are fixed by the design note:** `rules/do/step-3-plan.md` and `rules/do/step-4-execute.md` (taxonomy table rows for Step 3 and Step 4). The `rules/do/` subdirectory was confirmed in Slice 1 and reused in Slices 2/3; this slice follows it without re-deciding.
- **Module structure mirrors the Slice 1/2/3 modules:** single `# Title` H1 (e.g. `# Step 3 — PLAN` / `# Step 4 — EXECUTE`, or a semantically equivalent H1), no carried-over H2, prose otherwise verbatim. Exact H1 wording is the Implementer's choice within the move-and-link contract; it must not change meaning.
- **`{CRAFTER_HOME}` enumeration (re-confirm at extraction time):**
  - `rules/do/step-3-plan.md` — Step 3's body contains NO concrete `~/.claude/crafter/...` runtime path. Its only path-like reference is `{PROJECT_PATH}/{CRAFTER_DIR}/ARCHITECTURE.md`, a project-relative placeholder (not a `~/.claude/...` runtime install path), which stays unchanged — same treatment as the bare-mention carve-outs in Slices 2/3. Therefore Phase 2 applies ZERO substitutions to this module. The Implementer must re-scan the extracted block to confirm no `~/.claude/...` occurrence exists.
  - `rules/do/step-4-execute.md` — Step 4's body contains exactly one concrete runtime path: `~/.claude/crafter/rules/do/extension-skills.md` (in the "Extension skill check (supplemental only)" block). It becomes `{CRAFTER_HOME}/rules/do/extension-skills.md`. The bare `rules/do-workflow.md → ### Extension-skill supplemental-only invariant` reference and bare `crafter-implementer` / Step-number mentions are not runtime install paths and stay unchanged (same treatment as Slice 3's bare `do-workflow.md`/`crafter-analyzer` mentions).
  - SKILL.md loader-list and pointer-stub lines stay `~/.claude/...` (installer-resolved carve-out) — identical to Slices 1–3.
  The Implementer must re-enumerate the exact `~/.claude/...` occurrences in each extracted block at extraction time and normalize precisely those (one total, in step-4), leaving bare module/agent/Step mentions and the project-relative `{PROJECT_PATH}/...` reference untouched.
- **Installer change IS required (×2), per Slice 2 R2 (confirmed, not "likely none").** `install.sh` enumerates each `rules/do/*.md` file explicitly (the `step-2-discuss.md` line is install.sh:326) and `tests/test_install.sh` enumerates each expected file (the `step-2-discuss.md` entry is tests/test_install.sh:300). Two new modules therefore need exactly two mechanical `cp` lines in `install.sh` and two expected-path entries in `tests/test_install.sh`. `mkdir -p "$rules_dest/do"` already exists — no directory-creation change.
- **Lower-drift cross-reference strategy (chosen — Interpretation A):** keep both `## Step 3 — PLAN` and `## Step 4 — EXECUTE` headings in SKILL.md as thin pointer stubs rather than deleting the headings and rewriting every referrer. This preserves all existing "Step 3"/"Step 4" references (Step 0 module's plan-status routing, Step 6 fix loop, Step 6a, Step 1, Step 2, Step 5 replan) with zero edits to other sections — strictly lower drift than renaming/retargeting referrers, exactly the conservative posture Slices 1–3 used. Justified in §7.

**Competing interpretations:**

- *Interpretation A (chosen):* thin pointer stubs under the retained `## Step 3 — PLAN` and `## Step 4 — EXECUTE` headings; referrers untouched. Same posture as Slices 1–3.
- *Interpretation B (rejected, as in Slices 2/3):* delete the Step 3/Step 4 headings, move all content out, and update every "Step 3"/"Step 4" referrer (Step 0 module, Step 6 fix loop, Step 6a, Step 1, Step 2, Step 5 replan) to point at the modules. Rejected — higher drift, touches sections and a sibling module outside the extraction targets, and risks weakening the plan-approval gate, the `draft → approved` Edit binding, and the Step 3 → Step 4 → Step 5 ordering. If the user prefers B, Phase 1 changes shape and the cross-reference sweep expands.

### 3. Non-goals

- No other Step extraction (Steps 0–2 are already modules; Steps 5–9b stay inline; this is Slice 4 only).
- No prose rewriting, reordering, splitting, or "while-we're-here" edits. Move-and-link only.
- No changes to the plan-approval gate semantics, the `draft → approved` status-flip mechanic, the structured-summary contract, the extension-skill supplemental-only invariant, or the agent-delegation contracts — only the reference *paths* / pointer wording are carried into the modules (and the one `extension-skills.md` runtime path normalized in Phase 2); the binding text itself is not edited.
- No edits to `rules/task-lifecycle.md`, `rules/do-workflow.md`, `rules/do/extension-skills.md`, `rules/do/step-0-resume.md`, or any agent file (`crafter-planner`, `crafter-implementer`, etc.) — only reference *paths* into them are normalized inside the new modules.
- No changes to `agents/*.md`, CLI/Go code, buffer or task-file schema.
- No edits to any SKILL.md section other than: the Step 3 stub, the Step 4 stub, the loader list, and (cross-reference sweep) any genuinely dangling internal reference — none expected under Interpretation A.
- No repo-wide `~/.claude/...` normalization; only the one enumerated runtime path inside `step-4-execute.md` (sibling task `20260421-skills-first-runtime-portability.md` owns the broader sweep).
- No edits to installed runtime copies under `~/.claude/...`.
- No reordering or renaming of existing Step numbers or rules files; no switch of the installer to a directory copy.

### 4. Relevant areas

- `skills/crafter-do/SKILL.md` —
  - Extraction target 1: lines 108–129 (`## Step 3 — PLAN` through "…execute one step at a time. Phase boundaries determine when phase verification and full review run."). Critical bindings inside: the `crafter-planner` delegation (lines 110–114), the structured-summary requirements (lines 115–122), the plan-approval gate (line 123 "Wait for explicit user approval"), the revision loop (line 125), the orchestrator-side Edit instruction (line 127 — THE highest-value binding), and the phase-routing into Step 4 (line 129). The `{PROJECT_PATH}/{CRAFTER_DIR}/ARCHITECTURE.md` mention (line 113) is a project-relative placeholder, not a runtime path.
  - Extraction target 2: lines 131–142 (`## Step 4 — EXECUTE` through "…run Step 5a (phase verification) and Step 6 (phase review)."). Bindings inside: the "Extension skill check (supplemental only)" block (line 133, references `~/.claude/crafter/rules/do/extension-skills.md` + `rules/do-workflow.md → ### Extension-skill supplemental-only invariant`), the `crafter-implementer` delegation (lines 135–140), and the per-step / per-phase routing into Steps 5/5a/6 (line 142).
  - Loader list lines 16–22 (`<!-- do/* capability modules -->` group; add two entries after the `step-2-discuss.md` line at line 22).
  - Inbound references to preserve: Step 0 module's plan-status routing (now in `rules/do/step-0-resume.md`; resolves to the retained SKILL.md `## Step 3`/`## Step 4` headings by name); Step 6 fix loop sub-step (f) at lines 196–206 ("Spawn the `crafter-implementer` agent", "re-run **Step 6**"); Step 6a at line 280 ("go back to **Step 4 (EXECUTE)**"); Step 1 stub at line 102 ("continues to Step 3"); Step 2 stub at line 106 ("continues to Step 3"); Step 5 at line 160 ("**replan:** return to Step 3 with the new discovery"). Confirm all in the 1.1 gate and the 1.5 sweep.
- `rules/do/step-3-plan.md`, `rules/do/step-4-execute.md` — new files (do not exist yet).
- `rules/do/flag-validation.md`, `rules/do/project-resolution.md`, `rules/do/extension-skills.md`, `rules/do/step-0-resume.md`, `rules/do/step-1-scope.md`, `rules/do/step-2-discuss.md` — precedent for module structure (single H1, tone, `{CRAFTER_HOME}` usage) and the Slice 3 stub convention (pointer + bindings in prose, no secondary path re-citation).
- `docs/core-capabilities.md` — governing design note: taxonomy rows for Step 3 (`rules/do/step-3-plan.md`) and Step 4 (`rules/do/step-4-execute.md`), naming scheme, loading convention, runtime-path policy, deferred-slice ordering note (Steps 3–4 = recommended next slice, "medium density; test the pattern on agent-delegation prose"), and the Slice 1/2/3 ledger entries to extend.
- `install.sh` line 326 (`step-2-discuss.md` cp line) — `rules/do/` deployment block; two `cp` lines to add after it.
- `tests/test_install.sh` line 300 (`step-2-discuss.md` entry) — expected `rules/do/` files; two entries to add after it.
- `.crafter/tasks/20260517-refactor-crafter-do-slice-3-steps-1-2.md` — the immediate precedent: two-phase structure, pre-extraction binding gate, R1–R5 risk framing, Interpretation A vs B, installer/test "one mechanical entry per module" handling, Karpathy-contract style, the `## Decisions` stub-convention ruling.

### 5. Vertical phases and steps

Two phases, mirroring Slice 3's exact shape. Phase 1 = the extraction (pre-extraction binding gate + two modules + two pointer stubs + loader/installer/test wiring ×2 + cross-reference sweep) — independently committable and behavior-identical. Phase 2 = runtime-path hygiene scoped to the new module(s) + design-note ledger bookkeeping. Each phase ends in a reviewable state with a `crafter-reviewer` gate.

#### Phase 1 — Extract Steps 3–4 into capability modules (move-and-link)

After Phase 1, `## Step 3 — PLAN` and `## Step 4 — EXECUTE` in `SKILL.md` are thin pointer stubs under their original headings; the full prose lives verbatim in `rules/do/step-3-plan.md` and `rules/do/step-4-execute.md`; the loader list and installer/test deploy both new modules; all cross-references resolve. Behavior identical across all four flows.

- [x] **1.1 — Validate Step 3/Step 4 bindings survive pointer stubs (pre-extraction binding gate, analysis-only, GO/NO-GO).** — GO; all 6 clusters PRESERVED (cluster iii Edit-instruction preservable with explicit imperative stub wording), verbatim-quote check clean, Interpretation A confirmed. Before moving anything, confirm in writing (in the step's working notes / drift evidence) a GO/NO-GO verdict that each binding cluster is pointer-preservable AND that no other SKILL.md section quotes Step 3/Step 4 prose verbatim (it must reference them by name/number, not duplicate the text): (i) Step 3's `crafter-planner` delegation + structured-summary contract; (ii) Step 3's plan-approval gate ("Wait for explicit user approval") and revision loop; (iii) **Step 3's orchestrator-side Edit instruction** — after approval the orchestrator itself uses the Edit tool to change `**Plan status:** draft` to `**Plan status:** approved` (the single highest-value binding; verify a stub can carry it without weakening it); (iv) Step 3's "if the approved plan contains phases, execute one step at a time" routing into Step 4; (v) Step 4's extension-skill supplemental-only check + `crafter-implementer` delegation + "after each step run Step 5 / after a phase run Step 5a + Step 6" routing; (vi) inbound references that depend on the retained headings — Step 0 module's plan-status routing ("draft → Step 3", "approved → Step 4"), Step 6 fix loop sub-step (f) ("Spawn the `crafter-implementer` agent" / re-run Step 6), Step 6a ("go back to **Step 4 (EXECUTE)**"), Step 1's "continue to Step 3", Step 2's "continue to Step 3", Step 5's `replan` "return to Step 3". If any binding cannot survive a pointer stub, stop and surface as a risk before extracting (NO-GO).
  - **Karpathy contract:** outcome = a written GO/NO-GO on pointer-preservability of all six binding clusters, each with an explicit preserved/at-risk verdict citing the inbound/forward reference it depends on, with cluster (iii) called out as the headline binding; scope = analysis only, zero file edits; non-goals = moving prose, editing referrers, drafting stub text; simplicity = a short evidence note, not a redesign; drift = beginning any extraction before the verdict is recorded, or expanding into a referrer rewrite; verification = each of the six clusters has an explicit verdict, cluster (iii) explicitly addressed, and a confirmation that no section duplicates Step 3/Step 4 prose verbatim; stop = any binding judged not pointer-preservable (especially (iii)), or any verbatim duplication found → halt, record risk, do not extract (Interpretation B territory).

- [x] **1.2 — Create `rules/do/step-3-plan.md` with verbatim Step 3 prose.** Move the full `## Step 3 — PLAN` body (SKILL.md lines 110–129, from "Delegate planning to the **`crafter-planner`** agent:" through "…Phase boundaries determine when phase verification and full review run.") into the new module. Single `# Title` H1, no carried-over `## Step 3` H2 (same convention as the Slice 1/2/3 modules). Prose otherwise byte-identical — Step 3 has no `~/.claude/...` runtime path, so no normalization applies; the `{PROJECT_PATH}/{CRAFTER_DIR}/ARCHITECTURE.md` mention is copied exactly as-is.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 3 prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing the `{PROJECT_PATH}/...` reference, touching SKILL.md yet, creating the Step 4 module yet; simplicity = pure move + H2→H1; drift = any non-mechanical change to the planner-delegation, structured-summary, approval-gate, Edit-instruction, or phase-routing wording; verification = a literal diff of the module body against the original SKILL.md Step 3 block shows only the H2→H1 line change and no other differences; the `**Plan status:** draft`→`approved` Edit sentence is present verbatim; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.3 — Create `rules/do/step-4-execute.md` with verbatim Step 4 prose.** Move the full `## Step 4 — EXECUTE` body (SKILL.md lines 133–142, from "**Extension skill check (supplemental only).**" through "…run Step 5a (phase verification) and Step 6 (phase review).") into the new module. Single `# Title` H1, no carried-over `## Step 4` H2. Prose otherwise byte-identical — the `{CRAFTER_HOME}` substitution for the one `~/.claude/crafter/rules/do/extension-skills.md` path is deferred to Phase 2, so this step copies it exactly as-is.
  - **Karpathy contract:** outcome = one new module file containing the full original Step 4 prose; scope = exactly this file; non-goals = rewording, reordering, splitting, normalizing paths (deferred to Phase 2), touching SKILL.md yet, altering the Step 3 module; simplicity = pure move + H2→H1; drift = any non-mechanical change to the extension-skill supplemental-only block, `crafter-implementer` delegation, or the Step 5/5a/6 routing; verification = a literal diff of the module body against the original SKILL.md Step 4 block shows only the H2→H1 line change and no other differences; stop = if the section cannot be cleanly excised because another section quotes its prose verbatim, stop and re-plan.

- [x] **1.4 — Replace both SKILL.md sections with binding-preserving pointer stubs.** Keep both headings exactly (`## Step 3 — PLAN`, `## Step 4 — EXECUTE`). Replace each body with a short pointer matching the established Step 0/1/2 stub convention (module pointer via `~/.claude/...` + bindings stated in plain prose, NO secondary path re-citation — per the Slice 3 `## Decisions` ruling): the Step 3 stub points to `~/.claude/crafter/rules/do/step-3-plan.md` and explicitly states it delegates planning to `crafter-planner`, presents the structured summary, waits for explicit user approval, **and that once the user approves the orchestrator itself uses the Edit tool to change `**Plan status:** draft` to `**Plan status:** approved` in the task file**, then routes phased plans into one-step-at-a-time execution (Step 4); the Step 4 stub points to `~/.claude/crafter/rules/do/step-4-execute.md` and explicitly states it applies the extension-skill supplemental-only check, delegates implementation to `crafter-implementer` one step at a time, and routes to Step 5 after each step / Step 5a + Step 6 after a phase. Pointer paths stay `~/.claude/...` (installer-resolved carve-out). No other section touched.
  - **Karpathy contract:** outcome = both Step 3 and Step 4 sections are thin stubs under their original headings, each preserving all bindings from 1.1, with the Step 3 stub explicitly carrying the orchestrator-side `draft → approved` Edit instruction; scope = only the Step 3 and Step 4 sections of SKILL.md; non-goals = touching any other section, changing the heading text, rewording referrers, merging the two stubs, re-citing secondary paths; simplicity = two short pointers mirroring the Step 0/1/2 stubs; drift = dropping the Edit instruction or any routing/extension-skill binding language, altering either heading, re-citing secondary paths (Slice 3 convention violation), editing adjacent sections; verification = both headings unchanged; the Step 3 stub explicitly names the `draft → approved` orchestrator Edit binding plus planner/summary/approval/phase-routing; the Step 4 stub explicitly names the extension-skill check + implementer + Step 5/5a/6 routing; no other SKILL.md section diffs; stop = if a pointer cannot carry a binding (especially the Edit instruction) without rewording a referrer, stop and surface (Interpretation B territory).

- [x] **1.5 — Wire the loader list, installer, and install test (×2), and run the cross-reference sweep.** Add `~/.claude/crafter/rules/do/step-3-plan.md` and `~/.claude/crafter/rules/do/step-4-execute.md` to the SKILL.md loader list under the existing `<!-- do/* capability modules -->` group, after the `step-2-discuss.md` line (order: step-3-plan then step-4-execute, consistent with Step ordering and prior slices). Add two `cp` lines in `install.sh`'s `rules/do/` block after install.sh:326 (the `step-2-discuss.md` line); add two expected-path entries (`crafter/rules/do/step-3-plan.md`, `crafter/rules/do/step-4-execute.md`) to `tests/test_install.sh` after line 300 (the `step-2-discuss.md` entry). Run `tests/test_install.sh`. Then read the post-extraction `SKILL.md` end-to-end and explicitly confirm every inbound reference still resolves to the retained `## Step 3`/`## Step 4` headings: the Step 0 module's plan-status routing (`step-0-resume.md` "draft → Step 3" / "approved → Step 4"), Step 6 fix loop sub-step (f) ("Spawn the `crafter-implementer` agent" / re-run Step 6), Step 6a ("go back to **Step 4 (EXECUTE)**"), Step 1's "continue to Step 3", Step 2's "continue to Step 3", Step 5's `replan` "return to Step 3"; and every forward reference from the now-stubbed sections / new modules (to `crafter-planner`, `crafter-implementer`, `extension-skills.md`, `do-workflow.md → invariant`, `{PROJECT_PATH}/{CRAFTER_DIR}/ARCHITECTURE.md`, Steps 4/5/5a/6) still resolves. No content edits beyond 1.4 — under Interpretation A no referrer should need changing.
  - **Karpathy contract:** outcome = both new modules are loaded by SKILL.md, deployed+tested by the installer, and confirmed to leave zero dangling internal references (including the Step 0 module and Step 6 fix-loop / Step 6a / Step 5 replan inbound refs); scope = loader list (two lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), plus read-only verification; non-goals = switching the installer to a directory copy, reordering existing entries, renaming files, "improving" referrers that already resolve, expanding the stubs; simplicity = exactly six one-line additions (2 + 2 + 2), each mirroring the adjacent `step-2-discuss.md` entry; drift = restructuring the loader list or installer enumeration, normalizing other entries, patching cosmetic referrers; verification = both loader entries sit in the `do/*` group after `step-2-discuss.md`; `tests/test_install.sh` passes; a fresh end-to-end read produces no broken Step 3/Step 4 inbound or forward references (the six enumerated inbound refs explicitly re-checked); stop = if the test framework cannot absorb two added files without broader changes (R4), or a real semantic gap (not cosmetic) surfaces, stop and re-plan rather than silently patch.

- [x] **Phase 1 verification.** — crafter-verifier 8/8 PASS; headline Edit binding verbatim; test 45/0; 6 inbound refs resolve. `rules/do/step-3-plan.md` and `rules/do/step-4-execute.md` each contain the full corresponding Step prose verbatim (only H2→H1 differs); SKILL.md's `## Step 3 — PLAN` and `## Step 4 — EXECUTE` headings are retained with binding-preserving pointer stubs matching the Step 0/1/2 convention, the Step 3 stub explicitly carrying the orchestrator-side `draft → approved` Edit instruction; loader list includes both new modules in the `do/*` group after `step-2-discuss.md`; `install.sh` + `tests/test_install.sh` updated (two entries each) and the test passes; all inbound (Step 0 module, Step 6 fix loop, Step 6a, Step 1, Step 2, Step 5 replan) and forward Step 3/Step 4 references resolve; no SKILL.md section other than the two stubs and the loader list changed; semantic behavior identical across default / `--fast` / `--auto` / resume. Delegate to `crafter-verifier`.
- [x] **Phase 1 review.** — crafter-reviewer no findings; scorecard all PASS; R1 Edit binding verbatim. `crafter-reviewer` pass; Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven. Reviewer must literally diff each moved block against its original and confirm move-and-link discipline (no rewording, only H2→H1), and explicitly confirm the Step 3 stub preserves the `draft → approved` orchestrator Edit binding.

#### Phase 2 — Runtime-path hygiene + bookkeeping

After Phase 2 the new module(s) follow the `{CRAFTER_HOME}` runtime-path policy and the design-note ledger reflects what shipped. Narrow, proof-of-policy scope — no repo-wide sweep.

- [ ] **2.1 — Apply the `{CRAFTER_HOME}` policy to the new modules.** In `rules/do/step-4-execute.md`, replace the one concrete runtime path `~/.claude/crafter/rules/do/extension-skills.md` → `{CRAFTER_HOME}/rules/do/extension-skills.md`. In `rules/do/step-3-plan.md`, confirm there is NO `~/.claude/...` occurrence (expected — Step 3 references ARCHITECTURE.md via `{PROJECT_PATH}/{CRAFTER_DIR}/`, a project-relative placeholder that stays unchanged); if any `~/.claude/...` is unexpectedly present, normalize it and flag the deviation. The bare `rules/do-workflow.md → ### Extension-skill supplemental-only invariant` reference, the `{PROJECT_PATH}/{CRAFTER_DIR}/ARCHITECTURE.md` reference, and bare `crafter-planner`/`crafter-implementer`/Step-number mentions are not concrete `~/.claude/...` runtime install paths and stay unchanged. Re-enumerate the exact occurrences at edit time to ensure every `~/.claude/...` in each module is normalized and nothing else. No other files touched; SKILL.md pointer/loader lines keep `~/.claude/...` (installer-resolved carve-out).
  - **Karpathy contract:** outcome = both new modules have zero hard-coded `~/.claude/...` references (step-4 normalized ×1, step-3 confirmed already clean) and the correct `{CRAFTER_HOME}/...` form in step-4; scope = only `rules/do/step-3-plan.md` (read-only confirm) and `rules/do/step-4-execute.md` (one substitution); non-goals = repo-wide normalization, editing SKILL.md, editing `rules/task-lifecycle.md` / `rules/do-workflow.md` / `rules/do/extension-skills.md` / agents, normalizing the `{PROJECT_PATH}/...` reference, adding installer path logic, adding a footnote; simplicity = exactly the one enumerated string substitution and no more; drift = normalizing other files, introducing new placeholders, rewording surrounding prose, normalizing bare module/agent mentions or the project-relative placeholder; verification = `step-4-execute.md` contains zero `~/.claude/...` references and exactly the one `{CRAFTER_HOME}/rules/do/extension-skills.md` form; `step-3-plan.md` contains zero `~/.claude/...` references and its `{PROJECT_PATH}/...` reference is unchanged; bare `do-workflow.md`/agent mentions unchanged; stop = if applying the policy would require installer changes, stop (scope escalation).

- [ ] **2.2 — Update the design-note ledger and confirm sibling delineation.** In `docs/core-capabilities.md`, append a Slice 4 entry to the runtime-path "Applied in this task" ledger (after the existing Slice 3 entry) recording that Slice 4 created `rules/do/step-3-plan.md` and `rules/do/step-4-execute.md` and applied the `{CRAFTER_HOME}` policy to their runtime references (step-3-plan.md: zero — no runtime path, the `{PROJECT_PATH}/{CRAFTER_DIR}/ARCHITECTURE.md` reference is project-relative and was left unchanged; step-4-execute.md: one — `~/.claude/crafter/rules/do/extension-skills.md` → `{CRAFTER_HOME}/rules/do/extension-skills.md`), restating that the bare `do-workflow.md`/`crafter-planner`/`crafter-implementer` mentions are not runtime paths and that repo-wide normalization remains owned by `20260421-skills-first-runtime-portability.md`. Keep it to a few lines, consistent in tone and structure with the Slice 1/2/3 entries; do not restructure the doc or re-tag taxonomy rows.
  - **Karpathy contract:** outcome = the design note accurately reflects Slice 4's shipped scope, consistent with the Slice 1/2/3 ledger entries; scope = a few lines in `docs/core-capabilities.md` (the runtime-path "Applied in this task" area); non-goals = restructuring the taxonomy, restating the sibling task's plan, claiming repo-wide normalization, re-tagging other taxonomy rows, editing the Slice 1/2/3 entries beyond appending; simplicity = ≤ ~5 added lines; drift = expanding into a meta runtime-portability narrative or contradicting prior ledger entries; verification = the ledger names Slice 4, both new modules, the exact substitution count (0 + 1), and the sibling-task owner, and reads consistently with the Slice 3 entry; stop = if the edit would contradict the existing Slice 1/2/3 ledger entries, stop and reconcile explicitly.

- [ ] **Phase 2 verification.** `rules/do/step-4-execute.md` has zero hard-coded `~/.claude/...` references and exactly the one enumerated `{CRAFTER_HOME}/rules/do/extension-skills.md` form; `rules/do/step-3-plan.md` has zero `~/.claude/...` references and its `{PROJECT_PATH}/...` reference unchanged; bare `do-workflow.md`/agent mentions unchanged; `docs/core-capabilities.md` ledger reflects Slice 4 consistently with Slice 1/2/3; no files outside this phase's set touched; behavior still byte-identical. Delegate to `crafter-verifier`.
- [ ] **Phase 2 review.** `crafter-reviewer` pass; Karpathy scorecard PASS on Surgical Changes (no scope creep into untouched files) and Goal-Driven (policy applied exactly to the one enumerated path).

#### Post-task housekeeping

- [ ] **STATE.md and skillbook update** per `rules/post-change.md` — consolidated end-of-task commit.
- [ ] **Task file completion** per `rules/task-lifecycle.md` (Status → completed, `## Outcome` filled, steps checked).
- [ ] **Follow-up note** — record in `## Outcome` that Slice 5 (Steps 5, 5a — Drift + Phase Verification) is the next deferred candidate per `docs/core-capabilities.md`; remaining deferred modules after that: Steps 6/6b/6a (Review + Phase Summary + Session Break), then Steps 7–9/9b (Post-Change + PR Composition).

### 6. Karpathy Contract — overall

- **Outcome:** Steps 3 and 4 extracted into `rules/do/step-3-plan.md` and `rules/do/step-4-execute.md` with byte-identical behavior, binding-preserving pointer stubs under the retained headings (the Step 3 stub explicitly carrying the orchestrator-side `draft → approved` Edit instruction), loader/installer/test wiring (×2), and the `{CRAFTER_HOME}` policy applied to the one enumerated runtime path.
- **Scope boundary:** source files only — `rules/do/step-3-plan.md` (new), `rules/do/step-4-execute.md` (new), `skills/crafter-do/SKILL.md` (two stubs + two loader lines), `install.sh` (two cp lines), `tests/test_install.sh` (two entries), `docs/core-capabilities.md` (ledger lines). No agent edits, no CLI/Go, no installed-runtime edits, no schema changes, no edits to `rules/task-lifecycle.md` / `rules/do-workflow.md` / `rules/do/extension-skills.md` / `rules/do/step-0-resume.md`.
- **Non-goals:** other Step extractions, prose rewriting, reordering, referrer rewrites, repo-wide path normalization, plan-approval/Edit-binding/extension-skill semantic changes, installer style change to directory copy.
- **Simplicity constraint:** two new modules, two stub replacements, two loader lines, two cp lines, two test entries, exactly one `{CRAFTER_HOME}` substitution (in step-4), ≤ ~5 ledger lines. Move-and-link discipline only.
- **Drift criteria:** any reword/reorder/split of Step 3 or Step 4 prose; dropping the `draft → approved` Edit instruction or any routing/extension-skill binding from a stub; re-citing secondary paths in a stub (Slice 3 convention violation); altering either heading; editing any other SKILL.md section or any referrer that already resolves; normalizing `~/.claude/...` outside step-4 or normalizing bare module/agent mentions or the `{PROJECT_PATH}/...` placeholder; switching the installer to a directory copy; editing `task-lifecycle.md` / `do-workflow.md` / `extension-skills.md` / `step-0-resume.md` / agents / CLI / schema.
- **Verification evidence:** literal diff of each moved block vs. original shows only H2→H1 (Phase 1) plus the one `{CRAFTER_HOME}` substitution (Phase 2); all four flows read identically; all inbound (Step 0 module, Step 6 fix loop, Step 6a, Step 1, Step 2, Step 5 replan) and forward Step 3/Step 4 references resolve; the Step 3 stub provably carries the orchestrator-side Edit instruction; `tests/test_install.sh` passes; step-4 has zero `~/.claude/...` refs after Phase 2; reviewer Karpathy scorecard PASS on Simplicity / Surgical Changes / Goal-Driven.
- **Stop conditions:** stop and re-plan if (a) any Step 3/Step 4 binding — especially the orchestrator-side `draft → approved` Edit instruction — cannot be pointer-preserved or any verbatim duplication is found (1.1 NO-GO), (b) extraction forces editing a section outside the two stubs non-mechanically, (c) the install test cannot absorb two added files without broader change (R4), (d) the `{CRAFTER_HOME}` policy needs installer changes, or (e) the user prefers Interpretation B.

### 7. Alternatives considered

- **Delete the Step 3/Step 4 headings and retarget all referrers (Interpretation B)** — rejected. Higher drift: touches the Step 0 module, the Step 6 fix loop, Step 6a, Step 1/Step 2 stubs, and Step 5's replan action (all outside the extraction targets), and risks weakening the plan-approval gate, the highest-value `draft → approved` Edit binding, and the Step 3 → Step 4 → Step 5 ordering. Keeping both headings as thin stubs preserves every existing "Step 3"/"Step 4" reference with zero referrer edits — strictly lower drift, exactly the conservative posture Slices 1–3 used.
- **Extract Steps 3 and 4 in separate slices** — rejected. The design note explicitly recommends Steps 3–4 as a single "medium density; test the pattern on agent-delegation prose" slice; Step 3's approval gate flows directly into Step 4's execution loop, so a combined slice keeps that coupling reviewable in one diff with no added drift.
- **Defer the `{CRAFTER_HOME}` substitution out of this task** — rejected. Slices 1–3 established the policy applies to new module files in the same task that creates them; deferring would leave step-4 non-compliant and contradict the design note.
- **Switch `install.sh` to a `cp -R rules/do/` directory copy** — rejected for this slice. The installer's explicit per-file enumeration is the established pattern (Slices 1–3 kept it); two added `cp` lines are lower-drift than changing installer style, and `tests/test_install.sh` enumerates files explicitly too, so a directory copy would not reduce churn.
- **Skip the pre-extraction binding gate (1.1)** — rejected. Steps 3–4 are "medium density" and Step 3 carries the single most critical binding in the whole file (the orchestrator-side Edit instruction); validating it survives a pointer before extracting is the precise risk-control Slices 1–3 used.

### 8. Risks / unknowns / flags

- **R1. Edit-binding preservation (headline risk).** Step 3 carries the ONLY orchestrator-side Edit instruction in `SKILL.md`: after user approval the orchestrator itself uses the Edit tool to change `**Plan status:** draft` to `**Plan status:** approved` in the task file. This is the mechanism that gates execution and the Step 0 module's plan-status resume routing depends on it. If a pointer stub weakens or loses this binding, the approval gate and resume routing break. Strongest mitigation: 1.1 explicitly gates on cluster (iii); 1.2 verification requires the Edit sentence present verbatim in the module; 1.4 requires the stub to explicitly restate the orchestrator-side Edit instruction; Phase 1 review must explicitly confirm it. Flag for user: if a reviewer judges a pointer cannot safely carry this binding, this procedure may need to stay partly inline (escalate to Interpretation B or a hybrid).
- **R2. Installer change is required ×2 (confirmed, not "likely none").** `install.sh` and `tests/test_install.sh` both enumerate `rules/do/*` files explicitly (the `step-2-discuss.md` lines are install.sh:326 / tests/test_install.sh:300), so two cp lines + two test entries are mandatory. Low risk, fully mechanical, but real edits — not zero.
- **R3. Inbound Step 3/Step 4 references span more sites than prior slices.** The Step 0 module (`step-0-resume.md` — a sibling module, not just SKILL.md), the Step 6 fix loop sub-step (f), Step 6a, Step 1, Step 2, and Step 5's `replan` action all depend on the retained headings/Step numbers. Mitigation: Interpretation A retains both headings; the 1.5 sweep explicitly re-checks all six. If the user picks B, every one of these becomes a required edit (notably across the `step-0-resume.md` module boundary).
- **R4. Install-test fragility.** `tests/test_install.sh` uses an explicit expected-file list; adding two entries is straightforward, but if the test has stricter ordering/exact-count assertions the entries may need careful placement. Mitigation: run the test in 1.5 and adjust placement only (no logic change); escalate if logic changes are needed.
- **R5. Move-and-link reflex drift.** "Pure move" is easy to violate with a one-word tidy in the structured-summary or delegation prose, by accidentally normalizing the bare `do-workflow.md`/agent mentions or the `{PROJECT_PATH}/...` placeholder, or by re-citing a secondary path in a stub (violating the Slice 3 convention). Mitigation: reviewer must literally diff each moved block vs. original and confirm only H2→H1 (Phase 1) and exactly the one enumerated `{CRAFTER_HOME}` substitution (Phase 2) changed, and that stubs cite no secondary paths.

---

This contract protects the byte-identical behavior of `crafter-do`'s plan and execute steps — most critically the orchestrator-side `**Plan status:** draft → approved` Edit instruction, the plan-approval gate, and the Step 3 → Step 4 → Step 5 routing — while extracting Steps 3 and 4 into named modules exactly as Slices 1–3 extracted the preamble, resume detection, and scope/discuss. The approach is right because it reuses the proven, lowest-drift move-and-link pattern, gates the extraction on an explicit binding-preservation check that singles out the highest-value Edit binding, keeps both headings as stubs so no referrer (including the sibling Step 0 module) changes, extracts the natural pair together as the design note recommends, and handles the confirmed ×2 installer/test wiring mechanically.

## Decisions

## Outcome
<!-- Filled on completion: what was actually done, commit SHA(s), any deviations from plan -->
