{
  "version": 1,
  "updatedAt": "2026-06-09T10:19:09Z",
  "skills": [
    {
      "id": "7b91fbef8829ffe0",
      "agent": "implementer",
      "rule": "When implementing approval-path conditions, ensure the conditions cover ALL end-of-flow states explicitly — narrow conditions create coverage gaps that pass surface-level review but break in edge cases (e.g., 'fix loop ran successfully' state).",
      "rationale": "Fix-loop completion state was not explicitly covered in approval conditions, causing an edge-case gap that passed initial review but surfaced during verification of the green-commits task.",
      "sourceTask": "green-commits",
      "confidence": "low",
      "addedAt": "2026-05-05T21:08:41Z",
      "appliedCount": 29,
      "deprecated": false
    },
    {
      "id": "8c045cab9dea3432",
      "agent": "reviewer",
      "rule": "When reviewing changes with a canonical-vs-surface relationship (e.g., rules file as source of truth + skill file as user-facing surface), verify that EVERY item enumerated in the canonical source also appears in the surface enumeration — even when the surface uses a 'see canonical for full list' pointer. Partial enumerations are subtle cross-consistency drift; the pointer alone is not a substitute for the surface having a complete-by-itself summary.",
      "rationale": "Phase 2 review of GH#15 caught two Minor findings (#1 and #2) where the SKILL.md auto-branch checklist and removed-gates inline enumeration each silently omitted the most consequential item from rules/do-workflow.md (auto-fixed Critical/Major recorded as Decisions; Critical/Major findings the loop can clear). Both surfaces had a 'see rules for full list' pointer, so a quick glance suggested completeness — but a reader following only the SKILL.md prose would miss core --auto semantics. The polish iteration tightened cross-consistency promised by the Phase 2 contract.",
      "sourceTask": "20260509-feat-gh-15-auto-flag.md",
      "confidence": "low",
      "addedAt": "2026-05-09T11:14:07Z",
      "appliedCount": 18,
      "deprecated": false
    },
    {
      "id": "7db74d6bcb47cb1a",
      "agent": "implementer",
      "rule": "When a step contract says 'do not modify content X', distinguish text-byte-equivalence from positional-byte-equivalence. Structural repositioning of a block (moving it under a different heading, folding it into a new lead-in) is permitted as long as the actual text is unchanged. If the repositioning is needed to make the new structure coherent, do it and explicitly call it out as 'structural repositioning, text byte-equivalent' in the implementation summary so the verifier can confirm the distinction quickly.",
      "rationale": "Phase 2 Step 3 of GH#15 needed Step 6b restructured to add the --auto branch at the top, which required folding the existing Phase Summary content block into the new non-auto lead-in. The implementer correctly preserved every word of the four bullets and the 'If there were no findings...' paragraph — only the heading and paragraph position changed. The verifier confirmed this was acceptable structural repositioning, not drift on criterion (h). Surfacing the distinction in the summary made the drift check fast.",
      "sourceTask": "20260509-feat-gh-15-auto-flag.md",
      "confidence": "low",
      "addedAt": "2026-05-09T11:14:14Z",
      "appliedCount": 26,
      "deprecated": false
    },
    {
      "id": "6c6bb51cffac08df",
      "agent": "implementer",
      "rule": "When a buffer or log-append subcommand must handle values containing code fences and blank lines, prefer explicit caller-provided required flags over shell heredocs or env vars. The 'explicit required flags' pattern — `--run-dir`, `--task-id`, `--created-by` passed by the caller — avoids implicit environment lookup, makes the call site self-documenting, and moves the quoting responsibility to the Go flag parser rather than the calling shell, which handles multi-line strings and backticks far more reliably.",
      "rationale": "GH#16 crafter-buffer implementation: the decision to require `--run-dir`, `--task-id`, and `--created-by` as explicit flags (rather than auto-resolving from the environment) meant Phase 4 Step 2's awkward-content round-trip (fenced code blocks, triple backticks, blank-line runs) exercised only the Go JSON encoder — not shell quoting. All three entries round-tripped cleanly. The pattern also makes each buffer invocation auditable without knowing the caller's environment.",
      "sourceTask": "20260509-feat-gh-16-buffer-skill",
      "confidence": "low",
      "addedAt": "2026-05-10T00:10:41Z",
      "appliedCount": 25,
      "deprecated": false
    },
    {
      "id": "cbc640051fb016fd",
      "agent": "orchestrator",
      "rule": "When verifying a serialization round-trip for content with awkward characters (fenced code blocks, embedded blank lines, literal triple backticks), construct the verification set to exercise each escape class in a separate entry rather than combining them. Separate entries make the failure mode obvious — if one entry fails but the others pass, the exact escape class that broke is immediately identifiable without re-running a combined test case.",
      "rationale": "GH#16 Phase 4 Step 2: three distinct `crafter buffer uat` invocations targeted one escape class each — fenced code block via ANSI-C quoting, literal triple-backtick inline, embedded blank lines. All three passed. Because they were separate entries in the same NDJSON file, the Python round-trip check could report per-entry pass/fail against the known-good expected value, making the verification evidence unambiguous and independently reproducible.",
      "sourceTask": "20260509-feat-gh-16-buffer-skill",
      "confidence": "low",
      "addedAt": "2026-05-10T00:10:52Z",
      "appliedCount": 0,
      "deprecated": false
    },
    {
      "id": "573d8377e6d7f3b8",
      "agent": "reviewer",
      "rule": "When reusing an indent/format helper, check whether it applies to the whole content or only to continuation lines — applying it to the whole content over-indents the first line when that line is meant to render inline (e.g., after an em-dash separator).",
      "rationale": "GH#17 Phase 2 review found  calling indentBlock(detail) which over-indented the first line by 2 spaces, producing '— ' + 2-space-prefix + content (3 spaces between em-dash and content). Tests didn't catch it because no multi-line-detail test existed. Pattern: indent helpers usually want lines[1:] only, with lines[0] going inline.",
      "sourceTask": "20260510-feat-gh-17-pr-composer",
      "confidence": "low",
      "addedAt": "2026-05-10T02:56:32Z",
      "appliedCount": 17,
      "deprecated": false
    },
    {
      "id": "364ccd163daf6cc0",
      "agent": "implementer",
      "rule": "When using bufio.Scanner to read user-controlled text files (NDJSON buffers, Markdown task files, etc.), explicitly set scanner.Buffer with a sane upper bound (e.g., 1 MB) and wrap bufio.ErrTooLong with file path + limit info — the default 64KB token-size limit is silently too small for many real inputs.",
      "rationale": "GH#17 Phase 2 review found two Major findings (#3, #4) where bufio.Scanner default 64KB cap caused opaque errors on \u003e64KB lines in buffer files and task-file Markdown. Pattern: scanner.Buffer(make([]byte, 0, 64*1024), 1\u003c\u003c20) before the read loop, plus errors.Is(err, bufio.ErrTooLong) wrapping for clarity.",
      "sourceTask": "20260510-feat-gh-17-pr-composer",
      "confidence": "low",
      "addedAt": "2026-05-10T02:56:34Z",
      "appliedCount": 25,
      "deprecated": false
    },
    {
      "id": "c1b846af1762d0c3",
      "agent": "implementer",
      "rule": "When constructing  (or any shell command interpolating user-controlled text into double-quoted arguments), capture the value into a Bash variable first (TITLE=$(git log -1 --format='%s')) and pass \"$TITLE\" — Bash does not perform word-splitting on the right-hand side of assignment, so the variable captures literal content including embedded quotes/backticks/dollars/bangs.",
      "rationale": "GH#17 Phase 3 review caught a Major title-quoting hazard in skills/crafter-do/SKILL.md Step 9b:  would break on commit subjects containing special characters. The variable-capture pattern is the standard Bash-safe form. Alternative: gh ≥ 2.40 supports --title-file - for stdin input.",
      "sourceTask": "20260510-feat-gh-17-pr-composer",
      "confidence": "low",
      "addedAt": "2026-05-10T02:56:42Z",
      "appliedCount": 25,
      "deprecated": false
    },
    {
      "id": "f5813d301291993b",
      "agent": "reviewer",
      "rule": "When reviewing agent prompt additions that define exhaustive decision trees (catch-all defaults), verify that any cross-references in other files accurately describe whether the agent can or cannot produce 'unclassifiable' outputs. Exhaustive decision trees by definition cannot emit escape-hatch signals — cross-reference claims to the contrary are factual errors.",
      "rationale": "Reviewer's --auto classification had auto-fixable as catch-all default, making blocker emission impossible, but do-workflow.md claimed the Reviewer emits blockers. Caught as Major finding in review.",
      "sourceTask": "20260510-feat-gh-18-auto-agent-semantics",
      "confidence": "low",
      "addedAt": "2026-05-10T10:53:38Z",
      "appliedCount": 16,
      "deprecated": false
    },
    {
      "id": "bd3f8b7aedc31896",
      "agent": "implementer",
      "rule": "When adding conditional sections to agent prompt files (e.g., --auto mode sections), ensure cross-references written in companion documentation files (like rules/*.md) accurately describe the actual behavior of the implemented section — especially regarding which agents can and cannot emit specific signals.",
      "rationale": "Implementer wrote escape hatch summary in do-workflow.md claiming all three agents emit blockers, but the Reviewer's exhaustive decision tree makes blocker emission impossible. The cross-reference was written against intent rather than implementation.",
      "sourceTask": "20260510-feat-gh-18-auto-agent-semantics",
      "confidence": "low",
      "addedAt": "2026-05-10T10:53:45Z",
      "appliedCount": 24,
      "deprecated": false
    },
    {
      "id": "65e35a405a47a0f7",
      "agent": "planner",
      "rule": "When an instruction/guardrail must influence user-facing prose generated by agents, place the full content in a single source-of-truth rule (rules/core.md) and add only a one-line POINTER in the prose-generating agent prompts (planner/implementer/reviewer) — never duplicate the content into agents. This reinforces adherence at the point of generation while avoiding cross-file content drift.",
      "rationale": "User asked whether a single core.md rule would be strong enough against lexical priming; the resolution was single-source content + thin agent pointers, which the reviewer confirmed preserved single-source-of-truth with no duplication drift.",
      "sourceTask": "20260519-chore-crafter-jargon-context-bleed.md",
      "confidence": "low",
      "addedAt": "2026-05-19T21:10:43Z",
      "appliedCount": 5,
      "deprecated": false
    },
    {
      "id": "1baec7758ead97c6",
      "agent": "implementer",
      "rule": "When adding or mirroring an install.sh 'node -e' settings.json edit (like install_hook()/install_statusline()), wrap the script body in an IIFE — Node v26 runs '-e' scripts in module context where a top-level 'return' is a SyntaxError, so an early-return pattern silently breaks every invocation with exit 1.",
      "rationale": "install_statusline() used a top-level return inside node -e; it passed bash -n but failed at runtime on Node v26 until wrapped in (function(){...})(). Caught only by an install test, not syntax check.",
      "sourceTask": "20260601-plan-progress-statusline.md",
      "confidence": "low",
      "addedAt": "2026-06-02T13:11:33Z",
      "appliedCount": 10,
      "deprecated": false
    },
    {
      "id": "dcf23111e8c65ece",
      "agent": "implementer",
      "rule": "When emitting JSON for a user to paste that embeds a shell command, apply TWO escaping layers: build a JS object and serialize with JSON.stringify (JSON layer), AND POSIX single-quote-escape (' -\u003e '\\''') any value embedded inside an outer bash -c '...' (shell layer). Hand-concatenating JSON breaks on quoted paths; skipping the shell escape breaks for commands containing single quotes (awk/jq/sed).",
      "rationale": "The statusline collision guidance failed twice in review: first the printed block was invalid JSON (quoted binary path concatenated raw), then valid-JSON-but-invalid-shell when the existing command contained single quotes.",
      "sourceTask": "20260601-plan-progress-statusline.md",
      "confidence": "low",
      "addedAt": "2026-06-02T13:11:33Z",
      "appliedCount": 10,
      "deprecated": false
    },
    {
      "id": "721cca4ba8dcd1c7",
      "agent": "implementer",
      "rule": "Documentation examples that show computed/rendered output (progress bars, percentages, formatted strings) must be verified against the actual renderer's math, and any correction must be applied to ALL mirrored occurrences across docs (README + ARCHITECTURE.md), not just the first one found.",
      "rationale": "The bar example '[#######...] 58%' was inconsistent with filled=pct/10 (should be 5 filled, not 7); the first fix updated only README and phase verification caught the same stale example still in ARCHITECTURE.md.",
      "sourceTask": "20260601-plan-progress-statusline.md",
      "confidence": "low",
      "addedAt": "2026-06-02T13:11:33Z",
      "appliedCount": 10,
      "deprecated": false
    },
    {
      "id": "8997205e7bbc9be6",
      "agent": "verifier",
      "rule": "When step-drift-checking or phase-verifying a step in a multi-phase task where earlier phases are already committed, inspect the WORKING-TREE diff (git diff -- \u003cfiles\u003e / git status --short), never 'git diff HEAD~1 HEAD'. The latter shows the prior phase's already-committed-and-reviewed work and makes you report it as scope drift for the current step — a false positive.",
      "rationale": "In the statusline full-panel task, the verifier ran 'git diff HEAD~1 HEAD' on a tests-only step and flagged a +33-line cli/cmd/statusline.go change as scope drift; that change was actually the prior committed Phase 2, not the step under review. The working tree only contained the test file.",
      "sourceTask": "20260602-feat-statusline-full-panel.md",
      "confidence": "low",
      "addedAt": "2026-06-03T13:39:49Z",
      "appliedCount": 5,
      "deprecated": false
    },
    {
      "id": "df7c4d38ffe78629",
      "agent": "implementer",
      "rule": "When writing guard/degradation tests for a CLI command that falls back to os.Getwd() on missing/malformed input (e.g. the statusline silent-fail path), make the test hermetic with t.Chdir(t.TempDir()). Injecting workspace.current_dir via the JSON payload does NOT cover the malformed-JSON / empty-stdin cases (the payload is unparseable, so the fallback to os.Getwd() fires) — without chdir those subtests resolve to the live repo and silently render a full panel instead of exercising the degraded path their names imply.",
      "rationale": "Phase 3 review of the statusline task found the malformed-JSON and empty-stdin guard subtests were non-hermetic: they fell through to os.Getwd()=repo root and produced a populated panel. Fixed by t.Chdir(t.TempDir()) (Go 1.26 in cli/go.mod).",
      "sourceTask": "20260602-feat-statusline-full-panel.md",
      "confidence": "low",
      "addedAt": "2026-06-03T13:39:49Z",
      "appliedCount": 7,
      "deprecated": false
    },
    {
      "id": "4d17c2ec7f978016",
      "agent": "implementer",
      "rule": "When a bash installer must show an interactive prompt only for a specific case that the Go binary classifies (e.g. a 'foreign' settings.json collision), do NOT prompt unconditionally whenever the feature flag is passed. Add a non-interactive --classify/dry-run mode to the Go command that reuses the canonical classifier and prints the rung, then gate the bash prompt on that rung. Otherwise the prompt fires in the wrong states (absent/ours).",
      "rationale": "Phase 4's first cut prompted on every interactive --with-statusline install because bash never learned the absent/ours/foreign rung; review flagged it Major and it was fixed via classify-then-apply.",
      "sourceTask": "20260603-feat-statusline-install-replace",
      "confidence": "low",
      "addedAt": "2026-06-04T22:07:49Z",
      "appliedCount": 0,
      "deprecated": false
    },
    {
      "id": "c170d6a2f778b443",
      "agent": "implementer",
      "rule": "Install tests (tests/test_install.sh) that assert on mutated settings.json MUST run against a real mutation-capable crafter binary (the _make_real_go_bin_dir helper / source-build path), never the echo-only _make_fake_go_bin_dir shim — the fake binary cannot mutate JSON, so such assertions pass vacuously.",
      "rationale": "Across the node-\u003eGo settings-mutation migration (Phases 2-4) every settings-edit test had to be pointed at the real binary; the echo-only fake would silently make the assertions vacuous.",
      "sourceTask": "20260603-feat-statusline-install-replace",
      "confidence": "low",
      "addedAt": "2026-06-04T22:07:49Z",
      "appliedCount": 0,
      "deprecated": false
    },
    {
      "id": "63eb985bd44c248a",
      "agent": "implementer",
      "rule": "When removing a 'mkdir -p' (or any directory creation) from install.sh, check whether it TRANSITIVELY created a PARENT directory that later operations depend on — e.g. 'mkdir -p $HOME/.claude/hooks' also creates $HOME/.claude, which the settings-write path needs. An explicit-reference grep for the removed variable is insufficient; trace which downstream writes rely on the directory existing.",
      "rationale": "In this task, Step 2.2 removed 'mkdir -p $HOME/.claude/hooks' from install_hook() after a grep confirmed nothing referenced the hooks_dir local. The grep was correct for explicit references but missed that the mkdir also created $HOME/.claude, which the local-install claudesettings.Save() relied on; the local-install hook test then failed in Step 2.3, requiring a MkdirAll fix in store.go.",
      "sourceTask": "20260605-feat-statusline-auto-install-go-hook.md",
      "confidence": "low",
      "addedAt": "2026-06-07T19:58:35Z",
      "appliedCount": 0,
      "deprecated": false
    },
    {
      "id": "2685086b3adfaa1b",
      "agent": "reviewer",
      "rule": "When reviewing install-suite (tests/test_install.sh) tests that assert the presence or absence of a settings.json mutation (e.g. a statusLine or hook entry), confirm the test uses the REAL-binary harness (_make_real_go_bin_dir / _build_real_crafter_bin). The echo-only fake-go shim never mutates settings.json, so a presence/absence assertion run without the real binary is vacuous — it passes trivially because the file is never written.",
      "rationale": "A pre-existing 'local default install no statusline' test lacked the real-binary harness, so its absence assertion never fired (settings.json was never created). When inverting it to a default-on presence assertion, the harness had to be added to make the check meaningful. Vacuous presence/absence install tests can pass surface review while proving nothing.",
      "sourceTask": "20260605-feat-statusline-auto-install-go-hook.md",
      "confidence": "low",
      "addedAt": "2026-06-07T19:58:39Z",
      "appliedCount": 1,
      "deprecated": false
    },
    {
      "id": "c4a9f5fd285d9b28",
      "agent": "verifier",
      "rule": "When verifying the diff surface of a feature branch whose earlier phases were already squash-merged to main (and main then merged back into the branch), compute the cumulative diff against the CURRENT merge-base and expect already-merged files to net to zero — their absence from the branch diff is benign, not a regression. Use a content grep (e.g. for the expected normalized token) as the tiebreaker, and md5/byte-compare against main, rather than treating missing files as lost work.",
      "rationale": "In Step 3.3 of the progressive-loading task the Phase-1 normalize-classified files were absent from 'git diff merge-base..HEAD', which looked like a possible regression. It was benign: Phase 0+1 had squash-merged to main via PR #38 before the PR #44 merge-base, so those files were byte-identical to main and netted to zero in the branch diff. The grep-for-zero-remaining-literals check plus md5-vs-main confirmed normalization was intact.",
      "sourceTask": "20260518-crafter-do-progressive-loading.md",
      "confidence": "low",
      "addedAt": "2026-06-09T10:19:09Z",
      "appliedCount": 0,
      "deprecated": false
    }
  ]
}